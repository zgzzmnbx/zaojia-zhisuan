# 深度研究提示词：管勘智算调整系数匹配规则提炼

创建日期：2026-06-14

## 使用说明

把下面整段提示词复制给 ChatGPT 深度研究或其他具备文件阅读能力的 AI。请同时上传或提供提示词中列出的资料文件。

---

## 提示词正文

你是一名熟悉工程勘察设计收费、测绘生产成本定额、长输管道工程勘察测量最高投标限价编制、工程造价审核和 Excel 规则抽象的资深专家。你的任务不是写泛泛的研究报告，而是把我提供的收费标准、定额、项目样表、已有 AI 调研材料，提炼成后续 Codex 程序可以直接读取、实现和测试的 Markdown 规则文件。

### 一、项目背景

我正在开发一个本地软件智能体，项目名称为：

```text
管勘智算-长输管道勘察测量最高投标限价编制智能体
```

当前软件已经可以根据二维知识库匹配部分表格中的 `H-基价`。现在需要继续构建两类待填项的知识库或规则：

1. `实物工作费附加调整系数`
2. `技术工作费调整系数`

最终目标是让 Codex 根据你输出的 Markdown 规则，继续实现程序逻辑或构建结构化知识库，从而自动填写长输管道勘察测量控制价 Excel 中的待填项。

### 二、请重点研究的 Excel 工作簿和目标 sheet

项目样表示例文件：

```text
【项目例子】铜梁江津-遵义-贵阳-可行性研究勘察测量控制价 -v2.1【批注】.xlsx
```

重点 sheet 和目标列如下。

#### 1. 表2-通用工程测量费用

需要研究和填充：

```text
H-基价
M-实物工作费附加调整系数
P-技术工作费调整系数
```

已知表头结构大致为：

```text
要素1、要素2、要素4、要素5、单位、基价、实物工作费调整系数、技术工作费调整系数
```

当前样例中存在类似：

```text
不造标系数0.6
技术工作费调整系数 0.22
```

#### 2. 表3-地质测绘

需要研究和填充：

```text
H-基价
AC-实物工作费附加调整系数
AF-技术工作费调整系数
```

请注意：用户原始描述中曾把 AF 也写成“实物工作费附加调整系数”，但从表结构看，AF 更可能是技术工作费调整系数，请你核对原表、公式和标准依据后明确判断。

当前样例中可能存在：

```text
AC 附加调整系数
AE 技术工作类别，例如甲级
AF 技术工作费调整系数
```

#### 3. 表4-通用工程勘察费用

需要研究和填充：

```text
H-基价
M-实物工作费附加调整系数
P-技术工作费调整系数
```

当前样例中存在类似：

```text
泥浆护壁系数1.5
技术工作类别甲级
```

### 三、请研究的资料

请优先阅读和引用以下资料。输出时必须标明每条规则来自哪个文件、哪个章节、哪张表、哪个条款或你能定位到的依据位置。

#### 1. 主要标准资料

```text
财建[2009]17号-测绘生产成本费用定额.pdf
计价格[2002]10号-工程勘察设计收费标准使用手册.pdf
计价格[2002]10号-国家计委、建设部关于发布《工程勘察设计收费管理规定》的通知.pdf
```

#### 2. 已有 AI 调研材料

```text
研究报告-chatgpt2026年6月8日生成-长输管道工程勘察测量最高投标限价智能体开发分析报告.docx
```

#### 3. 项目样表

```text
【项目例子】铜梁江津-遵义-贵阳-可行性研究勘察测量控制价 -v2.1【批注】.xlsx
```

#### 4. 当前已有基价知识库

```text
【数据库】【导入】.xlsx
```

这个文件主要解决 `H-基价`，你可以参考它的二维化方式，但本次重点是构建调整系数规则。

### 四、你要解决的核心问题

请围绕下面两个问题输出可落地规则。

#### 问题 A：如何构建“实物工作费附加调整系数”规则或知识库？

请回答：

1. 哪些 sheet、哪些业务类别需要填写实物工作费附加调整系数？
2. 这些系数的依据是什么？
3. 哪些系数是标准明确规定的？
4. 哪些系数来自样表经验、工程经验、市场经验或当前表格批注？
5. 如果同一行同时存在多个调整因素，应该如何组合？
   - 相乘？
   - 取最大值？
   - 按主导因素取一个？
   - 需要人工复核？
6. 表2、表3、表4 的规则是否相同？如果不同，请分 sheet 说明。
7. 应该用结构化知识库表来实现，还是用规则引擎来实现，还是两者结合？

#### 问题 B：如何构建“技术工作费调整系数”规则或知识库？

请回答：

1. 技术工作费调整系数是否主要由技术工作类别决定？
2. 甲级、乙级、丙级等类别分别对应什么系数？
3. 表2、表3、表4 是否都适用同一套技术工作费调整系数？
4. 如果 Excel 中已经有“甲级”等类别字段，程序应如何映射到系数？
5. 如果缺少类别字段，应默认什么？还是输出待复核？
6. 哪些情况下技术工作费调整系数不能自动填写，必须人工复核？

### 五、请输出 Codex 可直接使用的 Markdown 文件

请不要只输出一篇研究报告。请按下面结构输出 Markdown。每个 Markdown 文件都要能被 Codex 后续读取并据此实现代码。

#### 文件 1：`调整系数匹配规则总说明.md`

必须包含：

```markdown
# 调整系数匹配规则总说明

## 1. 适用范围
## 2. 输入 Excel 中涉及的 sheet 和目标列
## 3. 两类调整系数的定义
## 4. 与 H-基价匹配逻辑的关系
## 5. 自动填写、默认填写、待复核的边界
## 6. 资料依据清单
## 7. 推荐实现方式
## 8. Codex 实现注意事项
```

#### 文件 2：`实物工作费附加调整系数规则.md`

必须包含：

```markdown
# 实物工作费附加调整系数规则

## 1. 规则总览
## 2. 表2-通用工程测量费用规则
## 3. 表3-地质测绘规则
## 4. 表4-通用工程勘察费用规则
## 5. 调整因素字典
## 6. 组合规则
## 7. 待复核规则
## 8. 推荐知识库表结构
## 9. 示例规则表
## 10. 测试样例
```

其中“推荐知识库表结构”必须给出字段设计，例如：

```markdown
| sheet范围 | 要素1 | 要素2 | 要素3 | 要素4 | 要素5 | 单位 | 触发条件字段 | 触发条件关键词 | 系数 | 依据文件 | 依据章节/表号 | 适用说明 | 冲突处理 | 置信度 |
```

“示例规则表”必须至少给出 10 条规则样例。如果资料不足以给出 10 条，必须说明不足原因，并给出待补资料清单。

#### 文件 3：`技术工作费调整系数规则.md`

必须包含：

```markdown
# 技术工作费调整系数规则

## 1. 规则总览
## 2. 技术工作类别与系数映射
## 3. 表2-通用工程测量费用规则
## 4. 表3-地质测绘规则
## 5. 表4-通用工程勘察费用规则
## 6. 默认值规则
## 7. 待复核规则
## 8. 推荐知识库表结构
## 9. 示例规则表
## 10. 测试样例
```

其中“技术工作类别与系数映射”必须输出结构化表格，例如：

```markdown
| 技术工作类别 | 系数 | 适用范围 | 依据文件 | 依据章节/表号 | 备注 |
```

#### 文件 4：`Codex实现任务清单.md`

请站在后续 Codex 开发的角度，输出实现任务清单：

```markdown
# Codex实现任务清单

## 1. 需要新增的数据文件
## 2. 需要新增或修改的后端模块
## 3. 需要新增或修改的前端界面
## 4. 需要新增的测试样例
## 5. 验收标准
## 6. 风险和人工确认点
```

### 六、输出规则要求

请严格遵守：

1. 不要编造标准条文。
2. 如果依据不足，请写“依据不足，建议人工确认”，不要硬给确定规则。
3. 每条规则必须标注来源类型：
   - `标准明确`
   - `样表经验`
   - `AI推断`
   - `人工待确认`
4. 每条规则必须有置信度：
   - `高`
   - `中`
   - `低`
5. 能结构化成表格的，一律结构化成表格。
6. 能转成程序判断条件的，要写成“如果……则……”。
7. 所有待复核场景必须列清楚。
8. 不要输出大段空泛背景介绍。
9. 不要只解释标准，要面向 Codex 后续实现。
10. 输出中必须保留原始术语，不要随意改名；例如 `实物工作费附加调整系数`、`技术工作费调整系数`、`基价`、`要素1` 至 `要素5`、`单位`。

### 七、请重点检查的具体判断点

请特别检查并回答：

1. 表2 中 `0.6` 是否应解释为“不造标系数”？它的适用边界是什么？
2. 表2 中技术工作费调整系数 `0.22` 的标准依据是什么？是否所有表2行都适用？
3. 表3 中 `AC` 列的 `1`、`1.3` 等系数分别由什么因素触发？
4. 表3 中 `AF` 列是否属于技术工作费调整系数？甲级对应系数是否为 `1.2`？
5. 表4 中 `M` 列 `1.5` 是否与“泥浆护壁”相关？适用哪些钻探场景？
6. 表4 中 `P` 列是否由 `甲级/乙级/丙级` 等技术工作类别决定？
7. 如果 Excel 备注列、取费系数说明列、收费标准里对应条款列中出现关键词，能否作为规则触发条件？
8. 如果多个关键词同时出现，是否应叠加系数？如何防止误叠加？
9. 哪些规则适合做成二维知识库，哪些更适合写成代码规则？
10. 后续 Codex 自动填表时，哪些情况必须输出 `待复核`？

### 八、最终交付格式

请按以下顺序输出：

```text
一、结论摘要
二、调整系数匹配规则总说明.md
三、实物工作费附加调整系数规则.md
四、技术工作费调整系数规则.md
五、Codex实现任务清单.md
六、需要人工补充或确认的资料清单
```

如果输出过长，请分多次回复，但每次回复都必须保持 Markdown 文件结构完整。

---

## 额外提醒

这项工作的最终使用者是 Codex 程序开发，而不是普通读者。因此请把研究结论写成规则、表格、字段、触发条件、依据、测试样例和待复核边界。
