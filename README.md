# 造价智算-工程造价辅助智能体

## 项目简介

造价智算是一个本地运行的工程造价辅助智能体。当前核心场景来自长输管道勘察测量最高投标限价编制，能力已经扩展为“规则引擎 + 经验池预警 + 大模型解释 + Word/Excel 成果输出”的造价数字员工雏形。

本项目不是让大模型直接猜价格。`基价 / 单价`、`实物工作费调整系数`、`技术工作费调整系数` 由结构化计价库、规则表和经验提示层处理；右侧“智算”助手只负责问答、解释、风险报告、导出提示和行级复核。

面向普通读者、汇报对象、参赛评委或其他 AI 的项目说明，以本 README 和 `00-PRD/00-产品总览.md` 为准；早期一页式介绍已归档到 `docs/归档/项目介绍-给人看的版本-历史.md`。

## 当前基线

- 产品名：造价智算
- 定位：工程造价辅助智能体
- 当前版本：v5.14.6
- 当前页面布局：`大尾巴主题 / daweiba` 唯一主界面
- 前端：React + TypeScript + Vite
- 后端：Python + FastAPI
- 当前对外交付入口：传统开发版、Windows 绿色版、Windows Tauri 桌面版
- 桌面壳：Tauri 2，作为开发调试入口和 Windows 桌面 exe 交付入口
- Excel：openpyxl
- Word：python-docx
- 大模型：默认官方 DeepSeek `deepseek-v4-flash`

## 先读哪些文件

新线程或新智能体进入项目，默认先读：

```text
AGENTS.md
README.md
CHANGELOG.md
00-PRD/02-当前版本计划.md
```

再按任务类型读取：

| 任务类型 | 继续读取 |
| --- | --- |
| 产品定位 / 对外表达 | `00-PRD/00-产品总览.md` |
| 某个模块开发 | `00-PRD/01-模块PRD/对应模块/PRD.md` |
| UI / 主题 / 布局 / 交互 | `00-PRD/03-整体UI设计PRD.md`、`00-PRD/03-UI设计规范.md` |
| UI 参考图 / Bug 截图 / 设计笔记 | `01-assets/01-UI参考图/` |
| 三个数字匹配规则 | `03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】项目以及总体匹配规则介绍.md` |
| 要素1-5 + 单位同类识别 | `03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】要素1-5和单位的匹配模式介绍.md` |
| Windows 绿色版 | `docs/绿色版说明.md` |
| Tauri 桌面版 | `docs/Tauri桌面壳MVP说明.md` |
| 统信 UOS 兼容性准备 | `docs/统信UOS兼容性开发说明.md` |

## PRD 入口

当前项目按轻量 PRD 驱动维护：

```text
00-PRD/00-PRD工作方式说明.md
00-PRD/00-产品总览.md
00-PRD/01-模块PRD/模块名/PRD.md
00-PRD/02-当前版本计划.md
00-PRD/03-整体UI设计PRD.md
00-PRD/03-UI设计规范.md
01-assets/01-UI参考图/
00-PRD/04-PRD一致性巡检.md
00-PRD/05-线程交接提示词/
```

PRD 备份改为手动触发：只有明确需要备份 PRD 时，才运行 `python tools/archive_prd.py` 或双击 `备份PRD.bat`；日常修改 PRD 不自动备份。

PRD 与项目现状的硬一致性检查：

```powershell
python tools/check_prd_consistency.py --strict
```

该脚本只检查结构、状态标记、关联资产路径和旧路径残留，不替代人工验收 `[已完成]` 是否真的完成。

## 当前能力概览

### 选择专业能力（Skill）

- v5.10.0 已完成专业 Skill P0-A 选择与登记骨架：全局导航栏提供默认折叠的“专业能力”入口，当前默认且唯一可创建任务的能力为“勘察测量最高投标限价编制” `v1.0.0`。
- 能力详情按“专业专用 / 通用能力复用”展示子 Skill 构成：勘察测量旗舰能力登记 10 项已启用模块；通用服务类登记 9 项规划模块，并明确只有真实跑通“上传—识别—计算—复核—导出”后才能进入内测。
- `business-skills/` 保存结构化 Manifest、说明和验收资产；Manifest 只引用现有结构化计价库、规则、模板和样例，不执行脚本，不保存密钥，`SKILL.md` 不参与价格或系数裁决。
- v5.12.0 已完成 P0-B 运行时接口：任务创建时把输入规范、白名单处理器、结构化计价库、规则、经验池 / 阈值、知识源、报告模板和验证信息冻结为 `SkillRuntimeContext`；批量匹配、预警、知识问答、报告和下载只读取该任务上下文。
- v5.14.0 已完成 P1 文件特征推荐与开放格式包装：用户选择真实 Excel 后，系统只读取文件名、有限工作表名和有限表头生成可解释推荐，不自动切换，必须由用户确认；三个能力目录均提供 `SKILL.md`，但程序不从自然语言说明中解析价格或系数规则。
- v5.14.0 已完成 P2 管理中心安全接口：可盘点本机能力包、查看开放格式并生成安装、启停、升级、回滚、卸载、导出的审核计划；当前为 `governed-interface` 只读模式，所有写操作关闭，不导入未知包、不执行外部脚本。
- 现有勘察测量流程继续复用同一 `FillEngine`，处理器只能取代码白名单 `survey-measurement-v1`，Manifest 不能声明模块、命令或脚本；旧任务缺少运行时字段时只走明确的勘察测量兼容上下文。
- 测试专用第二 Skill 已验证不同计价库、技术规则、经验池、知识源和 Word 模板不串用，且切换默认 Skill 不改变已创建任务。该测试 Skill 不进入用户清单，不代表“通用服务类造价测算”已有真实业务数据。
- “通用服务类造价测算”当前诚实标记为“规划中”，可查看边界，但不能创建真实任务。
- “更多造价专业”作为第三项扩展占位，标记为“即将开放”，用于表达专业能力将持续扩充，同样不能创建真实任务。

详细需求见：

```text
00-PRD/01-模块PRD/15-专业能力包与Skill管理/PRD.md
```

### 对话式智能体工作台

- v5.11.0 已完成 P0 对话式工作台：左侧“智算助手”一级入口与右侧 Dock 的“进入智算助手”会进入同一个宽屏会话工作区。
- Dock 与宽屏模式直接复用同一消息流、输入草稿、当前文件、任务、行级上下文和等待状态；切换模块或按 `Esc` 退出不会卸载专业工作区。
- 底部输入区接入 Excel 附件、Registry 真实专业能力、任务上下文和发送 / 停止；规划中 Skill 不可执行，任务创建后锁定 Skill 快照。
- 会话内的上传、开始转换、批量匹配、经验池预警、风险清单、报告和 Excel / Word 下载均调用现有真实业务链路；结果预览和 Word 精查可无损进入原专业页面。
- 当前会话 UI 已完成简约化收口：助手消息使用左侧无框正文，用户指令使用右侧浅色气泡并按轮次分割；输入区支持 Excel 拖拽，任务操作默认折叠，Skill 包使用浅蓝标签和自绘向上菜单；点击快捷操作会先形成右侧用户指令，再由助手反馈真实执行过程。
- 当前只有“勘察测量最高投标限价编制”是已上线真实 Skill；P0-B 底座接口已完成，第二个真实专业能力仍待业务资料、结构化数据、规则、风险口径和成果模板补齐。

详细需求见：

```text
00-PRD/01-模块PRD/16-对话式智能体工作台/PRD.md
```

### Excel 填价与三数字匹配

- 输入标准控制价 Excel，识别空单价、空白价格或包含“空单价”的价格列。
- 主界面采用两阶段体验：点击“开始转换”先生成待匹配预览；在预览窗口点击“批量匹配”后，再执行基价 / 单价、实物工作费调整系数和技术工作费调整系数的批量识别填写。
- 使用结构化计价库匹配 `基价 / 单价`。
- 技术工作费调整系数走第一层标准规则；实物工作费调整系数第一层暂未启用。
- 第一层命中标绿，第二层经验提示标黄，未命中 / 待复核标红。

详细需求见：

```text
00-PRD/01-模块PRD/01-Excel填价与三数字匹配/PRD.md
```

### 项目看板与历史项目（P0 待开发）

- P0 产品设计和新任务开发交接已完成；左侧继续使用“填价工作台”，中间增加“项目看板｜新建填价”双视图，不新增一级菜单。
- 项目看板将建立真实项目台账，严格区分项目、处理任务、业务版本和复核轮次，并汇总网页、对话式工作台和智能协同任务。
- 首屏规划真实 KPI、项目趋势柱状图、状态环形图、风险排行、匹配质量和历史项目表；图表与列表共用后端筛选口径，不使用前端假数据。
- 视觉参考指定 Mobbin Dashboard 的信息组织，并以 shadcn/ui preset `b27I2r68` 的 Rhea / Neutral / Blue / Inter / Lucide 作为组件质感基准；采用 `.project-dashboard` 局部转译，不全量引入 Tailwind / shadcn，最终颜色、状态语义和响应式仍服从项目 UI 规范。

详细需求和开发交接见：

```text
00-PRD/01-模块PRD/17-项目看板与历史项目/PRD.md
00-PRD/01-模块PRD/17-项目看板与历史项目/开发提示词.md
```

### 填价结果预览与输出

- 输出回填后的 Excel 和 Word 报告。
- 预览支持多 sheet、表头行偏好、显示列偏好、隐藏指定列无值行、最大列宽、按 `sheet 名 + 列标题` 记忆手动列宽、公式兜底计算；表格预览、列映射、工作量抓取和问问智算窗口的项目默认值统一来自 `config/project-default-settings.json`，弹窗内修改只临时生效，新用户和绿色版首次打开沿用该项目默认口径。
- 后续新增项目级默认设定时，默认写入 `config/project-default-settings.json`；仅密钥、运行环境、个人界面状态或技术边界明确不适合的设定可例外，例外必须说明原因、存放位置和生效范围。
- 下载 Excel 与填价结果预览共用隐藏空行口径；第一列以 `合计` 开头的汇总行始终保留。
- 结果预览支持人工修改输出 Excel 副本的普通值单元格；保存单元格时快速写入输出 Excel 和预览，必要时再手动点击“重算公式”刷新汇总公式和 Word 报告，并在 Excel 单元格和运行目录留存修改记录。
- “开始转换”只生成待匹配预览，不自动跳页；右侧智算引导进入表格预览，预览窗口、智算按钮、快捷指令和输入“批量匹配”共用同一批量匹配动作。

详细需求见：

```text
00-PRD/01-模块PRD/02-填价结果预览与输出/PRD.md
```

### 辅助填价与人工复核闭环

- 当前已完成基价 / 单价辅助填价 MVP：结果预览固定 `AI` 列中的书本图标打开弹窗，展示知识库相似候选、经验池同类均值候选和当前行价格轻量追溯。
- 辅助填价只做候选组织和人工确认写入，不让大模型生成数字，不改变主匹配引擎，不反写原始输入、知识库、规则表或经验池。
- 后续已独立成 P1 模块：扩展到实物工作费调整系数、技术工作费调整系数，补齐自定义值备注、分字段人工确认、候选配置和复核处理状态。

详细需求见：

```text
00-PRD/01-模块PRD/09-辅助填价与人工复核闭环/PRD.md
```

### 经验池预警

- 经验池独立于正式知识库，只用于转换后比选预警，不参与主匹配和第二层经验提示。
- 初次转换不自动运行预警，用户点击“运行经验池预警分析”后才写入 `预警参数`、`预警细节`。
- 当前同类识别按 `字段完全匹配 -> 非空要素顺序匹配 -> 未找到可比选同类记录` 执行。
- v5.4.0 后，经验池页新增只读治理报告，可检查重复记录、单位不一致、样本不足、极端离散、关键字段空值和经验数不可解析，并生成 Markdown 报告；治理不自动清洗、不反写正式知识库、不改变预警算法。

详细需求见：

```text
00-PRD/01-模块PRD/03-经验池预警/PRD.md
```

### 问问智算 AI 助手

- 大模型不参与价格裁决，只用于风险报告、行级复核、问答、导出提示和过程解释。
- v5.11.1 已修复正式标准资料目录迁移后的索引发现：知识问答同时兼容当前 `01-【业务资料】原始资料-勘察测量相关基础资料` 与旧 `01-原始资料`，并继续把其中 Markdown / Excel / CSV 标记为正式标准来源；不扩张 PDF OCR，不修改正式资料内容和三数字匹配。
- v5.12.0 起，任务内知识问答按任务冻结的 Skill 知识源建立独立索引；未创建任务时按当前可执行 Skill 检索，专业正式资料不会跨 Skill 串用。
- v5.4.0 后，后端提供结构化风险清单，把待复核行和经验池偏离统一为可跳转风险项；Word 报告页可查看风险清单，原大模型风险报告仍只做说明和润色。
- 知识库问答先由本地程序检索 Markdown / Excel / CSV 证据，再交给 DeepSeek 做依据解释。
- 用户以 `查库：`、`查库:`、`@知识库`、`#知识库` 开头提问时，会强制进入知识库检索模式，界面显示“已调用知识库”。
- `@知识库` 已按三层方案优化：补充功能类知识卡，行级复核固定携带关键诊断字段，风险报告、导出、行级 AI 等功能类问题可命中本地证据。
- 价格类 `@知识库` 问答优先检索结构化计价库；条件明确时引用精确行，条件模糊时列出相似候选项供用户选择，不让大模型凭空报价格。
- `03-【匹配规则】-勘察测绘知识库-匹配规则提炼/01-【业务资料】原始资料-勘察测量相关基础资料/` 下的 `计价格[2002]10号`、`财建[2009]17号` Markdown 和批注 Excel 已纳入问问智算检索；PDF 原件作为资料留存，不直接解析。
- v5.9.2 将通用知识改为按类型审核：操作方法和一般解释可按项目默认配置自动通过；项目口径、价格 / 调整系数、正式标准，以及与同范围既有知识存在冲突的内容一律进入人工确认。用户仍可点击“保存为知识候选”核对内容，也可输入“记住这一条”保存最近的结构化候选。
- 知识记忆支持同范围重复复用、相似项与冲突提示、确认后更正版本、项目知识提升为通用候选；自然语言还支持“只在当前项目记住”“把上一条改成……”“忘掉上一条”和“查看刚保存的知识”。更正版本只在新版确认后替代旧版；提升为通用不会让原项目知识失效。
- 项目和任务范围仍保留人工提交、确认、驳回、撤销和疑似失效流程，只有同一 `project_key` 下已确认内容参与问答；通用知识可被所有项目检索，正式标准和规则始终优先。自动通过类型和重复相似度阈值由 `config/project-default-settings.json` 的 `knowledgeMemory` 统一配置。
- 知识记忆使用 `Codex-Temp/runtime/knowledge-memory.sqlite3` 保存当前记录、版本和审计；它是 Git 忽略的运行数据，不是知识问答索引缓存。数据库不可用时自动降级为原有正式知识库问答，不影响 Excel 填价、预警、Word 报告和其他专业主流程。
- 当前操作人和确认角色属于本地试点能力，不等于企业级身份认证。后续生产化需接入可信身份、组织权限和 WeAct 待确认提醒；WeAct 只做提醒和安全跳转，不直接批准知识。
- v5.5.0 后，右侧“问问智算”身份区和折叠入口已接入极简可动机器人形象；待命态为蓝色呼吸光晕，思考 / 处理态为浅绿色呼吸和放大镜工作动效，预警 / 异常 / 完成有独立符号反馈；该形象只表达状态，不参与业务裁决或写回。
- 右侧“问问智算”的聊天区高度、Dock 宽度、纵向高度偏好、欢迎语、快捷指令、外观风格和显示项统一由 `config/project-default-settings.json` 的 `zhisuanWindow` 维护；设置页调整仅作用于当前会话，默认 Dock 宽度为 `400px`。

详细需求见：

```text
00-PRD/01-模块PRD/04-问问智算AI助手/PRD.md
docs/问问智算项目知识记忆说明.md
```

### 原始工作量抓取

- 从委托方工作量表按 `要素1-5 + 单位` 抓取数量、两个系数和备注。
- 先走工作量专用术语归并规则，再按 `字段完全匹配 -> 非空要素顺序匹配` 执行。
- 抓取结果直接写入当前表格预览对应的控制价输出副本；保留工作量抓取标注表下载用于核对漏项，不再单独下载已抓取控制价表；该模块不改变主价格识别逻辑。

详细需求见：

```text
00-PRD/01-模块PRD/05-原始工作量抓取/PRD.md
```

### Word 报告

- 按模板生成控制价报告，自动填日期、项目名称、费用汇总金额、匹配摘要和风险提示。
- 报告读取费用汇总时优先使用 Excel 缓存值；缓存为空时通过程序侧公式兜底计算常见汇总公式。
- v5.7.0 已在现有左侧“Word 报告”页完成真实 DOCX 只读预览：采用 `@file-viewer/react@2.1.25` 与独立 Word renderer，复用当前报告下载接口，把 Blob 封装为带 `.docx` 文件名的 `File`，并将 Worker / JSZip 同源本地化。页面支持加载、报告更新、失败恢复、任务切换和手动刷新，开发版、绿色版与 Tauri 均不依赖 CDN 或在线 Office。
- v5.7.1 修复长风险报告只停留在 3 个结构页的问题：保留模板页脚和页码，以分页容差忽略浮动页码文本框的固定装饰溢出，同时恢复有上限的正文动态续页；实际下载报告已从浏览器 3 页正确续排为 7 页，尾部内容完整可见。

详细需求见：

```text
00-PRD/01-模块PRD/06-Word报告生成/PRD.md
00-PRD/01-模块PRD/12-报告预览模块/PRD.md
```

### 智能协同

- v5.14.6 修复企业 WeAct 任务卡首次点击偶发失败：任务专属 Excel 改为先投递，成功后才开放“领取任务”卡片；领取以已成功送达的文件和待领取状态为准，消除卡片可见但文件状态尚未落库的竞态。机器人启动时预热复核任务运行库，避免首个卡片回调临时初始化；“创建并投递任务”调整为蓝色主按钮。
- v5.14.5 为多人复核卡新增经典卡片评论输入框：复核人可在“复核通过”或“退回编制”前填写最多 500 字评论；通过时评论可选，退回时评论必填。评论按复核人和轮次写入运行库、审计动作、后续卡片与智能协同台账，下一轮复核开始时重置本轮输入但保留历史动作。
- v5.14.4 新增 `@辅助审核` 确定性口令，与 `@上传` / `@上传文件` 共用原 1 分钟收件窗口和现有专业处理链路；收到 `.xlsx` 后仍按顺序完成匹配、风险识别、Excel 和 Word 输出，不新增第二套审核逻辑。
- v5.14.4 将 WeAct 主动消息受众限制固化为后端硬安全闸门：单个任务和单次发送最多触达 10 名可核验姓名的人员；个人消息只允许任务中明确指定并完成平台映射的编制人、复核人。明确选择工作群时，每次发送前实时核验群成员，无法核验、存在未命名成员或超过 10 人即拒绝发送，不自动扩大或回退收件范围。
- v5.13.0 已在现有“智能协同”页完成 P0-A“外部任务主动派发模拟”，不新增左侧菜单：页面模拟外部造价系统产生标准任务信号，先落库内部任务和 Skill 快照，再通过第二层企业应用机器人向唯一授权“智算测试”群发送经典卡片和任务专用 Excel 副本。
- v5.14.3 已补齐编制成果回传：编制人领取后点击“提交成果并进入复核”，机器人开启 1 分钟任务专属收件窗口；编制人直接发送完成后的单个 `.xlsx`，系统校验任务、人员、会话、时效和文件类型，绑定成果后把同一文件与复核卡投递给全部复核人。卡片回调先即时返回，完成／退回通知转入后台发送，避免业务已经成功但 WeAct 首次点击仍提示“出错了，请稍后重试”。台账同步展示成果文件和投递状态。
- v5.14.2 已完成 P0 简易多人复核：模拟任务依次经过“编制领取 → 提交多人复核 → 全员通过完成”；任一复核人可退回编制并开启下一轮，所有卡片操作均校验真实平台身份、保持幂等并记录轮次。模拟器自动生成外部任务编号和缺省项目名，台账展示编制人、全部复核人及状态，仍不回显平台用户 ID、群 ID 或文件 Key。
- P0 真实演示先使用“智算测试”授权群；即使先走群聊，任务也必须保存目标编制人的稳定平台用户 ID，并由同一投递服务保留 `group / direct` 两种模式。精准单聊需完成企业应用 A 主动触达权限、人员映射和目标可达范围实测后再启用，不按姓名猜人。
- 模拟器和未来真实造价系统共用标准 `TaskEnvelope`、`source_system + source_task_id + event_type` 业务幂等键、任务状态、文件版本和分步投递审计；卡片已发而文件失败时只重试文件，不重复建任务或重复发卡。前端只显示名称、状态和脱敏错误，不回显平台 ID、文件 Key、令牌或密钥。
- v5.8.21 为已通过收件窗口校验的 `.xlsx` 文件消息补充“了解”表情：单聊文件继续回应；群聊只有同会话、同发送人先发送 `@上传` / `@上传文件`，且文件命中原 1 分钟窗口时才回应。群内普通文件、其他格式和未开启收件窗口的文件仍保持静默。
- v5.8.20 修复企业 WeAct 文件事件延迟导致正常上传被误判为历史消息的问题：用户仍须在机器人回复后的 1 分钟内发送 `.xlsx`；如果平台延迟投递，系统以文件的平台发送时间校验原收件窗口，最长只接受 15 分钟内送达的单个 `.xlsx`。普通旧消息、窗口外文件和更长延迟仍按防重放规则静默拦截。
- v5.8.19 修复云端第二层机器人内部调用地址被本地默认配置覆盖的问题：本机继续默认 `127.0.0.1:8000`，云端通过 `FEISHU_APP_BOT_API_BASE_URL=http://127.0.0.1:1285` 强制覆盖；机器人建立业务接收前先检查专业服务健康，地址不可达时拒绝启动而不是接收文件后失败。仓库同时提供 systemd 配置安装脚本和本地 / 云端发布检查器，后续版本若端口覆盖缺失、服务未运行或健康接口异常会直接阻断上线验收。浏览器标题按回环 / Tauri 与非本地主机自动显示“造价智算（本地）”或“造价智算（云端）”。
- v5.8.18 已把第二层机器人身份登记提升为启动硬约束：普通飞书与企业 WeAct 的预期 App ID、域名统一登记在项目默认配置；页面切换、后端子进程拉起和独立运行器启动都会校验当前运行秘密。配置偏离时显示“配置不一致”并拒绝建立长连接，避免本地与云端误用不同 WeAct 应用导致消息随机无响应；App Secret 仍只保存在各环境的 Git 忽略运行目录。
- v5.8.17 已为第二层普通飞书 / WeAct 消息入口增加三层防护：事件 ID 与消息 ID 同时持久去重，平台创建时间超过 5 分钟的过期消息在表情和业务分流前静默拦截，运行控制台记录消息 ID、平台创建时间和本机接收时间。旧数据库自动增量迁移；平台未提供时间的兼容消息继续正常处理。
- v5.8.16 已完成第二层“第一梯队”确定性指令：`任务` / `最近任务` 查询当前会话最近 5 个任务，`进度 FS-任务编号` 查询阶段，`风险 FS-任务编号` 和 `高风险` 查询风险统计，`结果 FS-任务编号` 重新发送原 Excel、Word 和完成卡片，`帮助` / `指令` 返回完整用法。所有任务查询严格限定在任务原群或原单聊，不经过大模型；成果重发不重新计算、不改变原任务状态。
- v5.8.15 已为第二层增加第一版只读完成卡片：Excel 和 Word 成果文件上传成功后，机器人发送绿色卡片汇总任务编号、输入文件、风险总数和高风险数；配置“进入造价智算 URL”后显示安全跳转按钮。卡片不接收业务回调，发送失败自动降级为文字通知，不改变成果和任务状态。
- v5.8.14 已把群成员查询收紧为三条确定性指令：群聊中明确 `@机器人` 后发送“群里有几个人”“群成员”或“都有谁”，机器人通过飞书 / WeAct 群成员接口分页读取真实人数和姓名并返回；三条指令不经过大模型，单聊会提示到目标群中查询，近似说法仍进入普通问答。
- v5.8.13 已修复“群聊 @别人时机器人也搭话”：第二层启动时读取本机器人真实 Open ID，群聊只有 mentions 明确包含本机器人时才添加表情或进入上传、知识库、问候和大模型问答；身份无法确认时群聊保持静默，单聊不受影响。
- v5.8.12 已兼容企业 WeAct 的 `@_user_1` 等动态机器人提及占位符，“你好”固定返回完整自我介绍、Excel 自动处理 / 知识库 / 普通问答功能介绍和使用说明；大模型 TLS、超时或连接瞬断最多有限重试 3 次，真实 DeepSeek 普通问答已验证成功。
- v5.8.11 已统一普通飞书与企业 WeAct 的第二层消息分流：只有 `@上传` / `@上传文件` 开启 1 分钟收件窗口；`@知识库` 继续检索本地依据；`你好` 返回自我介绍和使用说明；其余文字由现有 `/api/llm-chat` 大模型托底。群聊只有明确 @机器人的消息才添加“了解”表情，群内普通聊天不回应；机器人单聊仍可回应。
- v5.8.10 已为第二层机器人增加收件表情确认：长连接收到群聊 `@`消息或机器人单聊消息后，立即在原消息下方异步添加“了解”（`Get`）表情回应；表情接口失败只写运行警告，不阻塞收件、知识库问答或专业任务。
- v5.8.9 已把第二层机器人运行控制台改为“刷新”旁按钮打开的弹窗，并增加业务追踪日志：记录已处理消息的发送人名称与 ID、群名与会话 ID、消息正文和附件名；名称查询无权限时回退显示 ID。飞书长连接事件不提供发送者来源 IP，控制台明确标注平台未提供；App Secret、访问令牌、连接票据、文件 Key 和完整 WebSocket 地址仍不回显。
- v5.8.8 已在智能协同设置页增加“第二层：机器人运行控制台”：每 3 秒可自动刷新长连接建立 / 断开 / 重连、消息接收、知识库问答和任务处理阶段；后端只汇总脱敏结构化事件，不向前端返回 App Secret、访问令牌、连接票据、文件 Key、群 ID、用户 ID 或完整 WebSocket 地址。
- v5.8.7 已把企业 WeAct 长连接实测成果接入第二层正式机器人：第二层明确提供 `默认机器人（普通飞书）` 与 `Weact机器人（管网内网）` 两个选项；Weact 配置同时使用企业 API 域名和企业 WebSocket 节点，并已真实连接 `lark-frontier.weact.pipechina.com.cn`。本机默认启用普通飞书，云端默认启用 Weact，用户仍可在智能协同页随时关闭或切换；两个配置共用单任务顺序队列。
- v5.8.6 已支持第一层多 Webhook 配置选择，并新增 `weact机器人（webhook）`；第一层 Webhook 与第二层企业应用机器人的全部秘密配置统一保存到 Git 忽略的 `Codex-Temp/runtime/feishu-robot-settings.json`，旧版两个分散配置文件可自动迁移。
- v5.8.5 已支持第二层多企业应用机器人配置选择：在智能协同页选择当前机器人，切换时安全退出旧长连接后再启动新配置；新增企业 Weact 配置（现界面名称为 `Weact机器人（管网内网）`），仍共用单任务顺序队列。
- v5.8.2 已增加第二层机器人知识库问答入口：群聊 `@机器人` 后输入 `@知识库：问题`，或在机器人单聊中直接输入该前缀，会异步调用现有本地知识库问答接口并返回答案、来源和边界提示；问答与 Excel 任务分流，幂等且不修改业务结果。
- v5.8.1 已增加两层独立即时开关：第一层可主动关闭 Webhook 通知，第二层可真正停止或自动重启本机长连接。服务器部署与本机试点使用同一应用凭证时，只保留一个第二层实例启用，避免飞书随机把任务分配到不同环境。
- v5.8.0 已完成第二层飞书企业自建应用本机长连接试点：任意已添加机器人的群聊可通过 `@机器人 + 单个 .xlsx` 发起任务，SQLite 全局顺序队列自动复用现有匹配、经验预警、结构化风险、Excel 和 Word 能力，并把摘要及两个成果文件返回原群。
- 第二层字段映射不可靠时停止并转人工，不猜测字段；DeepSeek 风险说明失败时降级但不影响结构化结果。任务原件、过程和成果默认本机保留 30 天，重复事件不会重复建任务。
- 企业应用凭证与第一层 Webhook 配置统一保存在 Git 忽略的 `Codex-Temp/runtime/feishu-robot-settings.json`；启动和飞书控制台配置见 `docs/飞书第二层机器人说明.md`。本期不做审批、多维表格、风险派单、用户角色权限、多文件任务和群内逐行改值。
- v5.8.0 验证：第二层专项 `13 passed`，全量后端 `186 passed, 3 skipped, 2 failed`，两项失败与 v5.7.1 既有资料漂移基线相同；前端构建、PRD 严格巡检、真实飞书长连接握手、绿色版依赖构建与 v5.8.0 健康检查通过。

- v5.6.0 已完成第一层飞书群自定义机器人 Webhook：左侧一级菜单在“知识库问答”下方新增“智能协同”，中间页提供连接状态、凭证保存、签名提示、通知开关、测试发送和最近发送记录，右侧问问智算保持不变。
- 第一层可从现有前端处理链路旁路推送任务开始、关键进度、批量匹配完成和失败通知；完成卡片可配置“进入造价智算”URL，按钮只做网页跳转，不接收卡片回调。
- Webhook 地址和签名密钥只保存在统一机器人配置文件的 `webhook` 分区；发送记录保存在 `Codex-Temp/runtime/feishu-webhook-history.jsonl`。清空配置可在智能协同页完成，状态接口、前端和发送记录均不返回完整凭证。
- 通知是 best-effort 旁路：未配置、禁用、超时、限流和发送失败均不改变 Excel 填价、批量匹配、经验池预警、工作量抓取、问问智算、Word 报告、绿色版和 Tauri 桌面版的业务结论。
- 第二层本机长连接自动处理、真实身份领取、编制成果回传、多人复核、评论和经典卡片回调已完成；生产级角色权限、多维表格台账、风险派单、可选审批和正式成果归档仍待开发。
- v5.7.1 全量后端回归仍为 `173 passed, 3 skipped, 2 failed`；两项失败稳定复现于本任务未修改的标准资料检索排序和项目样例第 90 行源值。报告预览专项测试、前端构建及真实 7 页 DOCX 浏览器回归通过；两项既有资料漂移需后续专项处理，不能把本版记录成“全量测试全部通过”。

详细需求见：

```text
00-PRD/01-模块PRD/13-飞书连接的场景智能体/PRD.md
```

### 数字化项目助手

- 造价智算已在左侧“智能协同”下方提供“数字化项目助手”入口，通过可降级 iframe 加载独立项目页面；宿主只维护菜单、嵌入地址、加载 / 超时 / 重试 / 新窗口降级，不复制功能点业务代码，也不代理独立后端 API。
- 独立项目位于 `D:\Codex-Temp\260712-【数字化造价助手】`，已完成 `v0.1.0` MVP：支持 DOCX / PDF / TXT / MD 文档解析、候选人工确认、确定性功能点计算、Excel 导出和 `?embed=1` 嵌入主题。
- 当前双方开发均已完成，下一步是同时启动宿主 `5174/8000` 与独立助手 `5175/8010` 做联合运行验收，并在通过后确定绿色版 / Tauri 的独立服务启动编排。

宿主入口与跨项目边界见：

```text
00-PRD/01-模块PRD/14-功能点助手模块/PRD.md
D:\Codex-Temp\260712-【数字化造价助手】\00-docs\02-架构与集成\03-造价智算宿主交接状态.md
```

### 运行入口

- 当前对外交付维护三类运行入口：传统开发版、Windows 绿色版、Windows Tauri 桌面版。
- 传统开发版用于本机开发调试：后端 `8000`、前端 Vite `5174`，依赖本机 Python 和 Node 环境。
- Windows 绿色版用于发给其他 Windows 电脑直接运行：仍然是后端 `8000`、前端 Vite `5174` 传统双服务启动，但随包携带 Python、Node 和前端依赖。
- Windows Tauri 桌面版用于发给其他 Windows 电脑直接双击 exe：随包携带 Python 和后端依赖，不需要目标电脑安装 Python、Node、npm、Rust 或 Cargo；前端使用 `frontend/dist`，由本地 FastAPI 静态托管。
- 绿色版和 Tauri 桌面版打包时会复制项目根目录现有 `.env.local`，便于目标电脑直接使用问问智算；`.env.local` 仍不写入源码和代码存档。

详细需求和运行说明见：

```text
00-PRD/01-模块PRD/07-桌面端与评委版/PRD.md
docs/绿色版说明.md
docs/Tauri桌面壳MVP说明.md
```

### UI / 大尾巴主题

- `大尾巴主题 / daweiba` 已成为唯一主界面，不再保留旧布局和旧主题入口。
- 大尾巴主题已整理为独立布局层：外层由 `daweiba-layout-shell`、`daweiba-main-content`、`daweiba-workspace-frame` 等 `daweiba-*` 类控制，不再依赖旧布局壳。
- 第一阶段已落地左侧模块导航、中间业务工作区、右侧问问智算随行助手；左侧导航和右侧 AI 助手均可折叠，折叠后中间区域获得更多横向空间。
- v5.1.1 后，“填价工作台”不再显示表格预览：上层为上传标准 Excel 和转换后概览，下层为工作状态；工作状态承接输入行、匹配行、价格列、输出文件、实物 / 技术 / 预警摘要、匹配状态和匹配质量分布。
- v5.1.1 后，“结果预览”页由表格预览模块独占中间工作区；“经验池预警”页拆为上方经验池预警模块和下方经验池导入模块，导入模块默认折叠。
- v5.2.0 后，旧布局、旧主题、旧宽屏方案、旧流程条及其页面设置入口已从活跃前端和活跃 PRD 中删除；历史内容只保留在 archive 快照中。
- v5.3.0 后，大尾巴主题按 Navattic-flat 口径压平：主体画布改为白底，左右侧栏与中间工作区只用 1px 分割线衔接，持久面板、表格容器和智算侧栏不再使用阴影；菜单选中态、按钮、表格工具条和聊天气泡同步降噪。
- v5.3.1 后，大尾巴主题补齐高级 SaaS 字体与文本颜色基建：全局禁用纯黑和传统深灰，文本 token 收敛为 `#0F172A / #334155 / #64748B`，字体栈改为 Inter 优先并开启抗锯齿，表格数字列启用等宽数字。
- v5.3.2 后，填价工作台比例、欢迎页标题、转换后概览、工作状态卡片、列映射内部滚动、结果预览 sheet chip / 单层表格框 / 横向滚动轨、智算快捷指令上弹和左侧匹配状态仪表盘字号完成细节打磨。
- v5.3.3 后，UI 基线固化为大尾巴主题 + Navattic 极简 SaaS：不再单独创建 `design.md`，全局 UI 口径以 `00-PRD/03-整体UI设计PRD.md` 和 `00-PRD/03-UI设计规范.md` 为准；严禁卡片套娃、常驻面板阴影和高饱和导航选中态；新增 `/v2-preview` 隔离试验场，用 `DaweibaLayoutV2` 和 `daweiba-v2-*` 命名空间验证平铺化三栏。
- v5.3.6 后，结果预览可人工修改输出副本单元格，修改会同步影响预览、下载 Excel 和 Word 报告，并保留浅蓝标记、批注和 JSON 留痕。
- v5.3.7 后，预览人工修改改为快速保存当前单元格快照，回车和失焦都按当前输入框保存；公式与报告刷新改为手动“重算公式”，避免每格保存都被 Excel 重算拖慢或串到其他单元格。
- v5.4.0 后，结果预览行操作区新增“辅助填价”入口，针对基价 / 单价待复核行组织“知识库-相似”和“经验池同类均值”候选；用户确认后才复用输出副本写回和浅蓝留痕，不改原始输入、知识库、规则表或经验池。
- v5.4.0 后，填价工作台可加载预置 100 行演示样例并按正式转换链路生成结果，演示模式提供“看结果 -> 跑预警 -> 风险清单 -> 输出报告”的轻引导，不改变日常工作流。
- v5.4.1 后，结果预览交互完成收口：表头边缘可像 Excel 一样拖拽调列宽并记忆到 `sheet 名 + 列标题`；开始转换后由右侧智算引导跳转表格预览并提供批量匹配；批量匹配统一为预览按钮、智算按钮、快捷指令和文字输入四个入口；左侧模块导航去掉数字编号，预览工具栏按钮降为紧凑低高度样式。
- v5.4.2 后，欢迎页改为浅色 Navattic 式首屏：白底细线框、居中大标题、克制主按钮和下方造价智算工作台预览；原深色欢迎页保留为未启用变体，不影响主界面和业务流程。
- v5.5.0 后，右侧问问智算顶部和收起态显示极简可动机器人形象，状态由现有前端任务流驱动；匹配质量分布环改为低饱和莫兰蒂风险色，保持工作台视觉克制。
- v5.5.1 后，表格预览、列映射和工作量抓取的默认设置统一由 `config/project-default-settings.json` 提供；弹窗内修改只临时生效，新用户和绿色版首次打开沿用项目默认口径。
- v5.5.2 后，`config/project-default-settings.json` 可直接人工维护：以 `_说明` 字段保留配置注释，程序会忽略这些说明字段；请勿使用 JSON 不支持的 `//` 或 `/* */` 注释。
- 当前配置还包含 `zhisuanWindow`：问问智算窗口不再读取浏览器本地偏好或单独欢迎语文件，项目配置成为唯一默认来源。
- v5.5.3 后，绿色版和云端发布都将 `config/project-default-settings.json` 作为必带运行配置；源文件缺失时，绿色版构建会生成带 `_说明` 的完整兜底配置。
- v5.5.4 后，问问智算默认快捷指令补回 `@知识库：` 前缀；浏览器页签标题统一为“造价智算”，快捷指令文本输入的路由优先级和验收口径同步写入模块 PRD。

### 参赛展示与申报材料

- 当前产品口径吸收了人工智能创新大赛申报书 v1.7 的核心表达：四类业务痛点、三链融合、混合 AI 架构、先助手后平台、规则裁决 / 模型解释 / 人工兜底和价值指标。
- 给其他 AI 查看导出包已支持核心文件、前端代码合并、后端代码合并和 `PRD合并.md`。

详细需求见：

```text
00-PRD/01-模块PRD/08-参赛展示与申报材料/PRD.md
```

## 当前 P1 方向

- 辅助填价与人工复核闭环：继续补齐两个系数候选、自定义依据、处理状态和风险入口联动。
- 知识库质量检测：只读检查重复组合、价格冲突、单位缺失、经验数异常和空值，不自动修库。
- 智能协同：第一层 Webhook 负责主动通知；第二层企业应用本机长连接试点负责群聊收取单个 Excel、顺序排队、自动专业处理和成果回传。两层均不复制专业匹配逻辑，飞书故障不改变本地处理结果；审批、台账、派单和角色权限留待后续。

详细需求见：

```text
00-PRD/01-模块PRD/13-飞书连接的场景智能体/PRD.md
```

## 关键资产

### 规则与知识库

```text
03-知识库-二维数据库制作/【数据库】【导入】.xlsx
03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】项目以及总体匹配规则介绍.md
03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】要素1-5和单位的匹配模式介绍.md
03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】【第一层】-标准规则命中表-说人话版-v1.0.xlsx
backend/app/rules/technical_fee_rules.xlsx
backend/app/rules/technical_fee_rules.csv
backend/app/rules/physical_factor_rules.xlsx
backend/app/rules/physical_factor_rules.csv
backend/app/rules/physical_factor_overrides.xlsx
backend/app/rules/physical_factor_overrides.csv
```

### 经验池与工作量

```text
05-经验池-预警数据/【经验池】【模板勿动】-管勘智算.xlsx
05-经验池-预警数据/【经验池】-管勘智算-【codex】.xlsx
05-经验池-预警数据/experience-warning-settings-【codex】.json
05-经验池-预警数据/experience-field-preferences-【codex】.json
config/project-default-settings.json
03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【术语归并】术语归并与匹配放宽规则表.xlsx
```

### 样例与说明

```text
03-知识库-二维数据库制作/【输入】【合并】-管勘智算数据库-v1.7.2-【输入测试-空单价100】（全部半角）.xlsx
03-知识库-二维数据库制作/【输入-答案】【合并】-管勘智算数据库-v1.7.2-【输入测试-空单价100】（全部半角）.xlsx
03-知识库-二维数据库制作/【委托方例子】【工作量信息抓取】委托方原始工作量和系数的例子.xlsx
00-PRD/00-产品总览.md
```

## 开发运行

后端：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

前端：

```powershell
cd frontend
npm install
npm run dev -- --host 127.0.0.1 --port 5174
```

打开：

```text
http://127.0.0.1:5174/
```

一键启动：

```text
启动造价智算.bat
```

大模型 API Key 放在项目根目录 `.env.local`，官方 DeepSeek 使用：

```text
DEEPSEEK_API_KEY=你的key
```

`.env.local` 只用于本机运行，不进入代码存档。

## 验证方式

后端测试：

```powershell
python -m pytest backend/tests -v
```

前端构建：

```powershell
cd frontend
npm run build
```

PRD 巡检：

```powershell
python tools/check_prd_consistency.py --strict
```

统信兼容性检查：

```powershell
python tools/check_platform_compat.py
```

该命令是未来统信 / Linux 迁移的手动专项检查，不是平时代码开发必跑项；日常 Windows 开发、前端构建、后端测试和绿色版流程不默认执行它。

常用样例：100 行空单价输入表、项目例子控制价表、委托方工作量抓取样例。

## 版本存档

代码存档：

双击：

```text
备份代码.bat
```

或运行：

```powershell
python tools/archive_code.py
```

规则：

- 每个版本完成时运行一次代码存档。
- 存档只打包代码和恢复所需上下文，不打包比赛资料、知识库原始大目录、PPT 和运行临时输出。
- `01-assets/01-UI参考图/01-assets/` 与 `01-assets/01-UI参考图/00-reference/` 属于 UI 图片素材和参考资料，不进入代码版存档；`02-notes/` 和静态 demo HTML 可保留在存档中。
- `tools/archive_code.py` 会在生成根目录 `04-输出版本存档` 内的压缩包后，同步复制一份到 `D:\Desktop\01-工作流-【造价】\工作流\260612-【ai大赛】-管勘智算-V2.0\04-输出版本存档`，作为工作流目录副本。
- 存档过程必须通过脚本完成，脚本运行结束后不要再额外检查 zip 是否存在、工作流副本是否复制成功、SHA256 是否一致或目录是否完整；是否存档成功由大尾巴人工查看。
- 改完代码后默认不自动重启 `8000` / `5174` 服务，除非用户明确要求或改了启动器、端口、环境变量、依赖、后端启动逻辑。

## 给其他 AI 评判项目

双击：

```text
导出给其他AI查看-核心代码与规则.bat
```

或运行：

```powershell
python tools/export_ai_review_bundle.py
```

输出目录：

```text
04-输出版本存档/给其他AI查看-核心代码与规则-yyyy-mm-dd
```

目录内包含第一层级文件、前端代码合并、后端代码合并、`PRD合并.md` 和上传建议清单。完整清单见：

```text
04-输出版本存档/给其他AI评判项目的重要文件清单.md
```

### 三类导出 / 备份脚本区别

| 脚本 | 用途 | 输出 |
| --- | --- | --- |
| `备份代码.bat` / `tools/archive_code.py` | 代码版恢复存档，版本完成时用 | `04-输出版本存档/造价智算-yyyy-mm-dd-v版本.zip` |
| `导出给其他AI查看-核心代码与规则.bat` / `tools/export_ai_review_bundle.py` | 给其他 AI 阅读评判，合并前端、后端、PRD 和规则说明 | `04-输出版本存档/给其他AI查看-核心代码与规则-yyyy-mm-dd/` |
| `备份PRD.bat` / `tools/archive_prd.py` | 手动备份当前 PRD Markdown 快照，便于回看旧版产品设计 | `00-PRD/archive/yyyy-mm-dd-vN/` |

## 上下文维护规则

- `README.md` 只做项目入口、当前状态摘要和关键链接，不再承载模块细节流水账。
- `00-PRD/` 写产品意图、模块边界、需求状态和验收口径。
- UI 参考图、Bug 截图、参考设计、技术经验、踩坑记录和 UI 小样实验统一进入 `01-assets/01-UI参考图/`；其中外部参考图放 `00-reference/`，自制设计稿 / Bug 图放 `01-assets/`，分析笔记放 `02-notes/`，静态原型放 `code/`，正式代码仍在 `frontend/`、`backend/`、`src-tauri/`。
- `CHANGELOG.md` 写已经完成的实际变化；每次集中修改后运行一次 `python tools/trim_changelog.py`，保持最新 3 条记录，更早内容进入 `CHANGELOG - 【归档】.md`。
- `AGENTS.md` 写协作规则、重要文件入口和长期注意事项。
- `docs/` 写绿色版、兼容性和历史计划等专项说明。
- 项目根目录只保留源码、规则资料、正式资料入口、文档和必要启动文件；临时缓存、Playwright 检查输出、pytest 缓存、临时外包件、截图 / 过程产物等统一放到 `Codex-Temp/` 下。`node_modules` 是根目录 npm 依赖，不按临时文件移动。
- 旧任务看板已归档到 `docs/归档/TASKS-归档-2026-06-18.md`，日常不再读取。


