# 回归验收说明

- 关键样例：`100 行空单价输入表`。
- 核心口径：改造前后逐行结果一致。
- 旧客户端和 WeAct 未传 Skill 时继续使用本能力。
- 显式错误 Skill、版本冲突或不安全资产必须拒绝创建任务。
- 文件推荐只读取文件名、有限工作表名和有限表头；结果必须包含命中理由，且不得自动改写当前 Skill。
- 管理中心只能返回本机盘点和生命周期审核计划；未具备可信签名、发布审批与组织授权时，所有写操作必须保持关闭。
