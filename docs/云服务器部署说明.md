# 造价智算云服务器部署说明

## 当前部署

- 服务器：`118.196.107.182`
- 端口：`1285`
- 访问地址：`http://118.196.107.182:1285/`
- 健康检查：`http://118.196.107.182:1285/api/health`
- 部署目录：`/opt/zaojiazhisuan`
- 当前代码目录：`/opt/zaojiazhisuan/current`
- 历史发布目录：`/opt/zaojiazhisuan/releases/`
- Python 虚拟环境：`/opt/zaojiazhisuan/venv`
- systemd 服务：`zaojiazhisuan.service`
- 日志：
  - `/opt/zaojiazhisuan/logs/service.log`
  - `/opt/zaojiazhisuan/logs/service-error.log`

## 运行方式

云端采用单端口网页版部署：

```text
FastAPI 后端：0.0.0.0:1285
React 前端：frontend/dist，由 FastAPI 静态托管
```

启动命令由 systemd 管理：

```bash
/opt/zaojiazhisuan/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 1285 --app-dir backend
```

环境变量：

```text
PYTHONUTF8=1
GUANKAN_FRONTEND_DIR=/opt/zaojiazhisuan/current/frontend/dist
```

大模型 Key 通过 `/opt/zaojiazhisuan/current/.env.local` 读取；不要把 Key 写入源码或文档。

## 常用维护命令

查看状态：

```bash
systemctl status zaojiazhisuan.service --no-pager
```

重启服务：

```bash
systemctl restart zaojiazhisuan.service
```

查看日志：

```bash
tail -n 100 /opt/zaojiazhisuan/logs/service.log
tail -n 100 /opt/zaojiazhisuan/logs/service-error.log
```

本机健康检查：

```bash
curl http://127.0.0.1:1285/api/health
```

公网健康检查：

```text
http://118.196.107.182:1285/api/health
```

## 更新部署口径

1. 本地执行 `npm run frontend:build`。
2. 生成云端部署包，包含：
   - `backend/app/`
   - `backend/requirements-runtime.txt`
   - `frontend/dist/`
   - 正式二维知识库
   - 报告模板
  - 经验池模板、经验池数据和偏好配置
   - `Codex-Temp/runtime/preview-column-preferences.json`，用于结果预览默认显示列、各 sheet 覆盖列、表头行和列宽
  - 匹配规则说明、术语归并表和知识问答所需资料
   - `.env.local.example`
   - 如需云端直接启用问问智算，可随包带入 `.env.local`
3. 上传到 `/tmp/`。
4. 解压到 `/opt/zaojiazhisuan/releases/新版本目录`。
5. 将 `/opt/zaojiazhisuan/current` 软链接指向新版本目录。
6. 运行：

```bash
/opt/zaojiazhisuan/venv/bin/pip install -r /opt/zaojiazhisuan/current/backend/requirements-runtime.txt
systemctl restart zaojiazhisuan.service
```

## 注意事项

- 当前云端包不包含样例 Excel，样例由使用者自行准备。
- 结果预览默认列和默认表头行依赖 `Codex-Temp/runtime/preview-column-preferences.json`。如果云端缺少该文件，接口会回退到内置默认列和空 `headerRows`，容易出现预览仍按旧表头行显示的问题。
- 云端部署不改变基价 / 单价匹配、调整系数匹配、经验池预警、工作量抓取和 Word 报告规则。
- 如果公网无法访问但服务器本机 `curl 127.0.0.1:1285` 正常，优先检查云服务器安全组是否放行 `1285/tcp`。
