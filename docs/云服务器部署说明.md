# 造价智算云服务器部署说明

## 当前部署

- 服务器：`118.196.107.182`
- 端口：`1285`
- 访问地址：`http://118.196.107.182:1285/`
- 健康检查：`http://118.196.107.182:1285/api/health`
- 部署目录：`/opt/zaojiazhisuan`
- 当前代码目录：`/opt/zaojiazhisuan/current`
- 当前发布版本：`v5.8.21`
- 当前发布目录：`/opt/zaojiazhisuan/releases/20260715-v5.8.21-file-reaction`
- 历史发布目录：`/opt/zaojiazhisuan/releases/`
- Python 虚拟环境：`/opt/zaojiazhisuan/venv`
- systemd 服务：`zaojiazhisuan.service`
- 日志：
  - `/opt/zaojiazhisuan/logs/service.log`
  - `/opt/zaojiazhisuan/logs/service-error.log`
- 飞书第二层机器人服务：`zaojiazhisuan-feishu-bot.service`
- 飞书机器人日志：`journalctl -u zaojiazhisuan-feishu-bot.service -n 100 --no-pager`

2026-07-15 已将 v5.8.18 身份加固版切换为云端当前版本：主服务与第二层机器人 systemd 服务均为 `active`，第二层激活 `weact_cost`，运行秘密与项目登记均为已验证应用 A `cli_a961a5cb3ab8d353`，状态接口返回 `profile_consistent=true`，企业 WeAct WebSocket 已连接且运行器恰好一个；本机保存同一 WeAct App ID 但第二层保持关闭、运行器为零，继续守住单实例。公网健康接口、首页 bundle 和知识库检索均通过，验证过程未向任何群发送测试消息。

2026-07-15 浏览器标题区分已发布到 `/opt/zaojiazhisuan/releases/20260715-v5.8.18-browser-title`：回环地址、localhost 与 Tauri 显示“造价智算（本地）”，其他主机地址显示“造价智算（云端）”。前端增量包 SHA256 为 `D6E5F0002D61BCFEDF90FC488AA96A9932F1B85D422767D0B74E06EF4BF84DE3`，回滚目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.18-feishu-profile-guard-r2`；真实本地页与云端页的 `document.title` 均已通过浏览器运行态验证，机器人身份、单实例和 WeAct 长连接保持正常。

2026-07-15 任务 `FS-20260715-105323-28CB4C` 暴露云端机器人内部地址被本地默认 `8000` 覆盖的问题：云端主服务 `1285` 正常、`8000` 未监听，因此专业处理阶段报 `[Errno 111] Connection refused`。根因不是网络偶发故障，而是发布包中的共享本机默认值覆盖了云端运行口径。v5.8.19 已形成三层永久保护：`load_bot_defaults()` 优先读取环境覆盖；运行器建立业务接收前强制检查 `/api/health`；仓库保存 systemd drop-in、安装脚本和发布检查器。当前发布目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.19-runtime-guard-r3`，运行增量包 SHA256 为 `97136A6D7CD232A7F132862696ECA95EB0D9370B2AC82774521D1C89CFD4F097`，回滚目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.19-api-base-guard`。上线检查确认两个 systemd 服务均显式使用 `1285`、专业服务健康、机器人配置一致、WeAct 长连接建立且运行器恰好一个；检查器会等待机器人最多 30 秒，避免把正常启动时序误判为失败。失败任务保留原记录，未自动重跑或发送群测试消息。

2026-07-15 v5.8.20 修复 WeAct 文件事件延迟：实测文件在平台发送后约 5 分 17 秒才投递，原 5 分钟防重放规则因此将正常文件静默拦截。当前只对“平台发送时间落在原 1 分钟收件窗口内、同会话同发送人、单个 `.xlsx`、15 分钟内送达”的事件继续处理，普通旧消息和窗口外文件不放宽。当前发布增量包 SHA256 为 `A29185E3381C47B5C9129BBEA244A0E45F603E834262FE3D2C8940AF934548F9`，回滚目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.19-runtime-guard-r3`；健康版本、端口保护、配置一致、WeAct 长连接和单运行器均已通过，旧文件事件未补执行。

2026-07-15 v5.8.21 为已命中收件窗口的 `.xlsx` 文件消息增加异步“了解”表情；群聊普通文件、未开启窗口文件和其他格式不回应。当前发布增量包 SHA256 为 `2549DDE50B1ED4206E4CA52ECF4FAF34ED4B37B8ABAC856BC5456CE3F6D3032E`，回滚目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.20-weact-delayed-file`；健康版本、端口保护、配置一致、WeAct 长连接和单运行器均已通过，未主动发送群测试消息。

v5.8.18 最终增量发布包 SHA256：`0308B2F41E2383BAFB711649480543F5917FDD0FCE6BB01F1D21FDFF14E1C524`。包只包含后端应用代码、独立机器人运行器、前端构建产物、项目默认配置和 README，不包含 `.env.local`、App Secret、Webhook 或运行数据库；新发布目录从上一版完整复制运行环境后覆盖这些非秘密文件。上一版回滚目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.18-feishu-profile-guard`，再上一版为 `/opt/zaojiazhisuan/releases/20260715-v5.8.17-ui-eb087d8`。

2026-07-15 早先已将 v5.8.17 切换为云端运行版本：主服务与第二层机器人 systemd 服务均为 `active + enabled`，第二层激活 `weact_cost`，企业 WeAct WebSocket 已连接，本机第二层已关闭，保持单实例。第一层 WeAct Webhook 配置已保留，当前通知开关仍为关闭。

v5.8.17 云端包 SHA256：`8984C184133C4DDB288364BC9D598821C198CCE41F60A985E47624F41ED80B54`。发布时从旧版复制 `.env.local`、机器人秘密配置、SQLite 任务队列与运行日志；上传包本身不含密钥。上一版回滚目录为 `/opt/zaojiazhisuan/releases/20260713-v5.8.7`。

2026-07-15 响应式界面小版本已发布到 `/opt/zaojiazhisuan/releases/20260715-v5.8.17-ui-eb087d8`，Git 提交为 `eb087d8`。本次只替换构建后的 `frontend/dist`，并从上一个 v5.8.17 发布目录复制完整运行环境；前端增量包 SHA256 为 `38DDDE0D2E45EF7DE9731221AAED4CCA7CF82A6537E6C66DE00022A18228F09F`，回滚目录为 `/opt/zaojiazhisuan/releases/20260715-v5.8.17`。公网首页、新前端 bundle、健康接口、知识库检索、第二层 WeAct 连接及第一层 Webhook 配置保留均已验证。

## 运行方式

云端采用单端口网页版部署：

```text
FastAPI 后端：0.0.0.0:1285
React 前端：frontend/dist，由 FastAPI 静态托管
```

启动命令由 systemd 管理：

```bash
/opt/zaojiazhisuan/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 1285 --app-dir backend
```

环境变量：

```text
PYTHONUTF8=1
GUANKAN_FRONTEND_DIR=/opt/zaojiazhisuan/current/frontend/dist
```

大模型 Key 通过 `/opt/zaojiazhisuan/current/.env.local` 读取；不要把 Key 写入源码或文档。

## 常用维护命令

查看状态：

```bash
systemctl status zaojiazhisuan.service --no-pager
```

重启服务：

```bash
systemctl restart zaojiazhisuan.service
```

## 飞书第二层机器人

当前服务器已启用唯一的飞书企业自建应用长连接实例：

```bash
systemctl status zaojiazhisuan-feishu-bot.service --no-pager
systemctl restart zaojiazhisuan-feishu-bot.service
curl http://127.0.0.1:1285/api/collaboration/feishu-app-bot/status
```

机器人程序位于当前发布目录的 `backend/feishu_bot_runner.py`，依赖 `backend/requirements-runtime.txt` 中的 `lark-oapi`。企业应用凭证与第一层 Webhook 配置统一保存在服务器当前发布目录的 `Codex-Temp/runtime/feishu-robot-settings.json`，权限为 `600`，不进入 GitHub、发布压缩包或前端。

同一飞书应用不要让本机和服务器同时启用长连接。迁移到服务器后，本机第二层机器人应保持关闭，服务器只保留一个运行实例。

云端端口口径：服务器 FastAPI 监听 `1285`，本机开发默认值仍为 `http://127.0.0.1:8000`。云端不要手改发布目录 JSON；仓库内 `deploy/systemd/feishu-api-base.conf` 是唯一部署源，必须通过 `deploy/install_cloud_runtime_guard.sh` 同时安装到主服务和机器人服务。环境值优先于项目默认配置，也能覆盖后续发布包重新携带的本机默认值。端口不一致时，飞书文件任务和知识库问答均会出现 `[Errno 111] Connection refused`。

查看日志：

```bash
tail -n 100 /opt/zaojiazhisuan/logs/service.log
tail -n 100 /opt/zaojiazhisuan/logs/service-error.log
```

本机健康检查：

```bash
curl http://127.0.0.1:1285/api/health
```

公网健康检查：

```text
http://118.196.107.182:1285/api/health
```

## 更新部署口径

1. 本地执行 `npm run frontend:build`。
2. 生成云端部署包，包含：
   - `backend/app/`
   - `backend/requirements-runtime.txt`
   - `frontend/dist/`
   - 正式二维知识库
   - 报告模板
   - 经验池模板、经验池数据和偏好配置
   - `config/project-default-settings.json`：必须随云端包发布，统一承载表格预览、列映射、工作量抓取和问问智算窗口默认设置；可用 `_说明` 字段人工维护，不能使用 `//` 或 `/* */` 注释
  - 匹配规则说明、术语归并表和知识问答所需资料
   - `.env.local.example`
   - 如需云端直接启用问问智算，可随包带入 `.env.local`
3. 上传到 `/tmp/`。
4. 解压到 `/opt/zaojiazhisuan/releases/新版本目录`。
5. 将 `/opt/zaojiazhisuan/current` 软链接指向新版本目录。
6. 从当前版本安装仓库化 systemd 保护、安装依赖并重启两个服务：

```bash
bash /opt/zaojiazhisuan/current/deploy/install_cloud_runtime_guard.sh /opt/zaojiazhisuan/current
/opt/zaojiazhisuan/venv/bin/pip install -r /opt/zaojiazhisuan/current/backend/requirements-runtime.txt
systemctl restart zaojiazhisuan.service
systemctl restart zaojiazhisuan-feishu-bot.service
```

7. 运行强制发布验收。命令返回非零时必须回滚或修复，不得把该版本标记为已上线：

```bash
cd /opt/zaojiazhisuan/current
/opt/zaojiazhisuan/venv/bin/python tools/check_feishu_deployment.py --mode cloud --check-health
```

### 中文路径打包注意事项

云端发布包包含中文目录和文件名，必须使用 UTF-8 保真的 `tar.gz` 上传并在 Linux 服务器上用 `tar -xzf` 解压。不要使用 Windows `Compress-Archive` 生成的 ZIP 直接在服务器上解压，否则可能把中文路径解码为乱码，造成知识库、报告模板和规则资料“文件不存在”。

## 注意事项

- 当前云端包不包含样例 Excel，样例由使用者自行准备。
- `config/project-default-settings.json` 是云端运行必需配置，必须与 `backend/app/` 位于同一发布根目录；缺失或 JSON 格式错误时程序会回退到内置默认值，可能与项目维护口径不一致。
- 云端部署不改变基价 / 单价匹配、调整系数匹配、经验池预警、工作量抓取和 Word 报告规则。
- 如果公网无法访问但服务器本机 `curl 127.0.0.1:1285` 正常，优先检查云服务器安全组是否放行 `1285/tcp`。
