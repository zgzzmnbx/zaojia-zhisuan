# CHANGELOG

## v5.14.1 - 2026-07-22

- 补齐智能协同 P0-A 领取闭环：WeAct / 飞书经典任务卡新增“领取任务”，长连接卡片回调以操作人真实 Open ID 校验指定编制人；非目标人拒绝，目标人重复点击幂等，领取状态和时间真实落库。
- 领取成功后原卡片更新为“已领取”并移除领取按钮；领取只改变任务流转状态，不批准、不修改专业数据、不触发计算或完成任务。
- 外部任务模拟器默认自动编写 `SIM-时间-随机码` 任务编号，项目名留空时自动补为“项目-XXXXXX”；后端接口同样提供缺省兜底。
- 派发台账新增“流程人员”列，展示角色、人员名称和领取状态；公开响应继续隐藏群 ID、用户 ID、文件 Key、运行路径和密钥。
- 验证完成：外部派发专项 `16 passed`，全量后端 `352 passed, 4 skipped`，前端工作台 `3 passed`、报告预览 `8 passed`、生产构建与 Python 编译均通过；内置浏览器已核对自动编号、缺省项目名、流程人员列、1000×720 页面无整体横向溢出且控制台零错误。

## v5.14.0 - 2026-07-22

- 完成专业 Skill P1 文件特征推荐：上传 Excel 后只读取文件名、有限工作表名和有限表头，按各 Manifest 的 `recommendationProfile` 返回候选分值、置信度和可解释命中理由；推荐不自动切换，必须由用户确认。
- 完成开放格式包装：三个能力目录均提供 `SKILL.md`，后端公开 `manifest.json + SKILL.md` 格式、必填字段、状态和安全策略；程序只信任结构化 Manifest 与代码白名单，不从 Markdown 解析价格、系数、风险规则或可执行入口。
- 完成专业能力管理中心 P2 安全接口与简约界面：可盘点本机能力、查看开放格式，并为安装、启停、升级、回滚、卸载和导出生成审核计划；当前统一返回 `changes_applied=false`，所有写操作关闭。
- 通用服务类和更多造价专业继续保持规划状态；本版本只搭建接口和治理边界，不复制勘察测量资产、不伪造第二专业数据、不导入未知包、不执行任意外部脚本。
- 验证完成：专业 Skill 专项 `22 passed, 1 skipped`，全量后端 `346 passed, 4 skipped`，前端生产构建、报告预览 `8 passed`、工作台 `3 passed`；内置浏览器已核对管理中心、审核计划、1280×720 无横向溢出和零控制台错误。

## v5.13.0 - 2026-07-22

- 完成智能协同 P0-A 外部任务主动派发模拟：新增标准 `TaskEnvelope`、来源任务幂等键、任务 / Skill / 模板快照和本地派发台账；先落库再投递，避免外部平台瞬时失败造成任务丢失。
- 复用现有第二层飞书 / WeAct 配置与受控人员映射，仅允许向唯一授权“智算测试”群发送经典卡片和任务专属 Excel 副本；精准单聊在主动触达未实测前保持硬关闭，不向其他群发送测试消息。
- 卡片与附件按独立步骤记录状态，支持只重试未完成步骤；前端智能协同页提供外部任务模拟表单、派发状态和脱敏台账，不回显平台用户 ID、群 ID、文件 Key 或运行秘密。
- 更新模块 13 PRD、产品总览、当前版本计划、README、飞书第二层说明和长期安全约束；本阶段不实现多人复核、意见闭环、成果版本或外部系统生产鉴权。
- 验证完成：外部派发专项及全量后端测试纳入 v5.13.0 回归，前端生产构建通过；真实投递仍严格受授权目标和现有平台配置约束。

## v5.12.0 - 2026-07-22

- 完成专业 Skill P0-B 运行时接口：新增任务级 `SkillRuntimeContext`，冻结输入规范、白名单处理器、结构化计价库、规则资产、经验池与阈值、知识源、报告模板、验证信息和能力开关；任务状态只保存项目内相对路径，公开接口不泄露真实资产路径。
- 现有勘察测量流程包装为代码白名单 `survey-measurement-v1`，继续复用同一 `FillEngine` 和三数字口径；批量匹配、辅助填价、经验池预警、任务内知识问答、风险报告、Word 重生成和下载均从任务快照恢复运行时上下文，不再重新读取当前默认 Skill。
- 报告生成支持显式 Skill 模板，知识检索支持按 Skill 知识源独立建索引，经验池预警按 Skill 风险配置读取；前端知识问答自动携带当前任务或所选 Skill 上下文。
- 新增测试专用第二 Skill 隔离夹具，验证不同计价库、技术规则、经验池、知识源和 Word 模板真实切换，并验证默认 Skill 改变后既有任务不漂移；该测试能力不进入生产清单，通用服务类仍待真实资料与数据。
- 验证完成：专业 Skill 专项 `19 passed, 1 skipped`；全量后端 `333 passed, 4 skipped`；前端 v5.12.0 生产构建通过。现有 100 行旧请求与显式 Skill 逐工作表、逐单元格一致。
- 内置浏览器运行态验收通过：v5.12.0 演示样例 101 / 101 匹配、0 行待复核、任务 Skill 已锁定；1433px 视口无横向溢出，控制台无错误。

## v5.11.1 - 2026-07-22

- 修复问问智算正式标准资料索引的旧目录硬编码：知识源发现同时兼容当前 `01-【业务资料】原始资料-勘察测量相关基础资料` 与旧 `01-原始资料`，目录内 Markdown、Excel 和 CSV 继续进入本地证据索引并标记为 `standard`。
- 同步更新正式资料检索测试断言，不降低“前 5 条至少出现正式标准来源”的验收要求；本次不解析 PDF、不修改标准资料内容、不调整三数字匹配、规则、经验池或报告模板。
- 收口 v5.11.0 对话式智能体工作台第二轮体验：简约会话视觉、用户 / 助手分轮、Excel 拖拽、折叠任务操作、自绘 Skill 菜单和快捷操作用户指令回显已同步写入模块 PRD、当前版本计划和 README。
- 验证完成：正式标准资料检索专项 `1 passed`；全量后端恢复为 `331 passed, 4 skipped`；前端生产构建和工作台专项 `3 passed` 沿用本轮已完成结果。全局 PRD 严格巡检仅保留既有外部参赛申报书路径缺失。

## v5.11.0 - 2026-07-21

- 完成“对话式智能体工作台”P0：左侧新增“智算助手”一级入口，右侧智算 Dock 新增“进入智算助手”；两个入口进入同一宽屏会话工作台，只保留左侧导航，原专业工作区隐藏但不卸载。
- Dock 与宽屏模式直接复用同一消息流、输入草稿、Excel 文件、任务、行级上下文和等待状态；支持 `Esc` 退出、新会话、Enter 发送、Shift+Enter 换行和停止请求，切换模块后可无损恢复。
- 底部输入区接入 Registry 真实专业能力与任务 Skill 快照：勘察测量能力已上线，规划中能力不可执行，任务创建后锁定 Skill；当前仍只有单一勘察测量真实流程，不把界面选项宣传为多 Skill 运行时路由。
- 会话内结构化动作复用现有真实业务链路：上传 / inspect、开始转换、批量匹配、经验池预警、风险清单、风险报告、Excel / Word 下载与专业页跳转；未修改三数字匹配、经验池规则、正式知识库或报告模板。
- 验证完成：新增工作台状态单测 `2 passed`，报告预览回归 `8 passed`，前端 v5.11.0 生产构建通过；全量后端 `330 passed, 4 skipped, 1 failed`，唯一失败仍是开发前已知的正式标准来源前 5 排序问题。真实浏览器使用 100 行样例完成 101/101 匹配、0 行待复核、2 条预警、风险报告及 Excel / Word 成果入口验收；1024 / 1366 / 1920 三档无横向溢出。

## v5.10.0 - 2026-07-20

- 完成“选择专业能力（Skill）”P0：全局导航栏提供默认折叠的“专业能力”入口，展开后按三行展示能力卡片和详情；勘察测量能力以 `v1.0.0` 已上线，详情区分专业专用与通用能力复用并展示 10 项已启用子 Skill；通用服务类展示 9 项规划模块与最小闭环门槛，更多造价专业为即将开放，后两者均可查看但不可创建任务。
- 新增 `business-skills/` 与独立 Skill Registry：确定性校验 Manifest 完整性、状态、版本、项目内相对路径、资产存在性、路径越界、可执行入口和运行秘密；未知、规划中、停用、版本不匹配或不完整能力均不能创建任务。
- 任务状态固化 Skill ID、版本、Manifest SHA-256 摘要、创建时间和资产标识；处理、预览、预警、风险、报告和下载继续使用同一快照。旧前端和 WeAct 未传 Skill 时继续默认走勘察测量能力，Manifest 缺失时只对该默认旧请求使用明确的兼容快照。
- 绿色版构建显式携带 `business-skills/`，Tauri 发布复用同一阶段目录；同步回写 README、产品总览、模块 15 PRD、当前版本计划、项目长期规则和发布说明。本次未修改三数字匹配引擎、正式规则、结构化计价库、经验池或报告模板。
- 验证完成：专业能力专项 `17 passed, 1 skipped`，真实 100 行旧请求 / 显式 Skill 输出逐工作表、逐单元格一致；全量后端 `330 passed, 4 skipped, 1 failed`，仅保留既有的正式标准来源前 5 排序失败；前端 v5.10.0 生产构建、Python 编译和真实浏览器的选择、详情、规划禁用、子 Skill 滚动布局、100 行演示、任务快照、结果 / Word 预览与下载入口均通过，控制台无错误。PRD 模块 15 硬检查通过；全局严格巡检仅保留既有外部参赛申报书路径缺失。

## v5.9.2 - 2026-07-18

- 通用知识改为可配置的类型审核：默认仅操作方法和一般解释自动通过，项目口径、价格 / 调整系数、正式标准，以及同范围冲突内容必须人工确认；正式资料只提供核对线索，不交给大模型裁决。
- 新增同范围完全重复复用、相似项和冲突提示；支持已确认知识生成更正版本、项目知识提升为通用候选，并区分 `revision` 与 `promotion` 关系，确保只有确认后的更正版替代旧版，提升通用不影响原项目知识。
- “问问智算”新增确定性自然语言操作：`只在当前项目记住`、`把上一条改成……`、`忘掉上一条`、`这条知识不对`、`查看刚保存的知识`；保留按钮保存、知识类型选择和完整审计。
- 项目默认配置新增 `knowledgeMemory.autoApproveTypes` 与 `duplicateSimilarityThreshold`；同步更新 README、问问智算 PRD、当前版本计划、使用说明和项目长期规则。
- 验证完成：知识记忆专项 `21 passed`，配置接口专项 `1 passed`，Python 编译和前端生产构建通过；全量后端 `311 passed, 3 skipped, 2 failed`，失败仍为开发前已知的正式标准来源前 5 排序和项目样例第 90 行数据漂移，未出现本次新增回归。

## v5.9.1 - 2026-07-17

- 知识候选默认改为“通用知识”，无需选择项目；通用候选当前自动完成提交和系统确认，同时保留审核接口、版本、撤销与 create / submit / confirm 完整审计。
- 保留“保存为知识候选”按钮，并新增“记住这一条”等确定性口令：只保存当前对话中最近一条已完成的结构化知识候选，不把普通闲聊自动沉淀。
- 问答检索同时纳入已确认通用知识和当前 `project_key` 下已确认知识；项目/任务范围仍保持人工审核与跨项目隔离，正式标准和规则优先级不变。
- 验证完成：后端知识记忆专项、前端生产构建、Python 编译和真实浏览器的按钮保存、自然语言保存、自动确认及审计查看均通过；全量回归仍保留开发前已知的标准资料排序和第 90 行样例数据漂移两项失败。

## v5.9.0 - 2026-07-17

- 完成问问智算“知识候选池 + 项目知识记忆 + 人工确认”P0：知识库回答和行级复核可预填保存候选，网页支持编辑、提交、确认、驳回、撤销、标记疑似失效及完整审计。
- 新增 SQLite 项目知识记忆库与版本记录；只有同一 `project_key` 下已确认、未过期的知识参与问答，正式标准和规则保持更高优先级，回答明确展示项目范围、确认人、确认时间、适用条件和来源。
- 知识记忆故障自动降级到原有正式知识库问答，不影响 Excel 填价、经验池预警、Word 报告和其他专业主流程；当前操作人与确认角色明确限定为本地试点，不冒充企业身份认证。
- 验证完成：知识记忆专项 `10 passed`，全量后端 `300 passed, 3 skipped, 2 failed`，两项失败与开发前已知的标准资料检索排序、项目样例第 90 行数据漂移基线一致；前端生产构建、Python 编译、浏览器候选创建 / 提交 / 确认 / 疑似失效闭环通过。PRD 硬检查仅剩既有外部申报书路径缺失。

## v5.8.21 - 2026-07-15

- 为已命中收件窗口的 `.xlsx` 文件消息异步添加“了解”表情，让用户在任务入队回复前获得即时确认。
- 群聊范围继续严格限制为同会话、同发送人的有效收件窗口；普通群文件、未开启窗口文件、其他格式和群内日常消息不添加表情。

## v5.8.20 - 2026-07-15

- 修复企业 WeAct 将文件事件延迟约 5 分钟后投递时，正常上传被通用防重放规则静默拦截的问题。
- 保持收件窗口为 1 分钟：只允许平台发送时间落在原窗口内、且在 15 分钟内送达的单个 `.xlsx` 继续处理；普通旧消息、窗口外文件、其他格式和更长延迟仍保持静默拦截。
- 控制台为获准的延迟文件记录“通过原 1 分钟收件窗口校验”，便于区分平台延迟与历史重放。

## v5.8.19 - 2026-07-15

- 修复云端第二层机器人内部调用地址被本地默认 `127.0.0.1:8000` 覆盖、导致文件任务报 `[Errno 111] Connection refused` 的问题；新增 `FEISHU_APP_BOT_API_BASE_URL` 环境级覆盖，云端 systemd 固定使用 `http://127.0.0.1:1285`，本机默认仍为 `8000`。systemd drop-in、安装脚本和本地 / 云端检查器已纳入仓库，端口覆盖缺失、服务未运行或健康检查失败会阻断发布验收。
- 第二层独立运行器在建立业务接收前新增专业服务 `/api/health` 预检；专业服务不可达或未返回 `ok` 时记录明确错误并退出，不再先收文件再让任务失败。
- 浏览器标签按运行环境自动区分为“造价智算（本地）”和“造价智算（云端）”；只改变标签标题，不修改页面布局和专业业务链路。

## v5.8.18 - 2026-07-15

- 在项目默认配置集中登记普通飞书与企业 WeAct 的预期 App ID 和域名；本地与云端的 `weact_cost` 统一使用已完成消息端到端验证的应用 A，App Secret 继续只保存在各环境 Git 忽略的运行秘密文件中。
- 第二层页面开关、后端子进程拉起和独立运行器增加同一套身份校验；App ID 或域名与项目登记不一致时显示“配置不一致”并拒绝启动，避免错误应用虽然建立 WebSocket 却收不到正式机器人消息。
- 新增配置偏差回归用例并保留普通飞书 / WeAct 双配置切换、队列、上传、匹配、风险、Excel、Word、知识库与普通问答原有链路。

## v5.8.17 - 2026-07-15

- 第二层普通飞书 / WeAct 收件入口增加持久去重：同时对事件 ID 和消息 ID 建立 SQLite 唯一约束，解决平台以新事件 ID 重发同一条历史消息时重复回复的问题。
- 引入平台消息创建时间校验：能确定已超过 5 分钟的消息会在“了解”表情、文字回复、知识库、上传和任务路由之前静默拦截；平台未提供或无法解析时间时保持旧行为，避免损伤兼容消息。
- 运行控制台对处理、过期和重复消息统一记录消息 ID、平台创建时间和本机接收时间；旧 SQLite 事件表自动增量迁移，既有队列、上传、匹配、风险、Excel、Word、知识库和大模型问答接口不变。

## v5.8.16 - 2026-07-14

- 第二层机器人新增“第一梯队”确定性任务指令：`任务` / `最近任务`、`进度 FS-任务编号`、`风险 FS-任务编号`、`高风险`、`结果 FS-任务编号`、`帮助` / `指令`；完整指令直接读取本地任务队列，不进入大模型托底。
- 所有任务列表和详情查询按当前 `chat_id` 强制隔离，群与群、群与单聊之间不能交叉读取任务编号、文件名、风险和成果；群聊仍必须明确 @当前机器人，单聊可直接发送。
- “结果”只重发原任务已保存的 Excel、Word 和经典完成卡片，不重新计算、不修改原任务状态；成果过期、任务未完成、跨会话访问和上传失败均返回确定性提示并保留原记录。

## v5.8.15 - 2026-07-14

- 第二层机器人在 Excel、Word 成果文件成功上传后发送第一版绿色完成卡片，集中展示任务编号、输入文件、处理状态、结构化风险和高风险数量；配置“进入造价智算 URL”后增加只读跳转按钮。
- 卡片只负责结果摘要和页面跳转，不接收审批、复核或改值回调；卡片发送失败自动降级为原有文字完成通知，成果文件上传和任务完成状态不受影响。
- 完成卡片已通过企业 WeAct 真实消息接口验证；适配 WeAct 不接受 `update_multi=false` 的兼容差异，并保留平台业务错误码便于运行控制台排障。

## v5.8.14 - 2026-07-14

- 新增第二层群成员确定性查询：群聊明确 @本机器人后，完整输入“群里有几个人”“群成员”或“都有谁”，机器人直接调用飞书 / WeAct 群成员接口返回真实人数和编号名单，不经过大模型。
- 群成员接口支持 100 人每页的自动分页、重复成员去重、名单超长截断和权限错误提示；单聊输入三条指令时提示到目标群查询，近似说法继续走普通大模型问答。
- 自我介绍增加群成员查询用法，机器人运行控制台区分群成员接口查询与大模型托底；增加精确指令、近似说法、单聊边界、分页去重和输出格式测试。

## v5.8.13 - 2026-07-14

- 修复群聊中 @其他人或其他机器人时，本机器人仍添加“了解”表情并进入大模型问答的问题；启动时通过飞书 / WeAct 机器人信息接口读取本机器人真实 Open ID，群消息按 mentions 中被提及对象的 Open ID 精确过滤。
- 当前机器人身份无法确认时群聊保持静默，避免误回复；机器人单聊、收件窗口内独立文件消息和明确 @本机器人的上传、知识库、问候及普通问答继续按原规则处理。
- 增加 @本机器人、@其他对象、身份缺失静默、三类消息路由和机器人身份缓存测试；当前 WeAct 已成功解析本机器人身份。

## v5.8.12 - 2026-07-14

- 修复企业 WeAct 将群聊机器人提及编码为 `@_user_1`、`@_user_2` 等动态占位符时，“你好”和上传口令无法精确命中的问题；“你好”现在固定回复完整自我介绍、功能说明、群聊 / 单聊用法和专业边界。
- 大模型 HTTPS 调用对 TLS 提前断开、超时和连接中断增加最多 3 次有限重试，HTTP 业务错误仍立即返回；真实 `/api/llm-chat` 已使用当前 DeepSeek 配置返回“连接成功”。
- 增加动态提及、完整自我介绍、TLS EOF 重试恢复和 HTTP 错误不重试测试，不改变知识库、匹配、预警、Excel 或 Word 专业逻辑。

## v5.8.11 - 2026-07-14

- 普通飞书与企业 WeAct 共用精确消息分流：只有完整 `@上传` / `@上传文件` 才开启 1 分钟收件窗口；直接文件、相似说法和过期文件不建任务。
- `@知识库` 保持本地知识检索，`你好` 返回自我介绍与使用说明，其余文字复用网页普通问答接口进行大模型托底；各类文字事件保持幂等和异步回答。
- 紧急收紧表情确认范围：群聊仅明确 @机器人的消息添加“了解”，群内普通消息及未 @ 的独立文件消息不回应；机器人单聊仍正常添加。

## v5.8.10 - 2026-07-14

- 第二层机器人收到群聊 `@`消息或机器人单聊消息后，立即在原消息下方异步添加“了解”（`Get`）表情回应。
- 表情回应使用原消息 ID 和当前机器人的 REST API 域名，同时兼容普通飞书与企业 WeAct；重复投递在进程内去重。
- 表情回应失败只记录警告，不会阻断知识库问答、Excel 收件、顺序队列或专业处理链。

## v5.8.9 - 2026-07-14

- 第二层机器人运行控制台改为“刷新”旁按钮打开的弹窗，避免长期占用智能协同设置页空间。
- 消息日志新增发送人名称与 ID、群名与会话 ID、完整消息正文和附件名；通过飞书用户 / 群信息接口尽力解析名称并缓存，权限不足时回退显示 ID。长连接事件不提供来源 IP，控制台明确标注平台未提供。
- 业务日志仍隔离 App Secret、访问令牌、连接票据、access key、文件 Key 和完整 WebSocket 地址；机器人收件、顺序队列和专业处理链路保持不变。

## v5.8.8 - 2026-07-13

- 智能协同页新增第二层机器人运行控制台，实时汇总长连接、自动重连、消息接收、知识库问答和任务阶段日志，并显示当前机器人及进程状态。
- 新增只读日志接口与 2 MB 轮转结构化事件文件；原始 SDK 日志只提取安全状态，不回显访问令牌、连接票据、文件 Key、群 / 用户标识或完整连接地址。

## v5.8.7 - 2026-07-13

- 第二层企业应用明确提供 `默认机器人（普通飞书）` 与 `Weact机器人（管网内网）` 两个选项，并新增按机器人选择 API / WebSocket 域名的能力；本机默认启用普通飞书，云端默认启用 Weact，用户可随时关闭或切换。
- 企业 WeAct 配置已完成真实访问令牌获取和 WebSocket 长连接验证；启动器立即记录子进程 PID，避免 SDK 初始化期间页面短暂误报“未运行”。

## v5.8.6 - 2026-07-13

- 第一层 Webhook 支持多配置选择，新增 `weact机器人（webhook）`，切换时保留通知开关、通知规则和发送历史，不向前端回显完整地址或签名密钥。
- 第一层 Webhook 与第二层企业应用机器人配置统一迁移到 `Codex-Temp/runtime/feishu-robot-settings.json`，继续兼容旧版分散配置格式，并保证两个配置分区互不覆盖。

## v5.8.5 - 2026-07-13

- 第二层飞书机器人支持多个企业应用配置选择；新增企业 Weact 配置（现界面名称为 `Weact机器人（管网内网）`），切换时先安全退出旧长连接，再启动选中的机器人，继续复用同一单任务顺序队列。
- 保持旧版单机器人凭证文件格式兼容；状态接口只返回机器人名称和 App ID 尾号，不回显 App Secret。

## v5.8.4 - 2026-07-12

- 修复云端 FastAPI 固定监听 `1285` 时第二层飞书机器人仍沿用本机 `8000` 默认地址的问题，补充服务器运行配置说明，避免任务处理与知识库问答连接错误。
- 统一根目录、前端、后端、Tauri、Cargo、README、当前版本计划和健康检查版本号至 `v5.8.4`。

## v5.8.3 - 2026-07-12

- 统一根目录、前端、后端、Tauri、Cargo、README、当前版本计划和健康检查的版本号至 `v5.8.3`。
- 发布此前已完成的飞书第二层机器人启停开关与知识库问答能力，保持问答与 Excel 文件任务分流、事件幂等，并确保不修改价格、系数或正式知识库。
- 本次仅做版本发布与存档，不改变核心匹配规则。

## v5.8.2 - 2026-07-12

- 第二层飞书机器人新增知识库问答入口：群聊 `@机器人 + @知识库：问题`，或机器人单聊 `@知识库：问题`，自动调用现有 `/api/knowledge/ask`，先回执查询中，再异步返回答案、来源文件和“不改变程序填价结果”的边界提示。
- 知识库问答与 Excel 文件任务分流，使用事件 ID 幂等记录；无证据、模型异常或错误消息均明确回传，不进入单任务文件队列，不修改 Excel、价格、系数或正式知识库。
- 更新智能协同模块 PRD、第二层机器人操作说明、页面能力提示和版本号至 `v5.8.2`。

## v5.8.1 - 2026-07-11

- 智能协同页新增第一层“启用通知”和第二层“启用接收”即时开关；第二层关闭会真正结束本机长连接，再开启会自动拉起机器人并轮询至“运行中”，无需重新双击启动脚本。
- 修复第二层状态标签和任务表格挤出卡片的问题；明确同一飞书应用的多长连接实例为随机集群分发而非广播，本地与服务器迁移时应只启用一个实例，避免任务和成果分裂。
- 新增“数字化项目助手”一级入口：宿主仅以 iframe 嵌入独立项目的 `5175` 前端，支持 `VITE_DIGITAL_PROJECT_ASSISTANT_URL` 覆盖地址，并提供加载、10 秒超时、重试和新窗口打开降级；不复制独立业务代码、不代理 `8010` API。
- 收紧 Word 报告页操作区：将报告状态、文件名、下载 / 刷新 / 风险清单操作和统计信息重排为紧凑响应式工具栏，保留真实 DOCX 预览与所有既有下载、刷新、风险清单能力。
- 增加 `frontend/.env.example`，明确独立数字化项目助手默认嵌入地址；项目默认设定文档同步明确集中配置优先级与例外说明要求。
- 版本同步至 `v5.8.1`；验证见本次发布记录。

## v5.8.0 - 2026-07-11

- 完成“智能协同”第二层本机长连接试点：新增飞书企业自建应用机器人、群聊 `@机器人 + 单个 .xlsx` 收件、快速确认、SQLite 幂等持久队列和全局单任务顺序 Worker。
- 第二层严格复用现有专业 API，依次完成默认字段检查、待匹配任务、批量匹配、经验池预警、结构化风险、可选 DeepSeek 风险说明、Excel 和 Word 输出；字段映射不可靠时转人工，不猜测字段，大模型失败不阻断结构化成果。
- 新增飞书消息/文件 API 封装、有限重试、进程中断恢复、30 天任务文件清理、脱敏运维状态 API、智能协同第二层状态与任务列表，以及开发版/绿色版独立启动入口。
- App ID 与 App Secret 只保存在 Git 忽略的本机运行配置；绿色版携带程序和依赖但不携带秘密。第一层 Webhook 与全部既有专业模块保持独立，第二层故障不影响本地主流程。
- 新增 13 项第二层专项测试，覆盖消息校验、重复事件、FIFO、成功回传、人工处理、重试失败、恢复、清理和秘密不回显；全量后端为 `186 passed, 3 skipped, 2 failed`，两项失败与 v5.7.1 既有基线相同。前端构建、PRD 严格巡检、真实长连接握手、绿色版完整依赖构建及 v5.8.0 健康检查通过。

## v5.7.1 - 2026-07-10

- 修复风险报告正文增长后预览仍只显示 3 个结构页的问题：正式 DOCX 的元数据仍记录 3 页，但实际内容已自然排到约 7 页，原配置关闭动态分页后导致第 3 页出现约 `3395px` 正文溢出且后续内容不可见。
- 恢复有上限的动态分页，以 `120px` 分页容差忽略模板浮动页码文本框造成的约 `98px` 固定装饰溢出，并将最大动态分页轮次限制为 `100`；页脚和页码继续保留，不再制造大量空白续页。
- 使用实际下载的 `【输出】-控制价报告-20260710-2322.docx` 完成浏览器回归：预览从 3 页正确续排为 7 页，第 4—7 页及末尾“待复核提示”完整可见，控制台无错误；报告预览专项 `7 passed`、前端构建、PRD 严格巡检、绿色版完整构建和 Tauri release 编译通过；全量后端仍为与基线一致的 `173 passed, 3 skipped, 2 failed`。
- 版本同步至 `v5.7.1`，README、当前版本计划、报告预览 PRD、项目分页经验和版本记录已同步。

## v5.7.0 - 2026-07-10

- 在既有“Word 报告”页完成真实 DOCX 只读预览，用 `@file-viewer/react@2.1.25` 与独立 Word renderer 替换摘要字段拼出的模拟页面；保留 Excel / Word 下载、风险清单、返回处理和右侧问问智算，不新增一级菜单。
- 预览复用 `/api/download/{job_id}/report`，以 `Blob -> File(.docx)` 传入 renderer；File Viewer 与 Word renderer 按需加载，过期请求通过 `AbortController` 与请求序列取消，job 切换、重算、预警写回和风险报告写回会使旧预览失效。
- 新增加载、更新、成功、未生成与失败状态，HTTP / 空文件 / 非 DOCX / renderer 异常均只影响预览区；失败时可重试、下载 Word 或返回处理，且旧任务响应不能覆盖新任务。
- 将 DOCX Worker、JSZip 和第三方许可证同源同步到 `frontend/public/file-viewer`，Vite 构建复制到 `frontend/dist/file-viewer`；开发版、完整绿色版和 Tauri 静态托管均在阻断公网后成功读取真实报告。正式模板保留 Word 保存分页并关闭不适用的动态二次分页，3 页正式样例和 5 页显式分页样例均正常显示。
- 报告页重排为紧凑操作区与主体真实预览；1366×768、1920×1080、左右侧栏折叠和 1280×800 Tauri 常规窗口均无页面级横向溢出。版本同步至 `v5.7.0`，README、当前版本计划、报告预览 PRD、Word 报告 PRD、整体 UI PRD、绿色版 / Tauri 说明和项目经验已同步。
- 验证：报告预览专项 `6 passed`；前端构建和 PRD 严格一致性巡检通过；全量后端 `173 passed, 3 skipped, 2 failed`，两项失败与基线一致，分别来自本任务未修改的标准资料检索排序和项目样例第 90 行源值；完整绿色版构建与包内断网运行通过，Tauri release 编译通过，Worker / JSZip 在 Vite 与 FastAPI 静态托管下均返回真实 JavaScript 而非 404 / `index.html`。

## v5.6.1 - 2026-07-10

- 小版本升级：统一根目录 npm 包、前端 npm 包、后端 API、前端显示、欢迎页版本标识、Tauri 配置、Cargo 元数据、README 和当前版本计划至 `v5.6.1`。
- 本次未改变业务逻辑；验证：前端构建、版本健康接口回归和 PRD 严格一致性巡检通过。

## v5.6.0 - 2026-07-10

- 新增“智能协同”第一层飞书群自定义机器人 Webhook：左侧一级菜单固定在“知识库问答”下方，中间页提供连接状态、秘密配置、签名提示、通知规则、测试发送和脱敏历史，右侧问问智算保持不变。
- 新增独立 `backend/app/feishu_webhook.py` 与状态 / 设置 / 测试 / 历史 / 受限事件通知 API；Webhook 地址和签名密钥只写入被 Git 忽略的运行目录，支持飞书官方 HMAC-SHA256 签名、完成卡片 URL 跳转、瞬时故障一次有限重试和秘密脱敏。
- 现有“开始转换 -> 待匹配预览 -> 批量匹配”前端链路以 best-effort 方式旁路发送开始、进度、完成和失败通知；通知禁用、未配置、超时、限流或失败均不改变 Excel、匹配、预警、工作量抓取、问问智算和 Word 业务结果。
- 第一层完成通知只允许 `open_url` 跳转，不实现卡片回调；第二层企业自建应用机器人、收消息 / 文件、用户身份、任务台账、风险派单和审批继续保持未开发。
- 版本同步至 `v5.6.0`；README、当前版本计划、智能协同模块 PRD、整体 UI PRD、UI 设计规范和健康检查测试已同步，PRD 严格一致性巡检通过。
- 验证：Webhook 专项 `17 passed`；全量后端 `173 passed, 3 skipped, 2 failed`，两项失败分别来自当前未纳入本任务改动的标准资料检索排序和项目样例第 90 行源值漂移；前端构建、Tauri release 构建、绿色版完整构建及包内健康 / 未携带飞书秘密检查通过；Playwright 在 `1366×768` 和左右栏折叠态确认智能协同页与右侧智算可用。

## v5.5.4 - 2026-07-10

- 升级版本号到 `v5.5.4`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划和健康检查测试。
- 问问智算默认快捷指令补回 `@知识库：`，点击后预填输入框并聚焦，等待用户补充问题再发送；集中配置、绿色版兜底配置及回归测试同步更新。
- 将“聊天框输入文本包含已定义动作指令即执行对应快捷动作、强制查库前缀优先”的关系写入问问智算模块 PRD 和验收口径。
- 浏览器页签标题由“管勘智算”统一为“造价智算”。
- 验证：`npm run frontend:build`、项目默认配置 API 回归测试、绿色版配置复制测试与 PRD 严格一致性巡检通过。

## v5.5.3 - 2026-07-10

- 升级版本号到 `v5.5.3`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划和健康检查测试。
- 扩展 `config/project-default-settings.json`：新增带 `_说明` 的 `zhisuanWindow`，统一维护聊天区高度、Dock 宽度（默认 `400px`）、纵向高度偏好、欢迎语、快捷指令、外观风格和显示项。
- 问问智算设置页改为仅在当前会话生效，不再读写相关浏览器 localStorage 或单独欢迎语文件；可恢复项目默认。
- 绿色版构建会复制该配置，源文件缺失时生成带说明的完整兜底配置；云端部署说明同步将它列为必带运行配置。
- 验证：`npm run frontend:build`、项目默认配置 API 回归测试和绿色版配置复制测试通过。

## v5.5.2 - 2026-07-10

- 升级版本号到 `v5.5.2`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划和健康检查测试。
- 增强 `config/project-default-settings.json` 的人工维护体验：为表格预览、列映射和工作量抓取默认设置补充 `_说明` 字段，明确可手工修改的范围与含义。
- 保持标准 JSON 兼容：不用 `//` 或 `/* */` 注释，程序会忽略以下划线开头的说明字段；配置格式错误时继续回退到代码内置默认值。
- 本次不改变匹配、预览、列映射、工作量抓取或绿色版运行逻辑。

## v5.5.1 - 2026-07-09

- 升级版本号到 `v5.5.1`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 新增集中项目默认配置：`config/project-default-settings.json` 统一承载表格预览、列映射设置和工作量抓取列选择默认值，新用户、清空浏览器缓存和 Windows 绿色版首次打开都从该文件读取默认口径。
- 调整表格预览 / 列映射 / 工作量抓取设置：弹窗内修改只作为当前页面临时设置生效，不再写入浏览器 localStorage 或旧的用户偏好 JSON；弹窗提供“恢复项目默认”入口。
- 修正预览列设置保存时遗漏拖拽列宽的问题，保存默认设置后继续保留按 `sheet 名 + 列标题` 记忆的列宽。
- 绿色版打包会复制集中项目默认配置；若项目缺少该配置，则自动生成内置兜底配置，确保目标电脑首次打开也能生效。
- 新增 `tools/build_green_release.py`，生成 Windows 绿色版：后端 `8000`、前端 Vite `5174` 双服务启动，不使用桌面壳，也不以后端静态 `web/` 作为主入口。
- 绿色版随包携带 `runtime/python/`、`runtime/python-libs/`、`runtime/node/` 和 `frontend/node_modules/`，目标 Windows 电脑无需预装 Python、Node 或 npm。
- 新增 `docs/绿色版说明.md`，同步更新 README 和运行入口 PRD，明确当前对外交付只保留传统开发版和 Windows 绿色版两类入口；旧静态评委版不再作为当前交付物。
- Tauri 桌面壳保留为开发调试和未来桌面化资产，但不进入当前 Windows 绿色版；相关说明和构建脚本改为复用绿色版目录，不再引用旧评委版构建脚本。
- 新增当前 Tauri 桌面 exe 输出口径：`tools/build_tauri_release.py` 会生成 `造价智算-Tauri桌面版-日期-版本` 目录和 zip，随包携带 Python / Python 依赖 / `frontend/dist`，不携带 Node / npm / Rust / Cargo。
- 修正 Tauri 桌面版停止机制：停止入口改为 `停止造价智算-Tauri桌面版.bat` 调用 PowerShell 清理 PID 文件和当前包后端进程，降低中文路径和批处理转义问题。
- 绿色版和 Tauri 桌面版打包时复制项目根目录现有 `.env.local`，用于目标电脑直接使用问问智算；API Key 仍不写入源码和代码存档。
- 保持稳定边界：本次只改启动、打包和文档说明，不修改基价 / 单价匹配、调整系数规则、经验池预警、工作量抓取、Word 报告或大模型“只解释、不裁决”的业务边界。

## v5.5.0 - 2026-07-07

- 升级版本号到 `v5.5.0`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 落地问问智算可动虚拟形象：右侧智算身份区和收起入口接入极简内联 SVG 机器人，按待命、听取输入、思考、处理、需复核、异常、完成切换状态。
- 优化助手动效：待命态加入蓝色呼吸光晕、外圈节奏和状态点脉冲；思考 / 处理态改为浅绿色呼吸，处理时放大镜圆圈轻微晃动；折叠态显示机器人形象作为展开入口。
- 调整填价工作台匹配质量分布环为低饱和莫兰蒂配色，降低高风险红的视觉刺激。
- 文档同步：更新问问智算 PRD、智算助手形象 RRD、当前版本计划和 README，明确该形象不改后端 API、知识库检索、风险评级、价格 / 系数候选生成和写回逻辑。
- 验证：`npm run frontend:build` 通过。

## v5.4.2 - 2026-07-06

- 升级版本号到 `v5.4.2`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 重画欢迎页为浅色 Navattic 式首屏：白底细线构图、居中大标题、克制主按钮和下方造价智算工作台预览框，弱化原深色大屏感。
- 保留原深色欢迎页为未启用变体：旧深色流程板仍在前端分支和 CSS 中留存，默认入口暂不启用，不影响主界面和业务流程。
- 验证：`npm run frontend:build` 通过。

## v5.4.1 - 2026-07-05

- 升级版本号到 `v5.4.1`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 收口结果预览列宽体验：表头右边缘可像 Excel 一样拖拽调节列宽，按 `sheet 名 + 列标题` 保存本机偏好；不显示额外手柄，保持表格简洁。
- 收口两阶段批量匹配交互：`开始转换` 后不自动跳转结果预览，由右侧智算输出“跳转到表格预览”按钮；首次进入后说明表格预览功能，并提供“批量匹配”按钮。
- 统一批量匹配入口：预览窗口按钮、智算消息按钮、快捷指令首项“批量匹配”和输入框文字“批量匹配”均复用同一批量匹配动作；完成后中间按钮保留为“已完成匹配”禁用态。
- 优化大尾巴主题细节：左侧模块导航去掉 `01/02/03` 数字编号，预览窗口工具栏按钮降为紧凑低高度样式，完成态改为浅蓝轻量胶囊。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过。

## v5.4.0 - 2026-07-05

- 升级版本号到 `v5.4.0`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 新增经验池治理报告：`/api/quality/experience-pool` 只读检查重复记录、单位不一致、样本不足、极端离散、关键字段空值和经验数不可解析，并生成 Markdown 治理报告。
- 新增风险报告增强与标准依据轻量追溯：`/api/risk/summary` 输出结构化风险项，`/api/standard-trace` 汇总待复核、经验池来源和规则说明线索，前端 Word 报告页可查看风险清单并跳转定位。
- 新增演示 / 评委模式 MVP：填价工作台可一键加载预置 100 行样例，按正式转换链路生成结果，并提供结果、预警、风险清单和报告的轻引导。
- 修复演示样例加载：演示接口复用正常前端的候选 sheet 识别和列映射口径，自动跳过“费用汇总”等非明细页，避免完整控制价工作簿误报缺少 `要素1`、`单位`。
- 新增辅助填价 MVP：结果预览行操作区可打开辅助填价弹窗，展示知识库相似和经验池同类均值候选；用户确认后复用输出副本写回校验、浅蓝批注和 `preview-manual-edits.json` 留痕。
- 增强辅助填价与标准依据追溯联动：基价 / 单价辅助填价弹窗合并展示价格候选和当前行轻量追溯；两个系数候选与溯源作为第二步写入 PRD 待开发。
- 调整主流程为两阶段批量匹配体验：`开始转换` 只生成待匹配预览，预览窗口蓝色 `批量匹配` 按钮再执行价格和两个系数识别填写，右侧智算同步输出阶段提示。
- 修复两阶段批量匹配：刷新预览、人工修改或重算公式后继续保留开始转换时的列映射和 sheet 配置，避免批量匹配误报缺少 `要素1 / 单位`。
- 验证：`python -m pytest backend/tests -v` 通过（150 passed, 3 skipped）；`npm run frontend:build` 通过。

## v5.3.7 - 2026-07-03

- 升级版本号到 `v5.3.7`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 修复预览人工修改不可用问题：回车和失焦保存改为按当前输入框快照提交，避免双击其他单元格时串到下一个单元格。
- 优化保存性能：单元格保存只做输出 Excel 快速写值、浅蓝标记、批注和 JSON 留痕，不再每次触发 Excel 重算、全量预览刷新和 Word 报告重写。
- 新增结果预览“重算公式”按钮与 `/api/preview/recalculate` 接口：需要刷新汇总公式和 Word 报告时由用户手动触发。
- 验证：`npm run frontend:build` 通过；`python -m pytest backend/tests/test_api.py::test_preview_cell_edit_updates_output_workbook_preview_and_log backend/tests/test_api.py::test_preview_cell_edit_rejects_formula_and_merged_non_start_cells -v` 通过。

## v5.3.6 - 2026-07-02

- 升级版本号到 `v5.3.6`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 实现结果预览表格人工修改：用户可在结果预览中双击编辑输出 Excel 副本的普通值单元格，保存后即时刷新预览。
- 新增后端 `/api/preview/cell` 写回接口：校验公式单元格、合并单元格非左上角、隐藏行、受保护单元格和系统生成列只读，写入浅蓝底、批注和 `preview-manual-edits.json` 留痕。
- 调整公式解析口径：遇到公式优先尝试程序侧重算，失败再使用 Excel 缓存，保障人工改值后预览和 Word 报告关键金额尽量同步。
- 验证：`npm run frontend:build` 通过；`python -m pytest backend/tests -v` 通过。

## v5.3.5 - 2026-07-02

- 升级版本号到 `v5.3.5`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 调整填价工作台布局：上传标准 Excel 固定在中间左上，转换后概览移动到中间右上，工作状态移动到两个上层模块下方并自适应横向空间。
- 精简工作状态模块：取消冗余输出摘要和“实物 / 技术 / 预警”细分指标块，保留匹配状态、匹配质量分布和关键摘要。
- 统一智算报告跳转按钮视觉：“去 Word 报告”按钮改为主题蓝底白字，和主操作按钮保持一致。
- 验证：`npm run frontend:build` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/archive_code.py` 通过。

## v5.3.4 - 2026-07-02

- 升级版本号到 `v5.3.4`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README 和当前版本计划。
- 新增统信 UOS 兼容性准备说明：明确 Windows 主线不迁移，统信方向优先验证 FastAPI 后端 + React 构建产物的本地网页版，Tauri Linux / deb 打包留到后续阶段。
- 新增 `tools/check_platform_compat.py`：检查明显 Windows 绝对路径、Windows 专属调用、前端 import 大小写、关键相对路径和 `.env.local` 是否被排除在代码存档之外。
- 新增 `backend/app/paths.py`：统一收口项目根目录、知识库目录、经验池目录、模板目录、运行时目录和临时目录等路径常量，不移动真实业务文件。
- 新增 `scripts/start-linux.sh` 作为 Linux / 统信本地网页版启动草案，保留现有 Windows `.bat`、评委绿色版和 Tauri Windows 运行方式。
- 新增 `frontend/public/zhisuan-welcome-message.txt`：右侧“问问智算”默认开场白改为运行时读取纯文本文件；设置页保存的个人欢迎语仍优先生效。
- 修正前端显示版本常量遗漏，页面展示版本同步为 `v5.3.4`。
- 更新代码存档脚本：纳入 `scripts/`，并显式排除 `.env.local`。
- 验证：`python -m pytest backend/tests -v`、`cd frontend && npm run build`、`python tools/check_prd_consistency.py --strict`、`python tools/check_platform_compat.py`。

## v5.3.3 - 2026-07-01

- 升级版本号到 `v5.3.3`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 固化 UI 设计入口：不新增独立 `design.md`，以 `00-PRD/03-整体UI设计PRD.md` 和 `00-PRD/03-UI设计规范.md` 作为唯一 UI 设计口径。
- 明确 UI 基线为“大尾巴主题唯一主界面 + Navattic 极简 SaaS 工作台”；候选 A/D、旧主题、旧 demo 不再作为新 UI 依据。
- 固化后续前端整改硬基线：严禁卡片套娃、常驻面板禁阴影、导航选中态克制、主按钮无边框深蓝。
- 新增隔离式 V2 试验场：`/v2-preview` 指向 `DaweibaLayoutV2`，使用独立 `daweiba-v2-*` 命名空间验证 Navattic-flat 平铺三栏，不污染现有主界面。
- 优化填价工作台空态适配：转换后概览在未转换时的提示区自动填满标题下方剩余空间，并随面板高度动态伸缩。
- 修复浏览器标注细节：转换后概览空态底色与上传拖拽区统一，上传图标去除发光投影，左侧匹配状态环形图改为“匹配 / 复核 / 预警”互斥口径，避免百分比叠加超过 `100%`。
- 微调左侧模块导航：当前模块选中态背景加深一档，保持克制平铺风格的同时提升可辨识度。
- 继续处理浏览器标注：工作量抓取页改为标题置顶、内容区填满剩余高度；结果预览 active sheet chip 改为主题蓝描边浅蓝底；左侧当前模块标题改为主题蓝；主工作区标题徽标从数字统一替换为语义图标。
- 统一开关开启态：大尾巴主题下原生复选框与页面设置类滑动开关开启时统一使用主题蓝，并修正后置灰色规则覆盖 checked 状态的问题。
- 验证：`python tools/check_prd_consistency.py --strict` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py tools/archive_code.py` 通过；`npm run frontend:build` 通过；Chrome + Playwright 检查 `/v2-preview` 为白底、三栏零 gap、左右 1px 边框分割、关键容器无阴影和圆角。

## v5.3.2 - 2026-07-01

- 升级版本号到 `v5.3.2`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 统一大尾巴主题细节质感：左上“智”入口改为白底主题色描边并加粗，填价工作台上层 / 下层比例调整为约 `6:4`，转换后概览和工作状态卡片改为白底细边框体系。
- 优化填价结果预览：结果预览页去掉外层套娃框，sheet 选择按钮改为参考图式轻量 chip，前缀与正文统一灰蓝色，并移除伪下拉线；宽表列宽改为可读最小宽度并补充底部横向滚动轨。
- 修复智算快捷指令：快捷指令改为只显示按钮，悬停 / 聚焦时向上弹出，不再向下遮挡输入框。
- 修复智算用户气泡可读性：快捷指令发送后的结构化用户消息和高亮关键词不再沿用旧白字规则，浅蓝气泡统一使用深灰蓝文字，关键词使用主题蓝。
- 继续收口填价工作台视觉：欢迎页标题改为高对比浅色大字；转换后概览改为不重叠的紧凑摘要卡；工作状态空态和结果态统一白底细边框，移除三个装饰性下一步大卡；列映射展开区改为内部滚动。
- 降噪左侧匹配状态仪表盘：环形图中心数字和底部统计数字降低字号与字重，避免强数字压过导航层级。
- 验证：`npm run frontend:build` 通过；本机 Chrome + Playwright 检查填价工作台 1840px 宽屏比例、转换后概览无文字重叠、列映射内部滚动、结果预览宽表、快捷指令收起态和侧栏仪表盘显示。

## v5.3.1 - 2026-07-01

- 升级版本号到 `v5.3.1`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 更新 UI 设计规范：新增 Navattic 字体与文本渲染绝对约束，明确禁用纯黑和传统深灰，文本 token 收敛为 `#0F172A / #334155 / #64748B`，全局使用 Inter 优先字体栈并开启抗锯齿。
- 落地前端字体基建：`frontend/src/styles.css` 的根变量和大尾巴主题变量改为 Inter 优先、灰蓝文本体系，清理 `#000000`、`#333` 和黑色透明阴影残留。
- 优化表格数字渲染：预览表格数字列新增 `preview-number-cell`，数字列、金额列、统计强数字和 number 输入统一启用 `font-variant-numeric: tabular-nums`。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py` 通过；前端源码搜索确认无 `#000000`、`#000`、`#333`、`rgba(0, 0, 0...)` 残留；浏览器计算样式确认 body / shell 使用 Inter 优先字体栈并开启抗锯齿，标题色为 `rgb(15, 23, 42)`，辅助文字为 `rgb(100, 116, 139)`，`preview-number-cell` 为右对齐且 `tabular-nums`。

## v5.3.0 - 2026-07-01

- 升级版本号到 `v5.3.0`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 重构大尾巴主题全局骨架：`daweiba-layout-shell` 改为纯白画布、零间隙三栏网格，左侧模块栏 / 中间工作区 / 右侧智算仅通过 `1px` 分割线衔接，主体工作区和持久面板不再使用阴影。
- 降噪左侧导航、按钮和表格工具条：菜单 active/hover 改为极浅灰底 + 深色加粗文字，主按钮统一无边框工程蓝，次按钮统一白底细边框，表格工具条和 sheet chip 改为 32px-36px 的轻量样式。
- 降噪右侧“智算”侧栏：去除整栏阴影和厚卡片感，消息流使用单字母头像 `Z` / `U`，AI 消息改为极浅灰气泡，用户消息改为轻蓝气泡，输入区固定底部且仅保留细分割线。
- 清理残留提示：强制查库空问题提示不再使用“查库：0.22 是哪来的？”示例，避免默认快捷指令语义回流。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py` 通过；本机 Chrome + Playwright 检查 1920px 宽屏三栏为 `300px 1260px 360px`、shell gap 为 `0px`、关键容器 `box-shadow: none`；结果预览页中间模块 `1224px × 994px` 填满工作区；1366px 窄宽下智算保持第 3 栏且无文档级滚动。

## v5.2.0 - 2026-07-01

- 升级版本号到 `v5.2.0`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 删除旧布局和旧主题入口：前端不再保留页面布局 / 页面主题选择，不再保留旧宽屏方案、隐藏流程条和流程条本身；主页面固定使用 `大尾巴主题 / daweiba`。
- 清理旧运行代码：移除旧主题状态、旧布局状态、旧页面设置项、旧展示页 JSX、旧流程条 JSX，以及旧主题 / 旧布局相关 CSS 选择器。
- 修复删除过程中的布局回归：补回 `.daweiba-layout-shell` 与 `.daweiba-workspace-frame` 的独立 grid 主壳规则，确认左侧模块、中间工作区、右侧智算恢复三栏顶格。
- 同步清理活跃 UI PRD：整体 UI 设计 PRD 和 UI 设计规范改为“大尾巴主题唯一主界面”口径；删除两个已过时的前端交接提示词，避免后续线程按旧保底方案接手。
- 升级 Navattic 极简 UI 约束：整体 UI PRD 和 UI 设计规范新增“严禁卡片套娃、常驻面板禁用阴影、导航选中态克制、主按钮无边框深蓝”的绝对法则，并写入大尾巴主题 CSS token 和落地片段，后续前端需按 `【尾巴选】` 和 Navattic 精选图执行。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py` 通过；本机 Chrome + Playwright 检查 1920px 宽屏，主壳为 `300px 1188px 360px` 三栏，左侧 / 中间 / 智算均从 `y=70` 顶格开始，无文档级横纵滚动。

## v5.1.1 - 2026-07-01

- 升级版本号到 `v5.1.1`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 修复大尾巴主题小窗口下右侧“智算”位置异常：在 `daweiba-layout-shell` 内强制左侧导航、中间工作区、右侧智算分别保持第 1 / 2 / 3 栏，覆盖全局小屏规则对 `.ai-dock` 的 `order:-1` 和流式定位影响。
- 修正填价结果预览的 `Sheet：` 标签文字过浅问题：预览页 sheet pill 和内部 `Sheet：` span 在大尾巴主题下使用深蓝灰 / 主蓝色，不再继承半透明白色。
- 调整填价工作台上层高度：上传标准 Excel 与转换后概览所在行提高到 `minmax(360px, 0.48fr)`，为上传卡片和“列映射设置”保留更多可见空间。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py` 通过；本机 Chrome + Playwright 检查 1000px 小窗口时智算保持第 3 栏，2048px 宽屏填价上层高度约 `496px`，真实样例结果预览中 `Sheet：` span 颜色为主蓝 / 深灰。
- 生成代码版存档 `04-输出版本存档/造价智算-2026-07-01-v5.1.1.zip`，SHA256：`93BB66711CFBFD355B8B9959B23D23BF939A949B41175E7D5242EED19D5AB9B2`；脚本已同步输出工作流目录副本路径。

## v5.1.0 - 2026-07-01

- 升级版本号到 `v5.1.0`：同步根目录 npm 包、前端 npm 包、后端版本、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明。
- 写入并实现大尾巴主题独立布局层：`daweiba-layout-shell`、`daweiba-main-content`、`daweiba-workspace-frame` 接管主布局，旧 `.app-layout` / `.workspace` / `.main-grid` / `.preview-section` 宽屏规则不再挤压大尾巴主题。
- 调整填价工作台：不再显示表格预览；上层为“上传标准 Excel”和“转换后概览”，下层为“工作状态”；工作状态承接输入行、匹配行、价格列、输出文件、实物 / 技术 / 预警摘要、匹配状态和匹配质量分布。
- 调整结果预览与经验池预警：结果预览页由表格预览模块独占中间工作区；经验池预警页新增“经验池预警模块”，并把“经验池导入模块”放在下方且默认折叠。
- 同步更新整体 UI PRD、UI 设计规范、填价结果预览 PRD、经验池预警 PRD、README 和当前版本计划，明确 UI PRD 的已完成 / 进行中 / 待开发状态标注口径。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py` 通过；本机 Chrome + Playwright 检查 2048px 宽屏，填价页无表格预览、结果预览页中间区 `1304px × 1066px` 铺满、经验池导入模块默认折叠且页面无横向 / 纵向文档级滚动。
- 生成代码版存档 `04-输出版本存档/造价智算-2026-07-01-v5.1.0.zip`，SHA256：`37E5384ED74D1A99A998C39D579100DB4F2CF8177AE17ABEC1B20F219B490D1B`；脚本已同步输出工作流目录副本路径。

## 未发布小改 - 2026-06-30

- 将 `大尾巴主题 / daweiba` 从旧布局叠皮肤整理为独立布局层：React 外层改用 `daweiba-layout-shell`、`daweiba-main-content`、`daweiba-workspace-frame`、`daweiba-fill-grid`、`daweiba-preview-section` 控制主栅格和模块显隐，不再让旧 `.app-layout`、`.workspace`、`.main-grid`、`.preview-section` 或 `.is-widescreen-half` 参与大尾巴主题主布局。
- 同步更新整体 UI PRD 和 UI 设计规范，明确大尾巴主题独立布局层为后续实现验收口径；填价页保持上三列 + 下方表格预览，结果预览页表格窗口铺满中间工作区。
- 验证：`npm run frontend:build` 通过；`python tools/check_prd_consistency.py --strict` 通过；使用本机 Chrome + Playwright 检查 1760px 宽屏运行态，`layout` 为 `296px 1016px 360px` 三栏，填价页预览区 `1016px × 568px`，结果预览页预览窗口 `1016px × 994px`，且大尾巴主题下旧外层类选择器检查均为 false。
- 继续实现 `大尾巴主题 / daweiba` 宽屏主页逻辑：左侧“当前模块”改为真实模块切换，支持填价工作台、结果预览、经验池预警、工作量抓取、报告生成和预览、知识库问答六个模块；中间区域按当前模块单窗口填满，不再把所有模块堆成可滚动网页。
- 增强左侧模块侧栏底部状态仪表盘：新增匹配状态环形图，并同步展示输入行、可视化行、待复核和预警数据；左右侧栏收起后中间工作区会重新分配宽度。
- 重整右侧“智算”随行助手在大尾巴主题下的窗口逻辑：右侧侧栏与左侧模块窗保持同样的上下顶格软件窗体节奏，收起态外层宽度同步压缩，避免横向溢出；知识库模块会自动展开智算并预置 `查库：` 前缀。
- 修正大尾巴主题视觉污染和图表样式：转换概览主卡不再继承旧主题绿色 / 薄荷色铺底；左侧状态仪表盘由粗糙 CSS 饼图改为 SVG Dashboard donut chart，包含中心总数、`Total`、外侧引线和 `数值 (百分比)` 标注。
- 继续修正大尾巴主题宽屏细节：填价工作台调整为上方“上传 Excel / 转换后概览 / 工作状态”三列、下方“表格预览”横跨整行；结果预览页的表格预览模块铺满中间区域；右侧智算宽度上限收紧到 400px 并增加缩放兜底，优先保证中间工作区；右侧智算聊天角色徽标改为 `Z` / `U` 单字母胶囊，助手绿色使用环形图同款绿色；默认快捷指令移除“查 0.22 来源”。
- 验证：`npm run frontend:build` 通过；使用本机 Edge + Playwright 检查 1920px 宽屏首屏、模块切换、左侧收起、右侧收起和双侧收起，最终结果为无横向溢出、页面高度等于视口高度，截图保存在 `Codex-Temp/visual-check-daweiba-final.png` 和 `Codex-Temp/visual-check-daweiba-final-collapsed.png`。
- 正式落地 `大尾巴主题 / daweiba` 第一阶段：页面设置新增“大尾巴主题”布局入口，新增左侧 Navattic 式模块导航、中间业务工作区、右侧问问智算随行助手结构；左侧导航和右侧 AI 助手均可折叠，折叠后中间工作区获得更多宽度。
- 大尾巴主题样式隔离在 `.shell.layout-daweiba` 与 `daweiba-*` 作用域内，按 Navattic 式浅色专业工作区落地白底、浅灰分区、黑灰文字、轻边框、蓝色重点操作和少量蓝绿智能点缀；经典布局和 `ux-pro-max-柔和蓝绿风格` 仍保留，不复用 `workstation` 废案。
- 本次只改前端布局壳、CSS 和项目上下文，不修改后端接口、Excel 填价、三数字匹配、经验池预警、工作量抓取或 Word 报告生成逻辑。
- 验证：`npm run frontend:build` 通过；使用本机 Edge + Playwright 对 `layout-daweiba` 做 1366px 与 1720px 宽度检查，页面无横向溢出，左侧导航收起 / 展开可用，右侧 AI 助手收起可用，检查截图保存在 `Codex-Temp/visual-check-daweiba-1366.png` 和 `Codex-Temp/visual-check-daweiba-wide-ai-collapsed.png`。
- 写入并实现“问问智算”二维价格库优先与模糊候选规则：价格类 `@知识库` 问题会优先检索 `03-知识库-二维数据库制作/【数据库】【导入】.xlsx`；条件明确时引用精确行，条件模糊时返回相似二维库候选供用户选择，避免直接回答“知识库未找到依据”。
- 补强知识库检索词归并：新增“地形测量 / 地形图测绘”“首级控制 / 首级控制测量”“GPS E级 / GPS测量E级”等常见问法映射；价格类问题会把 `基价 / 单价 / 价格` 作为检索意图，同时把二维库候选门槛调整为更适合模糊问法的 2 个强条件命中。
- 保持稳定边界：本次只改问问智算知识库问答、PRD、README 和回归测试，不修改 Excel 填价、三数字匹配、经验池预警、工作量抓取或 Word 报告生成逻辑。
- 验证：`@知识库，控制测量 GPS E级 多少钱？` 本地模拟返回多条二维库候选；`@知识库，地形测量 山岭隧道 多少钱？` 本地模拟优先返回二维库山岭隧道候选；新增模糊价格问题回归测试通过；`npm run frontend:build` 通过；`python -m pytest backend/tests -v` 通过，结果为 140 passed、3 skipped；`python tools/check_prd_consistency.py --strict` 通过；`python -m py_compile backend/app/knowledge_qa.py` 通过。
- 修复 `@知识库` 具体单价问题的检索排序：当用户询问“地形图测绘（地形测量） 山岭隧道（洞身） 复杂 1:2000 一般单价多少？”这类问题时，系统会优先检索二维价格库精确行；`1:2000` 会自动扩展为 `比例-1:2000`，`简单 / 中等 / 复杂` 会作为等级条件单独加权，避免标准正文相近表格抢占首位。该例当前首条证据为 `【数据库】【导入】.xlsx` 序号 118，基价 `14244`。
- 调整问问智算自定义快捷指令：设置中逐行保存的自定义问题点击后只填入聊天输入框并聚焦，不再自动发送，用户可继续编辑后按 Enter 或点击发送；内置快捷指令仍保持原触发逻辑。
- 问问智算知识库补入核心原始资料：`01-原始资料/` 下的 `计价格[2002]10号`、`财建[2009]17号` Markdown 和批注 Excel 已统一纳入检索源；修复原先 glob 遇到文件名方括号导致标准资料未实际进入索引的问题，索引规模由约 799 个片段扩展到约 3922 个片段，其中标准资料片段约 3123 个。
- 版本号升级到 `v5.0.1`：同步前端显示版本、后端 `/api/health`、根目录 npm 包、前端 npm 包、Tauri 配置、Cargo 元数据、README、当前版本计划、项目介绍和 Tauri 说明；本次只做版本面收口，不改变匹配逻辑。
- 验证：`npm run frontend:build` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`python -m py_compile backend/app/main.py tools/build_tauri_release.py` 通过；`npm run tauri:build` 通过并生成 `src-tauri/target/release/guankanzhisuan-desktop.exe`。
- 生成代码版存档 `04-输出版本存档/造价智算-2026-06-30-v5.0.1.zip`，SHA256：`B985B8B20CB5D2942E60AC7494FA563C07C20B2534F7E7F7DF162E3083A8A15E`；脚本已同步输出工作流目录副本路径。
- 修复 `@知识库，实物工作系数` 这类自然表达无法命中依据的问题：强制知识库前缀现在兼容中文逗号等标点，知识库检索会把“实物工作系数 / 实物工作费系数 / 实物系数 / 工作费系数”映射到“实物工作费调整系数”，并优先展示实物工作费调整系数规则资料；新增回归测试覆盖截图中的问法。
- 验证：`@知识库，实物工作系数如何确定` 本地模拟命中 5 条证据且首条为“实物工作费调整系数”；知识库专项测试 3 项通过；`npm run frontend:build` 通过；`python -m pytest backend/tests -v` 通过，结果为 136 passed、3 skipped。
- 按三层方案优化“问问智算”知识库回答质量：PRD 新增第一层功能知识卡、第二层行级上下文增强、第三层检索和反馈优化；新增 `问问智算功能知识卡.md` 并纳入本地索引，扩展风险报告、导出、行级 AI、强制知识库等功能类检索词，点击行级 AI 时额外携带匹配状态、匹配说明、候选数量、预警参数和预警细节等关键诊断字段。
- 验证：10 个 `@知识库` 高频模拟问题均命中本地证据；知识库专项测试 5 项通过；`npm run frontend:build` 通过；`python -m pytest backend/tests -v` 通过，结果为 135 passed、3 skipped；`python tools/check_prd_consistency.py --strict` 通过。
- 生成代码版存档 `04-输出版本存档/造价智算-2026-06-30-v5.0.0.zip`，SHA256：`9BAFB163631DC40682B16DE800AA299D87487BEFAB19639068241BD0AA239176`；脚本已同步输出工作流目录副本路径。
- 补充 `00-PRD/01-模块PRD/04-问问智算AI助手/PRD.md`：新增“问答模式与路由规则”小节，明确快捷指令、强制知识库、自动知识库问答、普通自由问答、行级 AI 复核和风险报告六种模式，以及强制知识库前缀优先的路由顺序和统一反馈要求。
- 整理项目根目录临时产物：将 `.build-cache`、`.playwright-cli`、`.pytest_cache`、`.superpowers`、`.workbuddy`、`output`、`OUTput_OrTemp_Workbuddy` 移入 `Codex-Temp/root-temp-archive-20260630/`；README / AGENTS 已补充根目录只保留开发主结构、临时过程产物进 `Codex-Temp/` 的规则，并明确 `.runtime` 和 `node_modules` 暂不迁移。
- 补充版本存档收尾规则：代码存档必须通过 `python tools/archive_code.py` 实现；脚本运行结束后不再额外检查 zip 是否存在、工作流副本是否复制成功、SHA256 是否一致或目录是否完整，是否存档成功由大尾巴人工查看。
- 更新代码存档规则：`tools/archive_code.py` 生成根目录 `04-输出版本存档` 压缩包后，会同步复制一份到 `D:\Desktop\01-工作流-【造价】\工作流\260612-【ai大赛】-管勘智算-V2.0\04-输出版本存档`；README / AGENTS 已写入该工作流副本要求。
- 瘦身 `README.md`：将其从模块细节流水账调整为项目入口、当前能力摘要、关键资产索引、运行验证和维护规则；评委版、Tauri、UI、问问智算、经验池、工作量抓取等细节回到对应 PRD / docs / 规则文档。
- 更新 `00-PRD/00-PRD工作方式说明.md`，明确 `03-UI设计规范.md` 和 `05-线程交接提示词/` 的定位；已实现的问问智算查库交接提示词改为历史交接记录，避免误判为待办。
- 新增 `tools/trim_changelog.py`，默认保留 `CHANGELOG.md` 前 3 条记录并把更早记录合并进 `CHANGELOG - 【归档】.md`，支持 `--keep` 和 `--dry-run`；同步把 README / AGENTS 的维护规则从“最新 10 个版本”收紧为“最新 3 条记录”。后续每次升级版本、集中补充变更记录或修改 `CHANGELOG.md` 后，只需运行一次该脚本；脚本未报错即可，不再反复校验归档结果，由大尾巴人工查看。
- 升级 UI 规划资产：重写 `00-PRD/03-整体UI设计PRD.md` 为全局 UI 总纲，新增 `00-PRD/03-UI设计规范.md` 作为可执行设计规范，明确造价智算应走“Excel 工作台 + 规则审查台 + AI 复核助手 + 成果输出中心”的专业工作台路线。
- 新增给【前端】线程的 UI 交接提示词 `00-PRD/05-线程交接提示词/前端-整体UI规范升级-交接提示词.md`，约定前端后续优先优化填价结果预览、问问智算 AI Dock、设置弹窗和首页 / 欢迎页，并严格不改变后端业务逻辑。
- 新增 `01-assets/01-UI参考图/` UI 参考素材工作区：按 `00-reference`、`01-assets/design`、`01-assets/bug`、`02-notes`、`code` 分流外部参考、设计稿、Bug 截图、技术经验和 UI 小样实验；同步更新 PRD 工作方式说明、README 和 AGENTS，明确 README 只保留入口、AGENTS 只沉淀长期规则。
- 判定 `现代工作台 / workstation` 为 UI 废案：移除前端布局选项、现代工作台侧栏、滚动定位属性和对应 CSS；整体 UI PRD / UI 设计规范 / 当前版本计划新增“UI 主题与布局策略”，将“大尾巴主题”作为新的独立主线候选，目标为左侧模块导航、中间主体、右侧 AI 随行助手且左右可折叠。
- 新增“大尾巴主题”A / D 静态原型 `01-assets/01-UI参考图/code/大尾巴主题-A-D-静态原型.html`：A 为浅色专业工作台，D 为柔和蓝绿智能工作区；PRD 明确若进入实现，建议交给新的【前端】线程或新 Codex 线程单独执行，不覆盖经典布局、不复用 workstation 废案代码。
- 优化代码存档脚本：`tools/archive_code.py` 排除 `01-assets/01-UI参考图/01-assets/` 和 `01-assets/01-UI参考图/00-reference/` 图片素材目录，避免 UI 参考截图把代码版存档撑大；静态 demo HTML 仍可随代码上下文归档。
- 补充 Navattic 左侧菜单参考：新增 `01-assets/01-UI参考图/02-notes/Navattic左侧菜单参考分析.md`，并在整体 UI PRD / UI 设计规范 / 当前版本计划中明确大尾巴主题可参考“极窄图标栏 + 一级模块栏 + 二级功能栏”，但必须按造价智算业务重组，避免挤压中间表格和右侧 AI 助手。
- 补充 Navattic 表格工作台参考：新增 `01-assets/01-UI参考图/02-notes/Navattic表格工作台参考分析.md`，并在整体 UI PRD / UI 设计规范 / 当前版本计划中明确大尾巴主题表格可参考搜索、筛选、筛选 chip、轻边框表格和底部分页节奏，但实现时需提高数据密度并保留固定 AI 列、状态色和 Excel 横向滚动。
- 补充 Navattic AI 助手侧栏参考：新增 `01-assets/01-UI参考图/02-notes/NavatticAI助手侧栏参考分析.md`，并在整体 UI PRD / UI 设计规范 / 当前版本计划中明确大尾巴主题右侧 AI 可参考独立抽屉、顶部身份区、固定底部输入框和折叠 / 展开动作，但必须服务当前行、知识库证据、匹配状态和风险摘要。
- 固化“大尾巴主题”正式落地设计令牌：在 `00-PRD/03-UI设计规范.md` 中补充主色、辅助色、背景色、文字色、状态色、字号、字重、行高、间距、圆角、阴影和按钮 / 卡片 / 导航栏规则；新增 `00-PRD/05-线程交接提示词/前端-大尾巴主题正式落地-交接提示词.md`，用于交给新的【前端】线程实现。
- 补充大尾巴主题隔离护栏：PRD 和交接提示词明确新线程只实现 `layout-daweiba`，不得参考 Apple / Claude / Pinterest / Starbucks / VoltAgent 等历史皮肤，不得复用 `workstation` 废案，样式必须限定在 `.shell.layout-daweiba` 或等价作用域内。
- 调整大尾巴主题正式色调：不采用候选 D 的大面积绿色 / 薄荷色背景，改为参考大尾巴提供的 Navattic 图片，使用白底、浅灰分区、黑灰文字、轻边框和少量蓝色 / 蓝绿点缀；设计令牌同步调整为主色 `#2563EB`、背景 `#F7F9FC`、边框 `#E2E8F0`。
- 更新 UI 参考素材路径：UI 参考图工作区已调整到根目录 `01-assets/01-UI参考图/`，外部参考目录改为 `00-reference/`；当前 README、AGENTS、整体 UI PRD、UI 交接提示词、参考图分析笔记和代码存档脚本已同步新路径。

## 未发布小改 - 2026-06-29

- 按问问智算 AI 助手 PRD 实现“强制知识库模式”：用户在“问问智算”中以 `查库：`、`查库:`、`@知识库`、`#知识库` 开头提问时，前端会剥离触发前缀并强制调用知识库问答路径，回答中显示“已调用知识库”。
- 后端同步增加强制查库兜底：`/api/knowledge/search`、`/api/knowledge/ask` 会识别并剥离强制查库前缀；`/api/llm-chat` 收到这些前缀时也会转入知识库检索 + 证据约束回答，避免普通大模型自由发挥。
- 更新 `00-PRD/01-模块PRD/04-问问智算AI助手/PRD.md`，将“强制知识库问答触发前缀”状态从 `[待开发]` 调整为 `[已完成]`。
- 保持稳定边界：本次只改问问智算前端路由、后端知识库问答兜底、测试和项目上下文，不修改 Excel 填价、三数字匹配、经验池预警、工作量抓取或 Word 报告生成逻辑。
- 验证：新增强制查库后端专项测试 2 项通过；`npm run frontend:build` 通过；`python -m py_compile backend/app/main.py backend/app/knowledge_qa.py` 通过；`python -m pytest backend/tests -v` 通过，结果为 134 passed、3 skipped；`python tools/check_prd_consistency.py --strict` 通过；生成代码版存档 `04-输出版本存档/造价智算-2026-06-29-v5.0.0.zip`，SHA256：`6AC5535373C4E23357971D13C67DDF2BB80D4E94C208D74135F798CFF8E8AC27`。
- 调整 PRD 备份习惯：PRD 备份改为手动触发，只有用户明确要求“备份 PRD”时才运行 `tools/archive_prd.py` 或 `备份PRD.bat`；日常修改 PRD 不再自动备份，并同步更新 `AGENTS.md`、`README.md` 和 `00-PRD/00-PRD工作方式说明.md`。
- 更新问问智算 AI 助手 PRD：新增“强制知识库模式”待开发需求，规定以 `查库：`、`查库:`、`@知识库`、`#知识库` 开头的问题必须走本地知识库检索后回答，无有效证据时固定提示人工复核，不让大模型自由补依据。
- 重排 `00-PRD/02-当前版本计划.md`：明确区分当前基线、本版已完成、本版待确认、持续优化项、本阶段暂不做和验收口径，避免把已经完成的“给其他 AI 查看导出包”误读为未完成待办；同步在 `README.md` 中说明当前版本计划的读取方式。
- 优化“给其他 AI 查看”导出脚本：`tools/export_ai_review_bundle.py` 现在会额外生成 `PRD合并.md`，自动合并当前 `00-PRD/` 下的产品总览、版本计划、整体 UI 设计和各模块 PRD，并排除 `00-PRD/archive/` 历史快照，方便直接喂给其他大模型评审。
- 新增 PRD 一致性巡检脚本 `tools/check_prd_consistency.py`，可检查模块 PRD 的需求清单、关联资产、状态标记、资产路径和旧路径残留，并生成 `00-PRD/04-PRD一致性巡检.md`。
- 当前巡检结论为硬一致性通过：8 个模块、52 条需求、59 个关联资产，缺失资产 0，旧 PRD 路径残留 0；说明 `[已完成]` 是否真实完成仍需结合 README、CHANGELOG、测试记录和实际功能人工验收。
- 为各模块 PRD 增加 `## 关联资产` 表，按模块引用现有模板、匹配规则、知识库、配置和关键代码路径；本次不移动任何真实模板、规则表、知识库或程序读取文件。
- 为全部模块 PRD 增加 `## 需求清单`，统一使用 `[已完成]`、`[进行中]`、`[待开发]`、`[暂缓]`、`[不做]` 区分完成与未完成需求；同步更新 `00-PRD/00-PRD工作方式说明.md`、`README.md` 和 `AGENTS.md` 的状态标记规则。
- 根据《人工智能创新大赛参赛作品申报书-创新-造价智算-v1.7》更新 PRD：将四类业务痛点、三链融合、混合 AI 架构、先助手后平台、规则裁决 / 模型解释 / 人工兜底、价值指标和推广路线沉淀到 `00-PRD/00-产品总览.md`、`00-PRD/02-当前版本计划.md`、`00-PRD/03-整体UI设计PRD.md` 及相关模块 PRD。
- 明确申报书和 PRD 的分工：申报书服务对外展示和参赛表达，PRD 只吸收能指导产品、规则、交互和验收的内容，不把宣传性长文直接搬入开发需求。
- 新增根目录轻量 PRD 驱动目录 `00-PRD/`：保留产品总览和当前版本计划，并将原设想的 `01-模块PRD索引.md` 改为 `01-模块PRD/模块名/PRD.md` 的分模块文件夹结构。
- 新增全局 UI 设计 PRD：`00-PRD/03-整体UI设计PRD.md`，用于沉淀主题、布局、表格预览、设置入口、AI Dock 和参赛展示的统一体验原则；模块级界面细节仍写入对应模块 PRD。
- 新增 PRD 备份脚本 `tools/archive_prd.py` 和双击入口 `备份PRD.bat`；修改 PRD 前可将当前 PRD 快照归档到 `00-PRD/archive/yyyy-mm-dd-vN/`，日常开发默认不读取历史 archive。
- 更新 `README.md` 和 `AGENTS.md`，明确新线程默认读取 `00-PRD/02-当前版本计划.md`，涉及具体功能时再读取对应模块 PRD，涉及全局界面时再读取整体 UI 设计 PRD，避免一次性读取全部 PRD 浪费上下文。
- 验证：`python -m py_compile tools/archive_code.py tools/archive_prd.py tools/export_ai_review_bundle.py tools/check_prd_consistency.py` 通过；`python tools/check_prd_consistency.py --strict` 通过并生成 `00-PRD/04-PRD一致性巡检.md`；本轮修改前已运行 `python tools/archive_prd.py --date 2026-06-29`，生成 `00-PRD/archive/2026-06-29-v6`；当前根目录仅保留 `00-PRD` 作为 PRD 入口。
- 新增“给其他 AI 评判项目”的重要文件清单：`04-输出版本存档/给其他AI评判项目的重要文件清单.md`，按第一优先级、第二优先级、测试验收、桌面打包、可选样例和不建议上传目录整理。
- 新增导出脚本 `tools/export_ai_review_bundle.py` 和双击入口 `导出给其他AI查看-核心代码与规则.bat`；运行后会在 `04-输出版本存档/给其他AI查看-核心代码与规则-yyyy-mm-dd` 下复制第一层级文件，并生成 `前端代码合并.md`、`后端代码合并.md` 和说明文件，方便直接交给其他大模型审阅。
- 修复 `导出给其他AI查看-核心代码与规则.bat` 双击后一闪而过的问题：bat 改为只负责用 `powershell -NoExit` 拉起 `tools/export_ai_review_bundle_window.ps1`，并用 `%~dp0.` 传递项目目录，避免末尾反斜杠吞掉结束引号；PowerShell 窗口负责执行导出、显示错误或结果，并在完成后等待按回车关闭。
- 保持稳定边界：本次只新增 AI 审阅导出工具和说明，不修改前端运行界面、后端匹配逻辑、规则资产、经验池预警、工作量抓取、报告生成或桌面壳运行逻辑。
- 验证：`python tools/export_ai_review_bundle.py --date 2026-06-29` 已生成目标目录；第一层级文件 14 个，前端合并文件和后端合并文件均存在；`powershell -NoProfile -ExecutionPolicy Bypass -File tools\export_ai_review_bundle_window.ps1 -ProjectRoot "D:\Codex-Temp\260612-【ai大赛】-管勘智算-V2.0\."` 通过；`python -m py_compile tools/archive_code.py tools/export_ai_review_bundle.py` 通过。
- 页面设置新增第三个主页面布局“ux-pro-max-柔和蓝绿风格”：借鉴柔和蓝绿、圆润卡片和可信服务页的视觉语言，但内容完全围绕造价智算；优先保留原有左侧功能入口和右侧预览 / 智算布局，并补充本地匹配、规则复核、经验池预警、报告输出等造价软件说明模块。
- 三种主页面布局的主工作区左上角新增左侧功能列收起 / 展开按钮：可隐藏上传、转换概览和经验池列，并让可视化表格窗口占据更多横向空间；原始工作量抓取模块和右侧智算 Dock 保持可见，收起偏好保存到浏览器本地。
- 按 `ui-ux-pro-max` 的深色高对比设计方向，重绘欢迎页右侧流程预览面板：将原白色卡片改为深色玻璃控制台风格，并同步调整流程项、版本号、指标卡的背景、边框和青蓝重点色，使其与首页深色背景更协调。
- 保持稳定边界：经典布局仍为默认；本次只改前端布局枚举、展示区、收起状态和 CSS，不修改上传、列映射、转换、预览、下载、经验池预警、工作量抓取、Word 报告、大模型接口或后端业务逻辑。
- 验证：`npm run frontend:build` 通过；使用 Vite preview 临时端口 `5186` 和 Playwright 检查桌面 / 移动截图，控制台 0 error / 0 warning；本次欢迎页右侧面板微调后再次运行 `npm run frontend:build` 通过。

## 未发布小改 - 2026-06-28

- 调整右侧“智算”Dock 的首屏起始位置：在经典布局和宽屏半版下，页面位于顶部时让智算窗口上沿更接近左侧工作窗口上沿，滚动时仍保持原有粘性跟随。
- 修正智算 Dock 随页面滚动后顶部仍残留大块空白的问题：动态偏移现在只在页面顶端启用，并通过实测左侧主窗口与智算 Dock 的上沿差值计算，滚动进入内容区后立即归零。
- 修正经典布局下智算 Dock 粘性吸附仍为顶部导航预留 62px 的问题；经典布局和宽屏半版滚动后只保留 14px 呼吸边距，现代工作台布局继续保留自身 58px 顶部吸附距离。
- “大模型设置 -> 智算窗口”新增“纵向高度跟随当前窗口”开关，默认关闭；开启后右侧智算 Dock 的纵向高度会按当前浏览器窗口自动适配，并保存到浏览器本地。
- 保持稳定边界：本次只改前端 Dock 布局和用户偏好设置，不修改 Apple 主题变量、不修改转换、预览数据、经验池预警、工作量抓取、下载或大模型逻辑。
- 验证：`npm run frontend:build` 通过。

## 未发布小改 - 2026-06-27

- 基于已确认的黑底霓虹流光粒子拼图风格，新增 18 张人工智能创新大赛 PPT 逐页高清单页视觉稿，输出目录为 `07-汇报PPT 和 演示素材/260625-【申报书PPT】人工智能创新大赛参赛作品申报PPT（深色背景）/02-PPT半成品【AI】/260627-BIGDATA流光粒子-逐页高清单页/`；单页规格为 2560 x 1440、16:9 PNG，画面内不绘制页码，文件名按 `P01` 至 `P18` 排序。
- 新增逐页清单 `260627-BIGDATA流光粒子-逐页高清单页/逐页清单.md`，记录内容依据、视觉依据、输出规格、文件清单和校验记录；本次为汇报视觉素材新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。
- 参考 Pinterest 深色 BIG DATA 模板预览风格，新增 PPT 拼图方案 D：`07-汇报PPT 和 演示素材/260625-【申报书PPT】人工智能创新大赛参赛作品申报PPT（深色背景）/02-PPT半成品【AI】/260627-PPT拼图方案D-BIGDATA流光粒子.png`，采用黑底压边、青蓝霓虹流光、粒子星点和科技模板预览式边框，仍按 18 页文稿顺序生成，缩略页为 16:9，不绘制页码。
- 新增人工智能创新大赛 PPT 三版拼图视觉方案：`07-汇报PPT 和 演示素材/260625-【申报书PPT】人工智能创新大赛参赛作品申报PPT（深色背景）/02-PPT半成品【AI】/260627-PPT拼图方案A-深蓝数据指挥舱.png`、`260627-PPT拼图方案B-黑金工程蓝图.png`、`260627-PPT拼图方案C-电光玻璃瑞士网格.png`，均按 18 页文稿顺序生成，缩略页为 16:9，不绘制页码。
- 新增 `260627-PPT拼图方案说明.md`，记录三版视觉方向、适用场景和校验结果；本次为汇报视觉素材新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。
- 修正 VoltAgent 深色主题下部分界面块过亮的问题：补齐深色表面色变量，并压暗智算 Dock、聊天输入区、快捷指令、预警状态单元格和深色滚动条表现。
- 页面设置新增“VoltAgent 蓝色重点色”开关，默认关闭；开启后只在 VoltAgent 主题下把电光绿色重点色切换为工程感蓝色，并保存到浏览器本地。
- 保持稳定边界：本次只改前端主题设置和深色主题样式，不修改 Apple 默认风格、不修改转换、预览数据、经验池预警、工作量抓取、下载或大模型逻辑。
- 验证：`npm run frontend:build` 通过。

## v5.0.0 - 2026-06-26

- 产品对外名称升级为“造价智算”，副标题升级为“工程造价辅助智能体”；前端导航、欢迎页、首页主标题、Tauri 窗口标题、后端 API 标题、Word 报告标题和大模型提示词已统一使用新名称。
- 前端版本、后端 `/api/health`、根目录 npm 包、前端 npm 包、Tauri 配置和 Cargo 元数据同步升级到 `v5.0.0` / `5.0.0`。
- “界面微调”里如果曾保存旧主标题“管勘智算”或旧副标题“长输管道勘察测量最高投标限价智能体”，升级后会自动迁移为“造价智算 / 工程造价辅助智能体”，避免本机缓存继续显示旧名。
- 代码存档、评委绿色版和 Tauri 绿色版生成脚本的对外项目名与输出包名前缀同步改为“造价智算”；历史知识库、经验池模板、输入样例和兼容启动脚本文件名暂不批量重命名，避免破坏真实路径引用。
- 更新 `README.md`、`AGENTS.md`、`docs/评委运行版说明.md` 和 `docs/Tauri桌面壳MVP说明.md`，明确新产品定位、版本状态和历史文件名兼容边界。
- 保持稳定边界：本次只改品牌文案、版本号、窗口 / 报告 / 发布脚本命名和项目上下文，不修改基价 / 单价匹配、两个调整系数规则、经验池预警算法、工作量抓取规则或“大模型只解释、不裁决”的业务边界。
- 验证：`python -m py_compile backend/app/main.py backend/app/report.py backend/app/llm.py backend/app/knowledge_qa.py tools/archive_code.py tools/build_judge_release.py tools/build_tauri_release.py` 通过；`npm run frontend:build` 通过；`python -m pytest backend/tests -v` 通过，结果为 132 passed、3 skipped；`npm run tauri:build` 通过并生成 `src-tauri/target/release/guankanzhisuan-desktop.exe`；代码版存档输出为 `04-输出版本存档/造价智算-2026-06-26-v5.0.0.zip`。

## 未发布小改 - 2026-06-26

- 新增主页面布局切换：页面设置中可在“经典布局”和“A 方案现代工作台”之间切换；经典布局默认保留原页面，现代工作台增加左侧窄导航、紧凑面板和浅色专业工作台排布。
- 保持稳定边界：布局切换只复用同一套 React 主流程和同一套后端 API，不复制 UI、不改变上传、列映射、转换、预览、下载、经验池预警、工作量抓取或 Word 报告逻辑。
- 新增 A 方案欢迎页 MVP：软件打开后显示“造价智算”启动画面，点击“进入系统”进入现有主界面；可勾选“下次直接进入主界面”，并可通过顶部“欢迎页”按钮再次打开。
- 调整欢迎页为纯色深蓝黑背景，并新增欢迎页版本标记；新版欢迎页会在网页和 Tauri 桌面版重新显示一次，避免旧的“下次直接进入”设置导致桌面版看不到欢迎页。
- 保持稳定边界：欢迎页只改前端入口体验，不修改 React 现有主流程、不修改 FastAPI 后端、不修改基价 / 单价匹配、调整系数、经验池预警、工作量抓取或 Word 报告逻辑。
- 修复 Tauri 桌面壳内文件拖入上传无效的问题：窗口创建时关闭 Tauri 默认文件拖放接管，让 WebView2 把外部 `.xlsx` 文件作为 HTML5 `File` 交给现有 React 拖拽上传区处理。
- 修正评委绿色版大模型配置排障体验：新增 `配置大模型Key-评委版.bat`，可在目标电脑自动生成并打开 `.env.local`，避免评委只修改 `.env.local.example` 后后端仍读不到 Key。
- 评委版状态检查现在会先加载 `.env.local`，并明确显示 `.env.local` 是否存在、`DEEPSEEK_API_KEY` / `SILICONFLOW_API_KEY` 是否已读取，以及修改 Key 后需停止并重启后端的提示。
- 同步更新 `README.md` 和 `docs/评委运行版说明.md`，明确绿色包不携带真实 API Key、只修改 `.env.local.example` 不生效、Windows 系统环境变量变更后需重启绿色后端。
- 保持稳定边界：本次只改评委版配置脚本、状态提示和说明文档，不修改基价 / 单价匹配、调整系数规则、经验池预警、工作量抓取、报告生成或大模型“只解释、不裁决”的业务边界。

## v4.5.0 - 2026-06-25

- 新增 Tauri 桌面壳 MVP，作为开发入口和阶段一评委浏览器入口之外的第三入口；Tauri 只负责启动或复用 `127.0.0.1:8000` 后端、等待 `/api/health`、打开桌面 WebView，不重写 React 页面或 FastAPI 业务逻辑。
- 新增根目录 `package.json` / `package-lock.json`、`src-tauri/`、`启动管勘智算-Tauri-MVP.bat`，Tauri 开发入口为 `npm run tauri:dev`，会先构建 `frontend/dist`，再由 Rust 壳启动或复用后端。
- 新增 `tools/build_tauri_release.py`，用于复用阶段一评委运行版绿色目录，把 Tauri release exe 和启动 bat 放入同一目录，后续目标电脑不需要安装 Node。
- 新增 `docs/Tauri桌面壳MVP说明.md`，说明 Tauri 目录、后端进程管理、评委版兼容、资源目录、日志目录、关闭清理和后续产品化边界。
- Tauri 壳后端启动优先使用绿色目录 `runtime/python/python.exe` 与 `runtime/python-libs`；开发目录下则使用系统 `python`，并通过 `GUANKAN_FRONTEND_DIR` 指向 `web/` 或 `frontend/dist/`。
- 修正 `启动管勘智算-Tauri-MVP.bat` 双击后一闪而过的问题：bat 不再直接调用 `npm.cmd`，改为直接调用 `tools/run_tauri.ps1 dev`，确保错误和退出状态能留在窗口里。
- 保持稳定边界：本次只新增桌面入口、启动管理、构建脚本、版本号和项目上下文，不修改基价 / 单价匹配、两个调整系数规则、经验池预警算法、工作量抓取规则、Word 报告业务内容或“大模型只解释、不裁决”的边界。
- 验证：`python -m py_compile tools/build_tauri_release.py tools/archive_code.py backend/app/main.py` 通过；`npm run frontend:build` 通过；`python -m pytest backend/tests -v` 通过，结果为 132 passed、3 skipped；`npm exec tauri -- --version` 返回 `tauri-cli 2.11.3`；已确认本机 Rust/Cargo 实际存在但当前 PATH 未刷新，新增 `tools/run_tauri.ps1` 和 `启动管勘智算-Tauri-MVP.bat` 自动补 `%USERPROFILE%\.cargo\bin`；已新增项目级 Cargo 镜像配置 `src-tauri/.cargo/config.toml`，解决 crates.io TLS / 超时问题；安装 Visual Studio Build Tools C++ 生成工具后，`npm run tauri:build` 通过并生成 `src-tauri/target/release/guankanzhisuan-desktop.exe`；临时后端 `/api/health` 返回 `v4.5.0`，同源根路径返回前端页面；使用项目样例 Excel 经 `/api/inspect` + `/api/process` 成功生成输出 Excel 和 Word，下载后 openpyxl / python-docx 可打开；经验池预警接口返回 101 个候选、100 行可比选、1 条高风险预警，并成功写回 `预警参数 / 预警细节`；原始工作量抓取接口成功生成标注表和填好数量后的控制价表；阶段一评委运行版 `v4.24` 可独立启动，`/api/health` 返回 `v4.24`，根路径返回前端页面，停止脚本已清理后端进程；当前环境未配置 `DEEPSEEK_API_KEY`，核心 Excel / Word / 预警 / 工作量抓取流程不受影响；生成代码存档 `04-输出版本存档/管勘智算-2026-06-25-v4.5.0.zip`，SHA256 以本次最终输出为准。

## v4.24 - 2026-06-25

- 新增 Windows 评委运行版第一阶段：前端生产构建后由 FastAPI 同源托管，评委访问 `http://127.0.0.1:8000/` 即可使用，不再需要启动 Vite 前端服务。
- 新增 `tools/build_judge_release.py`，可生成 `04-输出版本存档/管勘智算-评委运行版-YYYY-MM-DD-v4.24` 绿色目录和同名压缩包，目录内包含启动、停止、状态检查脚本、`.env.local.example` 和评委快速运行说明。
- 新增评委版说明 `docs/评委运行版说明.md` 与运行时依赖清单 `backend/requirements-runtime.txt`，明确哪些文件进入评委版、哪些文件不进入评委版。
- 评委版现随包内置官方 Windows embeddable Python 到 `runtime/python/python.exe`，并修补 `_pth` 以加载 `runtime/python-libs`，目标电脑无需预装 Python。
- 前端生产环境默认使用同源 API，开发环境继续默认调用 `http://127.0.0.1:8000`，不影响现有 `5174` 开发启动方式。
- 保持稳定边界：本次只改启动、托管、配置和打包交付层，不修改基价 / 单价匹配、两个调整系数规则、经验池预警算法、工作量抓取规则、Word 报告业务内容或“大模型只解释、不裁决”的边界。
- 验证：`python -m py_compile tools/build_judge_release.py backend/app/main.py` 通过；`npm run build` 通过；`python -m pytest backend/tests -v` 通过，结果为 132 passed、3 skipped；评委版启动器可启动 `http://127.0.0.1:8000/`，`/api/health` 返回 `v4.24`，根路径返回前端页面；使用项目示例 Excel 调用 `/api/process` 成功生成输出 Excel 和 Word，下载后 openpyxl / python-docx 可打开；经验池预警接口返回 101 个候选、100 行可比选、2 条预警；原始工作量抓取接口成功生成标注表和填好数量后的控制价表；未配置 DeepSeek API Key 时，大模型问答返回明确配置提示，核心 Excel / Word 流程不受影响；生成评委运行版 `04-输出版本存档/管勘智算-评委运行版-2026-06-25-v4.24.zip`；生成代码存档 `04-输出版本存档/管勘智算-2026-06-25-v4.24.zip`；两个 SHA256 以本次最终构建输出为准。

## 未发布小改 - 2026-06-23

- 新增人工智能创新大赛参赛作品申报书领导增强版：`07-汇报PPT 和 演示素材/260623-人工智能创新大赛参赛作品申报书-管勘智算-领导增强版.docx`，围绕数字化部领导视角强化“三链融合、人机协同、智能体、Skill、造价数字员工”等表达，保留模板封面、目录和数据合规承诺书结构。
- 本次为参赛申报材料新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。
- 移除首页独立 Version 卡片，将当前版本号合并展示到顶部“本地结构化匹配 · 127.0.0.1:8000”状态文本之后。
- 原始工作量抓取字段设置新增“匹配到要素1后，要素2-5默认识别后面的连续列”规则；工作量表默认开启，控制价计算表默认关闭，且遇到单位列时不把该列识别为要素。
- 原始工作量抓取字段设置继续保留“字段未识别时默认选择前一字段下一列”的兜底规则；本次只影响工作量表格列选择和控制价计算表列选择的自动映射，不改变抓取匹配 A+B 规则、主价格匹配、经验池预警或第二层经验提示。
- 验证：相关工作量映射与字段偏好专项测试 9 项通过；`npm run build` 通过。未提升版本号，未生成代码存档。
- 新增今天汇报使用的参赛汇报与交流提纲：`07-汇报PPT 和 演示素材/20260623-管勘智算参赛汇报与交流提纲.md`，按当前 v4.23 进度整理项目介绍、现场演示顺序、交流内容和需讨论问题清单。
- 本次为汇报材料新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。

## v4.23 - 2026-06-23

- 价格匹配模块的列映射设置新增“不匹配指定列无值的行”，默认开启，指定列默认 `数量`；空白、数字 `0`、公式结果 `0` 都视为无值。
- 满足该排除条件的行不做基价、实物工作费调整系数、技术工作费调整系数匹配，不写逐行匹配报告，也不计入匹配统计。
- 匹配报告列新增结果着色：匹配不上 / 无候选标红，需要复核的多候选或冲突类待复核标黄；价格单元格原有成功绿、待复核红口径保持不变。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 127 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.23.zip`，SHA256 以本版本 `python tools/archive_code.py` 最后一次输出为准。测试进程收尾时曾出现一次 Windows COM 相关 fatal exception 堆栈，但后续最终全量测试未再出现且 pytest 返回通过。

## 未发布小改 - 2026-06-22

- 新增 3 张管勘智算广告式招牌页素材：`07-汇报PPT 和 演示素材/20260622-管勘智算招牌页-【codex】`，用于项目介绍 PPT 或参赛展示封面/过渡页。
- 本次为视觉素材新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。

## v4.22 - 2026-06-23

- 调整宽屏半版模块排布：经验池与预警数据模块改到转换后概览下方，横向宽度与转换后概览所在左列对齐。
- 原始工作量抓取模块改到可视化表格窗口下方，横向宽度与可视化表格窗口所在主列对齐；经验池模块与工作量模块在同一行起始，上沿高度一致。
- 顺手同步前端版本角标，修正页面显示版本仍停留在 `v4.20` 的问题。
- 保持稳定边界：本次只改宽屏半版前端布局、版本号和项目上下文，不改变默认非宽屏布局、转换、预览数据、经验池预警、工作量抓取或后端匹配逻辑。
- 验证：`npm run build` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.22.zip`，SHA256 以本版本 `python tools/archive_code.py --version v4.22 --date 2026-06-23` 最后一次输出为准。

## v4.21 - 2026-06-23

- 原始工作量抓取模块新增“控制价表设置”，可保存控制价计算表字段自动识别偏好，覆盖 `要素1-5`、`单位`、`数量(信息抓取)`、两个系数、`委托方备注(信息抓取)` 和 `抓取日志`。
- 新增后端接口 `/api/workload-capture/target-field-preferences`，设置保存到 `05-经验池-预警数据/workload-target-field-preferences-【codex】.json`；保存后如已上传控制价计算表，前端会自动重新识别目标表表头。
- 目标表 inspect 现在会读取目标表字段偏好；例如维护 `实物工作费调整系数(信息抓取)=实物工作费调整系数-测试` 后，控制价计算表列选择会优先选中该列。
- 保持稳定边界：本次只改原始工作量抓取的目标表自动列识别设置，不改变 A+B 匹配规则、主价格匹配、经验池预警或第二层经验提示。
- 验证：`python -m pytest backend/tests/test_api.py::test_workload_target_field_preferences_are_saved_and_used_for_inspect backend/tests/test_api.py::test_workload_field_preferences_are_saved_and_used_for_inspect -v` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint backend/tests/test_api.py::test_workload_target_field_preferences_are_saved_and_used_for_inspect -v` 通过；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.21.zip`，SHA256：`3BA8F961ADA10F501AB1D920C20D23DBED65F2F195E803BB925FD701E620D2D8`。

## v4.20 - 2026-06-23

- 右上角页面设置新增“隐藏工作条”开关，用于隐藏宽屏半版顶部 1-5 流程工作条。
- “隐藏工作条”默认开启，即首次打开软件时默认不显示该工作条；用户可在页面设置中关闭该开关恢复显示。
- 保持稳定边界：本次只改前端页面设置和宽屏流程条显示，不改变转换、预览、预警、Excel 输出或 Word 报告。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 119 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.20.zip`，SHA256 以本版本 `python tools/archive_code.py` 最新输出为准。

## v4.19 - 2026-06-23

- 修复软件首次打开后，填价结果预览已保存的“每个 sheet 读取第几行作为列名”设置不会自动生效的问题。
- 转换完成拿到结果后，前端会检查当前 sheet 是否存在已保存的预览表头行设置；若存在，会自动调用 `/api/preview/refresh` 按用户设置刷新预览，不再要求手动点一次“保存设置”。
- 自动刷新失败时不影响主转换结果，只提示预览刷新失败，避免把已经成功生成的 Excel / Word 错误标成转换失败。
- 保持稳定边界：本次只改前端预览刷新时机，不改变匹配、预警、Excel 输出或 Word 报告。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 119 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.19.zip`，SHA256 以本版本 `python tools/archive_code.py` 最新输出为准。

## v4.18 - 2026-06-23

- 填价结果预览设置新增“每列最大显示宽度”，默认 8 个字符，可由用户保存到浏览器本地预览偏好。
- 可视化表格单元格改为按用户设置宽度自动换行，最多显示 2 行，超出部分隐藏；鼠标悬浮仍可查看完整内容。
- 取消预览单元格旧的固定 15 / 28 字符文本截断，改由统一 CSS 两行截断控制；AI 操作列不参与该宽度限制。
- 保持稳定边界：本次只改填价结果预览的前端显示和本地偏好，不改变输出 Excel、Word 报告、匹配、预警或后端价格裁决。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 119 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.18.zip`，SHA256 以本版本 `python tools/archive_code.py` 最新输出为准。

## v4.17 - 2026-06-23

- 填价结果预览设置新增“读取第几行作为列名”：可按每个 sheet 单独指定表头行，并保存到浏览器本地偏好。
- 保存预览设置时，如果当前已有转换结果，前端会调用新增的 `/api/preview/refresh`，后端按用户指定的表头行从输出 Excel 重新刷新预览；该操作不重新匹配、不改变输出 Excel。
- 运行经验池预警分析时同步携带用户保存的预览表头行设置，确保预警写回后可视化表格窗口仍按用户指定行展示列名。
- 补充后端接口回归测试，确认用户把某 sheet 从第2行表头改为第3行表头后，刷新预览返回第3行列名和对应数据行。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 119 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.17.zip`，SHA256 以本版本 `python tools/archive_code.py` 最新输出为准。

## v4.16 - 2026-06-23

- 修复运行经验池预警分析后，可视化表格窗口可能从用户选定的第3行表头误退回第2行表头的问题。
- `table_preview` 现在随转换结果保存每个 sheet 的 `header_row`；预警写回 `预警参数 / 预警细节` 后刷新预览时，优先沿用这个确定行号，不再只靠表头文字在前几行里重新猜。
- 补充回归测试：构造第2行像草稿表头、第3行为正式表头的场景，确认预警刷新后仍保留第3行表头和后续数据行。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 118 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.16.zip`，SHA256 以本版本 `python tools/archive_code.py` 最新输出为准。

## v4.15 - 2026-06-23

- 优化“输出与预览”里的隐藏空行规则：表2 / 表3 / 表4 第5行起按指定列过滤空值和 `0` 时，第一列以 `合计` 开头的汇总行始终保留，覆盖 `合计`、`合计（含税）`、`合计（不含税）` 等口径。
- 填价结果预览同步使用同一隐藏规则，避免可视化表格把汇总行误隐藏，而下载 Excel 保留或相反。
- 可视化表格预览刷新时接入项目统一合并单元格口径：跨行合并视为每一行都有该值，跨列合并只让第一列继承值，其他列按空值展示。
- 下载隐藏空行后的 Excel 文件名不再追加 `codex` 相关尾缀，继续遵守本项目对外输出不暴露 Codex 的命名约定。
- 前端版本卡片、后端 `/api/health`、前端包版本和项目文档同步到 `v4.15`。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 117 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-23-v4.15.zip`，SHA256 以本版本 `python tools/archive_code.py` 最新输出为准。

## v4.14 - 2026-06-22

- 修正原始工作量抓取源表列选择窗口的可选字段提示：`实物工作费调整系数（待抓取）`、`技术工作费调整系数（待抓取）`、`委托方备注（待抓取）` 在某个源 sheet 中选择 `不使用` 时，不再显示红色 `必选`。
- 运行前校验同步改为按 sheet 处理可选字段，不再因为其他启用 sheet 使用了某个系数或备注，就强制当前 sheet 也必须映射该字段。
- 后端写入也增加保护：某个源行没有可选字段值时，不再把空值写入目标表对应列；`数量（待抓取）` 仍按本次抓取的核心字段处理。
- 保持稳定边界：本次只修正原始工作量抓取可选字段的 UI、校验和写入一致性，不改 A+B 匹配规则、不改主价格匹配、不改经验池预警和第二层经验提示。
- 验证：专项 `python -m pytest backend/tests/test_workload_capture.py -v` 通过，结果为 10 passed；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；`npm run build` 已输出 `✓ built`，但 Windows 工具等待进程退出时超时；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.14.zip`，SHA256：`2596D36CF71A69F077B954E3283C291912D8619ECD065B101B2E469E1A83E7F1`。

## v4.13 - 2026-06-22

- 修正原始工作量抓取里“只抓取某一列有值的行”的过滤口径：当前空白、直接填 `0`、以及公式结果为 `0` 的行，统一按“没有值”处理并直接跳过，不再输出 `抓取日志`、`抓取诊断`。
- 保持稳定边界：本次只修正工作量抓取过滤判定，不改 A+B 匹配规则、不改主价格匹配、不改经验池预警和第二层经验提示。
- 验证：专项 `python -m pytest backend/tests/test_workload_capture.py -v` 通过，结果为 10 passed；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.13.zip`，SHA256：`DA6BB6771D6AB651CFC71FE1D908C5D2856F09AD4EDDC7AE1D27476F31B8AEB6`。

## v4.11 - 2026-06-22

- 原始工作量抓取的控制价计算表列选择窗口现已真正支持 `实物工作费调整系数（待抓取）`、`技术工作费调整系数（待抓取）`、`委托方备注（待抓取）` 选择 `不使用`；当前端留空时，后端不再偷偷自动新增这些列。
- “只抓取某一列有值的行”过滤掉的工作量源行，现已彻底跳过，不再写 `抓取日志`、`抓取诊断`；同时继续保持公式单元格抓取时写入当前值而不是公式文本。
- 原始工作量抓取界面新增可见进度条和阶段文案，抓取期间会显示读取、匹配、写入、生成文件几个阶段，减少长时间等待时的空白感。
- 一对多情况现已统一按黄色预警口径处理：导出的“【临时】-原表”和“【临时】-控制价计算表”中，一对多匹配相关字段、`抓取日志`、`抓取诊断` 统一标黄；完全未命中的抓取失败仍保持红色。前端结果卡片里的匹配日志预览也同步区分为绿 / 黄 / 红。
- 保持稳定边界：本次不改主价格匹配、经验池预警、第二层经验提示或工作量抓取 A+B 匹配规则，只修正工作量抓取模块的字段启用口径、过滤后输出口径、进度反馈和预警颜色表达。
- 验证：专项 `python -m pytest backend/tests/test_workload_capture.py -v` 通过，结果为 9 passed；全量 `python -m pytest backend/tests -v -rs` 通过，结果为 115 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.11.zip`，SHA256：`4A52779FCC8673822E4F701E1FC046CDA853225EAE24C59A5C25D7F06B78076E`。

## v4.10 - 2026-06-22

- 原始工作量抓取从工作量表读取待抓取字段时，若源单元格是 Excel 公式，现改为写入公式当前值，不再把公式文本本身写进控制价计算表。
- “只抓取某一列有值的行”继续默认按工作量表列选择窗口中的 `数量` 映射列判断；被这条过滤掉的工作量行，不再写入源表 `抓取日志`、`抓取诊断`。
- 工作量表格列选择窗口文案优化：把 `数量 / 实物工作费调整系数 / 技术工作费调整系数 / 委托方备注` 统一显示为 `（待抓取）` 口径，减少和目标表 `（信息抓取）` 列的混淆。
- 保持稳定边界：本次不改主价格匹配、经验池预警、第二层经验提示或工作量抓取 A+B 匹配规则，只优化取值方式、过滤后日志口径和前端映射文案。
- 验证：专项 `python -m pytest backend/tests/test_workload_capture.py -v` 通过，结果为 8 passed；全量 `python -m pytest backend/tests -v -rs` 通过，结果为 114 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.10.zip`，SHA256：`8263821E1D1DFAFEBCC8FE780DEDEBE712AE42443C09BE443A85B4ABEB4E8F50`。

## v4.9 - 2026-06-22

- 原始工作量抓取新增“术语归并后再匹配”的旁路规则层：后端会先读取 `03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【术语归并】术语归并与匹配放宽规则表.xlsx`，再执行原有模式 A+B（`字段完全匹配 -> 非空要素顺序匹配`）。
- 当前工作量抓取已接入三类规则解析：`单位严格等价`、`要素严格等价`，以及“允许自动写值”的 `要素弱等价`；其中 `字段名` 留空时，按作用于 `要素1-5` 全部字段处理。
- 保持稳定边界：本次只增强原始工作量抓取的同类识别，不修改主价格匹配、经验池预警、第二层经验提示、前端交互或输出文件结构。
- 兼容脏表维护：规则表中“启用=是”但原值/归并值为空的不完整行会自动跳过，不会把整次抓取跑坏。
- 验证：`python -m pytest backend/tests/test_workload_capture.py -v` 通过，结果为 7 passed；全量 `python -m pytest backend/tests -v -rs` 通过，结果为 113 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.9.zip`，SHA256：`91B49737DBEFA11A16F7A5C825DD02EC5020C0EC116AC3B1406DB8A667AC9F98`。

## v4.8 - 2026-06-22

- 原始工作量抓取导出的“【临时】-原表-(工作量信息抓取后标注符合用)-yyyymmdd-hhmm.xlsx”新增 `抓取诊断` 列，放在 `抓取日志` 之后。
- `抓取诊断` 会逐行写明三类信息：这行源表最终被识别成的 `要素1-5 + 单位`、当前最可能对应的目标表/目标行、以及卡点原因。
- 当前卡点说明会优先区分 `单位不一致`、`单位一致但要素1一致且要素顺序不一致`、`单位一致但要素1不一致`、`要素1接近但单位不一致` 等几类，方便直接看出是映射问题还是业务差异。
- 保持稳定边界：本次不修改工作量抓取的 A/B 匹配口径，不放宽到前缀匹配，不修改价格匹配、经验池预警、第二层经验提示或前端操作流程。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 112 passed、3 skipped；`npm run build` 通过；代码版存档与 SHA256 以本版本 `python tools/archive_code.py --version v4.8 --date 2026-06-22` 的最新输出为准。

## v4.7 - 2026-06-22

- 优化表格末列“行级AI复核”入口：点击后不再默认弹出独立右侧面板，而是自动打开“问问智算”，把相关行级复核问题填入聊天输入框，用户在“问问智算”里点击发送后完成回答。
- “大模型设置 -> 智算窗口 -> 显示项”新增“行级AI复核模块”开关，默认关闭；需要独立行级面板时可手动开启，设置继续保存到浏览器本地。
- 收敛“问问智算”聊天颜色：取消容易误读的低风险 / 已完成整行黄绿染色，保留主题蓝重点字和必要的高风险红色提示，避免依据来源、普通结论被误认为风险分类。
- 保持稳定边界：本次只改右侧智算 Dock 的前端入口、显示项和聊天文本颜色，不修改转换、列映射、预警算法、工作量抓取、下载、知识库问答接口或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 111 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.7.zip`，SHA256 以本版本 `python tools/archive_code.py --version v4.7 --date 2026-06-22` 最后一次输出为准。

## v4.6 - 2026-06-22

- “大模型设置 -> 智算窗口”新增“显示项”偏好开关，可分别控制右侧智算窗口里的 `本次结论`、`待复核`、`预警`、`智算只做解释与提示` 和 `调试信息` 是否显示。
- 五个显示项默认全部关闭，勾选后自动保存到浏览器本地；关闭 `调试信息` 时会同步收起调试详情。
- 保持稳定边界：本次只改右侧智算 Dock 的可见内容和本地显示偏好，不修改转换、列映射、预警算法、工作量抓取、下载、知识库问答或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 111 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.6.zip`，SHA256 以本版本 `python tools/archive_code.py --version v4.6 --date 2026-06-22` 最后一次输出为准。

## v4.5 - 2026-06-22

- “大模型设置”的“智算窗口”新增“智算窗口横向宽度（px）”，默认 360px，可在 300-560px 间调整并保存到浏览器本地；右侧 Dock 展开宽度会随设置变化，收起态仍保持窄条。
- 宽屏半版布局调整：经验池与预警数据模块、原始工作量抓取模块改为放在可视化表格窗口下方，两块左右并排各占一半空间；模块内部在半宽卡片里做紧凑排布。
- 保持稳定边界：本次只改前端本地设置、宽屏布局和样式变量，不修改默认竖屏板结构，不修改转换、列映射、预警算法、工作量抓取、下载或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 111 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.5.zip`，SHA256：`1130DB9879D393493DE78E27CB69EC26C0135F3E8FF0D10C7691F3FCA7AC2B74`。

## v4.4 - 2026-06-22

- 右上角页面设置新增“宽屏半版”开关，默认关闭；开启后按 16:9 宽屏重新排布主流程，让上传 / 概览集中在左侧，填价结果预览占据中间主宽区，右侧“智算”Dock 继续保持可折叠。
- 宽屏半版新增步骤式流程条、匹配洞察卡和匹配质量环形图，强化智能系统感；宽屏下“匹配状态”列会显示为带对勾 / 提醒图标的状态胶囊。
- 表格预览在宽屏模式下放大可用宽度并收紧表格行高；窄屏时即使开启宽屏半版也会退回单列，避免压坏小屏布局。
- 保持稳定边界：本次只新增独立宽屏布局 class、前端本地设置和视觉增强，不修改默认竖屏板结构，不修改转换、列映射、预警算法、工作量抓取、下载或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 111 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.4.zip`，SHA256 以本版本 `python tools/archive_code.py --version v4.4 --date 2026-06-22` 最后一次输出为准。

## v4.3 - 2026-06-22

- 优化“问问智算”消息排版：聊天输出支持小标题、段落、无序列表、加粗、主题色关键词高亮，以及高风险 / 低风险 / 无预警等语义强调色，长文本更容易阅读。
- 新增大模型等待过程态：知识库问答、普通大模型问答和风险报告生成都会先显示“正在检索 / 正在整理 / 正在生成”等原地阶段提示，避免用户等待时只看到空白。
- 风险报告升级为结合本地知识库证据生成：后端会先检索第二层经验提示、待复核、经验池预警、调整系数和附加系数不能连乘等依据，再把证据块放入风险报告提示词。
- 风险报告提示词结构进一步固定为“本次处理结论、主要风险概览、重点复核建议、规则依据提示、后续处理建议”，但仍明确不裁决最终单价或系数。
- 保持稳定边界：本次只改智算消息显示、等待体验、风险报告证据提示词、版本和项目上下文，不修改基价匹配、调整系数裁决、经验池预警算法、工作量抓取逻辑或 Excel 写值逻辑。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 111 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.3.zip`，SHA256：`2B5C55BDADD02FD110924753CE9E5B09F0A7F691503EA62FDB8D89BE2D3CDB97`。

## v4.2 - 2026-06-22

- 调整右侧智算 Dock 顺序：“行级AI复核”提前到“问问智算”之后，便于选中表格行后立即在助手区域复核。
- 将原“当前行复核”统一改名为“行级AI复核”，表格末列 AI 按钮的提示与右侧模块标题同步。
- 优化行级复核发送按钮：从撑满输入框高度的圆形按钮改为紧凑矩形按钮，文案改为“复核”，减少突兀感。
- 在“大模型设置”的“智算窗口”中新增“智算外观风格”，默认保持当前样式；新增“智算分析台”和“随行光带”两版默认关闭的新外观，只作用于右侧智算 Dock。
- 保持稳定边界：本次只改前端右侧助手布局、样式、本地设置、版本和项目上下文，不修改匹配逻辑、知识库问答逻辑、预警算法、工作量抓取逻辑或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 111 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-22-v4.2.zip`，SHA256：`A74E79AF1A0CC935AF5E33DECBF5B294A9763D0B5309C9574669E3277E1DC24D`。

## v4.1 - 2026-06-21

- 新增“问问智算”知识库依据问答 MVP：用户在原右侧聊天窗口提问依据类问题时，前端会优先调用后端本地知识库问答接口，不新增独立页面或第二个聊天窗口。
- 新增后端 `backend/app/knowledge_qa.py`，支持 Markdown 标题切片、Excel / CSV 规则卡片抽取、关键词和同义词扩展、本地评分检索，并把运行缓存写入 `Codex-Temp/runtime/knowledge-qa-index-【codex】.json`。
- 新增 `POST /api/knowledge/search` 和 `POST /api/knowledge/ask`：前者只做本地检索，便于测试和调试；后者先检索证据，再调用 DeepSeek 做依据解释。无明确证据时固定返回“当前知识库未找到明确依据，需要人工复核。”。
- 行级 AI 复核改为携带当前 sheet、行号和预览行字段上下文走知识库问答；回答仍回到“问问智算”窗口，并列出依据来源。
- “问问智算”新增依据类快捷问题，包括“查 0.22 来源”“解释第二层经验提示”“解释附加系数不能连乘”；动作类快捷指令继续复用原预警、风险报告、Excel / Word 下载逻辑。
- 保持稳定边界：本次不修改基价 / 单价匹配、实物工作费调整系数、技术工作费调整系数、第二层经验提示、经验池预警、原始工作量抓取、Word 报告或输出 Excel 写值逻辑；DeepSeek 只解释本地证据，不裁决三个最终数字。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 111 passed、3 skipped；前端 TypeScript 程序化检查返回 `0`，Vite 已生成 `dist` 并输出 `✓ built`，但当前 Windows/Node 子进程在工具中未正常退出，`npm run build` 被超时器截断；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-21-v4.1.zip`，SHA256 以 `python tools/archive_code.py` 最新输出为准。

## v4.0.3 - 2026-06-21

- 同步更新两份重要规则文档：`【重要匹配规则】项目以及总体匹配规则介绍.md` 更新到 v4.0.3，补充经验池预警、工作量抓取、Word 报告、智算助手、输出命名和维护清单；`【重要匹配规则】要素1-5和单位的匹配模式介绍.md` 补充当前各模块实际采用的 A/B/C/D 边界。
- 新增面向普通读者的一页式项目介绍 `项目介绍-给人看的版本-【codex】.md`，并在 `README.md`、`AGENTS.md` 中挂入口。
- 修正后端 `APP_VERSION` 与当前项目版本不一致的问题，`/api/health` 现在与 README / 前端版本保持 `v4.0.3`。
- 修正“问问智算”聊天重点字高亮：助手消息中的数字行数、`第x行`、预警行数、常用功能名和“实际偏离率 xx%”使用当前主题蓝色，字号和字重继承普通正文，不再显得灰或突兀。
- 用户发送的蓝色气泡中，重点字高亮保持白色，避免蓝底蓝字导致不可读。
- “第x行”高亮范围扩展为包含“第”字，避免只高亮 `x行`。
- 设置弹窗新增内部滚动条，尤其是“大模型设置”内容较多时可以完整浏览。
- 保持稳定边界：本次只改前端显示、样式、版本和项目上下文，不修改匹配逻辑、预警算法、工作量抓取逻辑或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v` 通过，结果为 106 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-21-v4.0.3.zip`，SHA256 以 `python tools/archive_code.py` 最新输出为准。

## v4.0.2 - 2026-06-21

- 将“问问智算”消息区高度从拖拽自动保存改为“大模型设置”里手动设定并保存，避免浏览器 resize 行为导致保存不稳定。
- 开场欢迎语默认改为“你好，我是智算...”，并在“大模型设置”中新增欢迎语编辑与保存；源码默认值位于 `frontend/src/App.tsx` 的 `ZHISUAN_WELCOME_MESSAGE`。
- 聊天正文新增主题色高亮：数字行数、预警行数、`经验池预警分析`、`输出风险报告`、`输出excel表格`、`输出word报告`、`生成 AI 审查摘要`、`实际偏离率 xx%` 等关键词会按当前主题色强调。
- 快捷指令区支持自动隐藏：开启后只显示“快捷指令”把手，鼠标移入或键盘聚焦时弹出完整快捷指令抽屉；关闭后保持常驻显示。
- 保持稳定边界：本次只改前端显示、样式、本地设置和项目上下文，不修改匹配逻辑、预警算法、工作量抓取逻辑或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 106 passed、3 skipped；关键文案扫描确认当前源码不再包含“大尾巴”开场称呼；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-21-v4.0.2.zip`，SHA256：`88B94DBC1713622137FB20264EA17F0BFA28C89C29EFFB224BF9AB93373D3CD7`。

## v4.0.1 - 2026-06-21

- 放大“问问智算”消息区默认高度，并支持在消息区右下角纵向拖拽调整高度；调整后的高度自动保存到浏览器本地。
- “问问智算”快捷指令改为可配置：大模型设置中可开关 8 个内置指令，包括“经验池预警分析”“输出风险报告”“输出excel表格”“输出word报告”“哪些行优先复核？”“为什么这行待复核？”“本次预警主要集中在哪？”“生成 AI 审查摘要”。
- 大模型设置中新增自定义快捷指令，一行一个；保存后出现在“问问智算”快捷指令区，命中预警 / 风险报告 / Excel / Word 关键词时复用现有功能，否则作为普通问题发送给智算。
- 输入区改为 `Enter` 发送、`Alt+Enter` 换行；发送按钮收进输入框底部工具条，避免输入框和按钮左右割裂。
- 保持稳定边界：本次只改前端交互、样式、浏览器本地设置和项目上下文，不修改匹配逻辑、预警算法、工作量抓取逻辑或后端价格裁决。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 106 passed、3 skipped；临时前端服务因 `5174` 已被既有进程占用自动跑到 `5175`，`http://127.0.0.1:5175/` HTTP 访问返回 200；已停止本次临时 `5175` 进程，未触碰原 `5174` 进程；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-21-v4.0.1.zip`，SHA256：`262DC3B7A254D5C5AEE51B6D1C29A495A8E080AE1568DBDA016C2BAED1A61122`。

## v4.12 - 2026-06-22

- 修正原始工作量抓取列选择窗口的“必选”判定：`实物工作费调整系数（待抓取）`、`技术工作费调整系数（待抓取）`、`委托方备注（待抓取）` 现在只要源表或目标表任一侧设为 `不使用`，本次就不再算必选，也不会参与实际抓取。
- 保持稳定边界：本次只修正前端必选提示与实际抓取字段推导的一致性，不改工作量抓取 A+B 匹配规则、不改主价格匹配、不改经验池预警和第二层经验提示。
- 验证：`npm run build` 通过；`python -m pytest backend/tests/test_api.py::test_health_endpoint -v` 通过；代码版存档与 SHA256 以本版本 `python tools/archive_code.py --version v4.12 --date 2026-06-22` 的最新输出为准。

## v4.0.0 - 2026-06-21

- 将右侧可折叠 Dock 的大模型角色升级为“智算”：默认标题改为“智算”，聊天入口改为“问问智算”，展开后第一排即显示聊天窗口，收起功能保留。
- 新增“智算”欢迎语、文件接收提示、转换阶段提示、转换完成摘要、经验池预警摘要、导出提示和行级复核同步消息，让系统进展集中体现在右侧助手中。
- “问问智算”新增快捷指令：“经验池预警分析”“输出风险报告”“输出excel表格”“输出word报告”；点击后先在聊天窗口显示文字命令，再触发原左侧对应功能，不把这些命令交给大模型自由裁决。
- 风险报告入口并入“问问智算”：生成成功后报告正文在聊天窗口吐字显示，同时继续照常写入 Word 报告；原独立风险报告按钮不再占用右侧中部。
- 新增聊天吐字动效：智算消息按打字效果输出，点击正在输出的消息可立即显示全文。
- 新增本地追问响应：“哪些行优先复核？”“为什么这行待复核？”“本次预警主要集中在哪？”“生成 AI 审查摘要”等可在聊天窗口内直接得到提示或触发现有报告流程。
- 保持稳定边界：本次只改前端交互、样式、版本和项目上下文，不修改基价匹配、调整系数规则、经验池预警算法、工作量抓取逻辑或任何后端价格裁决逻辑；大模型失败时仅显示“智算辅助暂不可用”，主流程继续。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 106 passed、3 skipped；前端临时服务 `http://127.0.0.1:5174/` HTTP 访问返回 200；本次尝试连接 Codex 内置浏览器做截图级检查时，浏览器工具返回环境元数据错误，未完成截图级检查；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-21-v4.0.0.zip`，SHA256：`94D6C7196D4CA4FFBD0F17C349CF4F22835B23587F7DBC18DC5DE9396BBBEE21`。

## v3.44 - 2026-06-20

- 修复输出 Excel 公式缓存为空时，Word 报告和填价结果预览读取不到费用汇总公式值的问题：新增程序侧公式兜底解析，支持跨 sheet 引用、区域求和、四则运算、`ROUND/MIN/MAX` 等本项目常见汇总公式。
- 前端表格预览中的数值统一最多显示小数点后 2 位，避免费用汇总、基价和系数在页面上出现过长小数。
- Word 报告金额格式同步收口为最多 2 位小数；当 Excel 缓存为空时，报告会先用公式兜底计算费用汇总后再写入模板。
- 新增回归测试，覆盖“有公式但无缓存”的预览刷新和 Word 报告费用汇总读取，防止再次退回空白。
- 验证：真实输出表 `费用汇总` 可抽取为不含税 `2404.34`、增值税 `144.26`；`python -m pytest backend/tests -v -rs` 通过，结果为 106 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.44.zip`，SHA256 以 `python tools/archive_code.py` 最后一次输出为准。

## v3.43 - 2026-06-20

- 优化“界面微调”的文字编辑方式：页面进入“选取页面元素”后，点击主标题、上传区文案、按钮文字、模块标题和 AI Dock 标题等可编辑文字，会自动把右侧“界面文字”下拉框切换到对应文案。
- “界面文字”下拉项改为更直观的标签，例如“经验池标题：独立经验池导入与预警数据”，方便不通过点击时也能直接找到要改的文字。
- 选取模式下新增可编辑文字的高亮提示；正常页面模式不拦截点击，不改变上传、映射、转换、预警分析、下载、工作量抓取或大模型请求逻辑。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 104 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.43.zip`，SHA256 以 `python tools/archive_code.py --version v3.43 --date 2026-06-20` 最后一次输出为准。

## v3.42 - 2026-06-20

- 扩展“界面微调”里的“选取页面元素”范围：新增输入面板、转换概览面板、预览模块、经验池模块、工作量模块、sheet 标签栏、字段映射项、复选项、映射行控制、下载区、预警操作条、预警信息卡片、结果提示卡等可选目标。
- 经验池与工作量模块补齐更细粒度的可选节点：经验池内容面板、经验池文件卡片、经验池字段行、经验池映射面板、工作量内容面板、工作量文件区域、工作量文件卡片、工作量字段行、工作量映射面板，以及工作量表 / 控制价表两个映射子面板。
- 选取逻辑仍只在用户主动进入“选取页面元素”模式时拦截点击；退出选取后不影响上传、勾选、列映射、转换、预警分析和下载等原有交互。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 104 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.42.zip`，SHA256 以 `python tools/archive_code.py --version v3.42 --date 2026-06-20` 最后一次输出为准。

## v3.41 - 2026-06-20

- 修复非 Apple 主题下部分晚定义面板仍显示 Apple 浅色块的问题，重点覆盖填价结果预览脚部、预览指标块、经验池导入面板、经验池文件选择区、工作量文件卡片、工作量映射子面板和运行预警提示条。
- 保持默认 Apple 主题基线不动：本次新增样式只匹配 `.shell:not(.theme-apple)`，VoltAgent 额外补深色工程面板背景。
- 这次只调整前端 CSS 和版本上下文，不改变转换、列映射、下载、经验池预警、工作量抓取或大模型请求逻辑。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 104 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.41.zip`，SHA256 以 `python tools/archive_code.py --version v3.41 --date 2026-06-20` 最后一次输出为准。

## v3.40 - 2026-06-20

- 右上角页面设置新增“界面微调”入口，可打开右下角浮动面板，对主标题区、上传拖拽框、主按钮、列映射面板、预览窗口、工作量文件卡片和 AI Dock 等关键区域做小范围样式微调。
- 微调能力默认关闭；启用后只叠加用户设置中的左右 / 上下留白、字号、圆角、间距、上边距和透明度，不改变转换、映射、下载、预警、工作量抓取或大模型请求逻辑。
- 微调面板新增“界面文字”编辑，可修改主标题、副标题、首页说明、上传区文案、按钮文案、模块标题和 AI Dock 标题；保存到 `Codex-Temp/runtime/ui-preferences-【codex】.json`。
- 后端新增 `GET /api/ui-preferences` 和 `POST /api/ui-preferences`，并对用户样式和文案做白名单清洗，避免用户设置文件写入不受控样式。
- 新增回归测试，覆盖页面用户设置保存、清洗和读取。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 104 passed、3 skipped；后端 `GET /api/health` 返回 `v3.40`，`GET /api/ui-preferences` 返回默认关闭的用户设置；前端 `http://127.0.0.1:5174` HTTP 访问返回 200；Playwright 点击验证因当前运行时缺少 `playwright-core` 未完成；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.40.zip`，SHA256 以 `python tools/archive_code.py --version v3.40 --date 2026-06-20` 最后一次输出为准。

## v3.39 - 2026-06-20

- 收窄全页面带文字的“设置”按钮：高度从偏重的 42px 压到 32px，字号、图标、横向留白和圆角同步变轻，避免表格工具栏和模块头部被按钮撑高。
- 保留统一文案和入口规则：带文字的设置入口仍显示齿轮图标加“设置”，原本纯图标入口继续保持纯图标。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 103 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.39.zip`，SHA256 以 `python tools/archive_code.py` 最后一次输出为准。

## v3.38 - 2026-06-20

- 统一全页面带文字的设置入口按钮：列映射、预览、预警、经验池、工作量抓取等入口统一为齿轮图标加“设置”的按钮文案和视觉风格；原本只有图标的设置入口继续保持纯图标，不额外加文字。
- 将经验池导入模块的“只导入某一列有值的行”和判断字段从主页面移入经验池设置窗口，主流程页面只保留导入文件、导入数值和映射选择。
- 将原始工作量抓取模块的“只抓取某一列有值的行”和判断字段从主页面移入工作量设置窗口，主流程页面只保留文件选择、抓取字段和抓取按钮。
- 新增设置弹窗内过滤行样式，并同步非 Apple 主题和移动端布局，避免设置项在不同主题下视觉不一致。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 103 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.38.zip`，SHA256 以 `python tools/archive_code.py` 最后一次输出为准。

## v3.37 - 2026-06-20

- 右上角“页面设置”新增“隐藏指定列无值的行”开关，默认开启；字段选择与预警模块“只核查指定列有值的行”保持同一组选项和默认值，当前默认按 `数量` 判断。
- 点击“下载 Excel”时，如果启用该设置，后端会临时生成一个隐藏空行版输出表：仅作用于表2 / 表3 / 表4，且只从第5行（含）开始隐藏指定列为空或为 `0` 的行；原始转换结果文件不被改动。
- 填价结果预览同步支持同一隐藏逻辑，非表2 / 表3 / 表4 的 sheet 仍显示全部行；预览标题会提示已隐藏行数，行级 AI 仍按原始预览行序传递上下文。
- 后端下载隐藏逻辑复用预警模块的字段别名和有值判断口径，`数量 / 工程量 / 工程数量 / 工程量合计` 可视为同一指定列，空白、`0`、公式结果为 `0` 均视为无值。
- 新增回归测试，覆盖下载 Excel 时只隐藏核心 sheet 第5行起的空 / 零指定列行，并确认费用汇总等非核心 sheet 不受影响。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 103 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.37.zip`，SHA256：`17AC0CB8DE2FCE9AC92A2582053F3E0DE8A7F4AC6BAB2CA557E7885D8B0813CC`。

## v3.36 - 2026-06-20

- 修复主题切换残留问题：非 Apple 主题现在会继续覆盖页面设置中的 Claude 字体行、右侧 AI Dock 的问答窗口 / 调试窗口 / 行级复核结果块、经验池预警卡片，以及工作量抓取结果卡片，不再出现切到 Pinterest、Starbucks、VoltAgent 后仍保留 Apple 式浅底模块的情况。
- 保持 Apple 主题基线不动：这次主题修复通过“只给非 Apple 主题补覆盖”的方式完成，避免把默认 Apple 风格带偏。
- 优化工作量表格 / 控制价计算表选择按钮的内边距和图标留白，解决图标紧贴边框、呼吸感不足的问题。
- 优化列映射面板内“字段设置”按钮的位置和字号，下移到更贴近正文的节奏，同时缩小按钮文案视觉重量。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 102 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-20-v3.36.zip`，SHA256：`F3B10BC49158EC78B2A4BD1C78D25F50C24A8541A4C7E486F085F9FD4AA278CE`。

## v3.35 - 2026-06-20

- 前端新增右侧可折叠“AI 复核助手”Dock：展开时常驻显示本次结论、待复核数量、预警状态、风险报告入口、调试信息和“问问本次结果”；收起时只保留窄条入口，避免占用左侧主流程空间。
- 原页面下方“大模型”独立区块迁入右侧 Dock，风险报告、问答、调试信息和大模型设置继续复用原有前端状态与后端接口，不改变请求参数和 API Key 读取方式。
- 表格行级 AI 按钮保持原有行级复核逻辑，并在点击时自动展开右侧 Dock；行级复核的提问框、答案和关闭操作集中到 Dock 内，让左侧表格只保留结果预览和行级入口。
- 本次只调整前端布局、样式、版本号和项目上下文；未修改基价匹配、调整系数规则、经验池预警算法、工作量抓取逻辑和任何后端价格裁决逻辑。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v` 通过，结果为 102 passed、3 skipped。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-20-v3.35.zip`；SHA256：`EB95EBFD7AB9C0F9E58110C6E958622EE67C05118ECF855E4B2372B3C5FDBF9A`。

## v3.34 - 2026-06-19

- 经验池预警新增“只核查指定列有值的行”候选过滤设置，默认开启，当前默认字段为 `数量`；该设置与预警阈值一起保存在 `05-经验池-预警数据/experience-warning-settings-【codex】.json`。
- 预警分析现在会先按当前 sheet 中 `数量` 字段对应的实际列筛候选行，再进入同类匹配、平均值偏离率计算和 Excel 写回；`0`、公式结果为 `0`、空白都按“没有值”处理，不进入预警候选。
- 预警候选过滤继续复用项目统一的合并单元格读取规则：跨行合并视为每一行都有值，跨列合并视为仅第一列有值。
- 若当前参与预警的 sheet 找不到指定过滤字段，后端会返回明确错误，不再静默跳过，也不会误把 `候选数量` 等诊断列当作真实数量列。
- 前端“预警设置”窗口新增“只核查指定列有值的行”开关和“指定列字段”下拉框。
- 新增 / 更新回归测试，覆盖指定列有值进入预警、空白 / `0` 不进入预警、跨行合并继承、设置持久化，以及缺少过滤字段时的明确报错。

## v3.33 - 2026-06-18

- 经验池预警改为“平均值偏离率”口径：对命中的同类记录分别计算 `基价 / 实物工作费调整系数 / 技术工作费调整系数` 的经验池平均值、最小值、最大值，风险等级只看当前值相对平均值的偏离率；经验范围只保留在说明文案里，不再参与触发判定。
- 经验池预警的“找同类”当前收紧为模式 A+B：`字段完全匹配 -> 非空要素顺序匹配 -> 未找到可比选同类记录`；不再沿用模式 C 的“单位一致 + 要素前缀匹配”。
- 新增预警阈值设置接口 `GET /api/experience-warnings/settings`、`POST /api/experience-warnings/settings`，并在前端补充“预警设置”窗口；阈值保存到 `05-经验池-预警数据/experience-warning-settings-【codex】.json`，默认 `低风险 5% / 高风险 20%`。
- Excel 写回、前端预览、报告明细的预警文案同步切换到新口径：会写明平均值、经验范围、实际偏离率、低/高风险阈值、匹配模式、来源项目和复核建议；无预警时也会写明已比对参数、阈值和“未超过阈值，故无预警”。
- 边界规则同步落地：当经验池平均值为 `0` 时，当前值无论为 `0` 还是非 `0`，当前版本都记为 `无预警`；没有可比经验值的参数不参与预警，但会写入说明。
- 新增 / 更新回归测试，覆盖低风险阈值内无预警、低风险、高风险、平均值为 `0` 的两类边界、预警设置持久化与生效、以及 merged-cell 继承规则不被破坏。

## v3.32 - 2026-06-18

- 统一导出文件命名：价格匹配模块输出 `【输出】-控制价计算表-yyyymmdd-hhmm.xlsx` 和 `【输出】-控制价报告-yyyymmdd-hhmm.docx`；原始工作量抓取模块输出 `【临时】-控制价计算表（填好数量后）-yyyymmdd-hhmm.xlsx` 和 `【临时】-原表-(工作量信息抓取后标注符合用)-yyyymmdd-hhmm.xlsx`。
- “导入已有控制价文件到经验池”支持拖入 `.xlsx` 控制价文件；“原始工作量抓取模块”的工作量表格和控制价计算表也支持拖入 `.xlsx`，并保留原有点击选择和自动识别流程。
- 修正经验池预警模块对合并单元格的读取口径：现在预警分析复用主填价的默认规则，跨行合并视为每一行都有该值，跨列合并仍只让第一列有值，避免同一合并块里只有首行能进入预警比选。
- 预警分析新增进度状态接口 `GET /api/experience-warnings/progress/{job_id}`；前端“运行经验池预警分析”时会实时显示进度条，以及“已匹配多少行 / 共多少行”“已找到可比选多少行”“已触发多少条预警”。
- 新增回归测试，覆盖预警分析对纵向合并单元格的继承，以及预警完成后的进度接口返回最终计数。
- 项目上下文补充统一约束：`AGENTS.md`、`README.md` 已明确合并单元格默认口径也适用于经验池预警等结构化读表模块；当前检查到的现有项目规则文件未发现与该默认口径冲突，`merge_vertical_cells` / `merge_horizontal_cells` 仅属于主填价流程的显式开关。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 90 passed、3 skipped；`npm run build` 通过。

## v3.31 - 2026-06-18

- 修正填价结果预览的列顺序：当某个 sheet 按默认列名单或已保存偏好筛出要显示的列后，前端现在会再按真实表头中的原始位置排序，避免预览列顺序跟着偏好名单跑偏。
- 这次改动不改变预览设置的列匹配规则、不改变各 sheet 的勾选偏好，也不改变“未命中默认列时回退显示全部列”的行为。
- 验证：`npm run build` 通过；`python -m pytest backend/tests -v -rs` 通过，结果为 89 passed、3 skipped；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-18-v3.31.zip`，SHA256：`4C3FBDF3CE6CE984C09AFD3680973568F764AF1E12BD5BFAE5F7F027094D30A4`。

## v3.30 - 2026-06-18

- 预览列设置面板中的 sheet 列选择不再只显示一组精选字段，而是直接按当前 sheet 的真实表头列出全部复选框，避免像 `要素1-5` 这类实际存在的列在设置面板里看不到。
- 默认显示列名继续作为自动配置入口：打开不同 sheet 时，会先按上方默认列名单做语义匹配，例如 `单价` 可以自动命中 `单价匹配-测试` 一类真实表头；若默认名单一个都没命中，仍回退为显示该 sheet 的全部列。
- 一旦用户在某个 sheet 里手动勾选或取消列，当前 sheet 会生成独立覆写配置；点击“恢复默认列”后，再回到按默认列名单自动勾选的状态。
- 验证：`npm run build` 通过。

## v3.29 - 2026-06-18

- 列映射设置新增“字段设置”入口，并补充 `GET /api/input/field-preferences`、`POST /api/input/field-preferences`；输入列自动识别会优先使用 `03-知识库-二维数据库制作/input-field-preferences-【codex】.json` 中保存的字段偏好。
- 保存输入字段偏好后，如果当前已选择输入 Excel，会立即重新读取当前映射行和当前 sheet 的表头，应用新的自动识别结果，不影响用户手工修改能力。
- 填价结果预览中，表2 / 表3 / 表4 的默认显示列调整为 `要素1-5、单位、单价、实物工作费调整系数、技术工作费调整系数、预警参数、预警细节`。
- 填价结果预览窗口右上角新增“设置”，支持两层偏好：编辑默认显示列名，以及按不同 sheet 单独保存显示列；预览设置保存在当前浏览器，不改后端数据和输出文件。
- 新增回归测试，覆盖输入字段偏好保存后会影响 `/api/inspect` 自动识别建议。
- 验证：`python -m pytest backend/tests -v -rs` 通过，结果为 89 passed、3 skipped；`npm run build` 通过。

## v3.28 - 2026-06-18

- 原始工作量抓取的“同类记录识别”改为复用 `要素1-5 + 单位` 规则文档中的模式A+B：先 `字段完全匹配`，A 未命中后再用 `非空要素顺序匹配`，暂不直接放宽到前缀匹配。
- 工作量抓取成功与预警日志会写明本次命中的匹配模式；无匹配时明确标注“模式A+B均未命中”，避免把抓取不到和无风险混在一起。
- 前端“原始工作量抓取模块”新增“字段设定”入口，可配置工作量表字段的优先识别关键词；新增 `GET /api/workload-capture/field-preferences` 与 `POST /api/workload-capture/field-preferences`，偏好保存到 `05-经验池-预警数据/workload-field-preferences-【codex】.json`。
- 工作量抓取新增“只抓取某一列有值的行”选项，默认开启，默认按 `数量 / 工程量` 的映射列判断；开启过滤后，空模板行不会进入抓取匹配。
- 新增回归测试，覆盖工作量抓取的模式B命中、字段偏好保存后 inspect 自动识别，以及按 `数量` 过滤空工作量行。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 88 passed、3 skipped；`npm run build` 通过；生成代码版存档 `04-输出版本存档/管勘智算-2026-06-18-v3.28.zip`，SHA256 以 `python tools/archive_code.py --version v3.28 --date 2026-06-18` 的最新输出为准。

## v3.27 - 2026-06-18

- 经验池预警的“找同类”逻辑从原先只按 `要素1-5 + 单位` 单一顺序键比对，升级为四档：`字段完全匹配 -> 非空要素顺序匹配 -> 单位一致 + 要素前缀匹配 -> 未找到可比选同类记录`。
- 新增经验池前缀匹配兜底：当主顺序键未命中，但同单位下 `要素1` 起连续前缀能对上时，允许该行进入经验池比选预警；这样更接近主功能第一层“标准规则命中”的放宽思路。
- 预警摘要新增 `输入候选 / 可比选 / 未找到同类` 统计和匹配模式分布；逐条预警和写回 Excel 的 `预警细节` 也会写明本次命中的匹配模式。
- 经验池预警分析后，后端不再只写“有预警”的行；对每个已比对成功的行都会写回输出 Excel 两列：有预警时写触发参数和复核细节，无预警时写 `预警参数=无预警`，并在 `预警细节` 中写明已比对参数、匹配模式和“比对后未发现预警”。
- 前端预警面板同步展示“未找到同类”数量、各匹配模式命中行数，以及逐条预警的匹配模式，避免把“0 条预警”误读成“所有行都已检查且安全”。
- 填价结果预览中的 `预警参数`、`预警细节` 两列新增图标和悬浮全文：有预警显示预警图标与橙色高亮，无预警显示通过图标与绿色高亮；悬浮时统一展示 `预警参数 + 预警细节` 全文。
- 新增核心规则子文档 `03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】要素1-5和单位的匹配模式介绍.md`，把 `要素1-5 + 单位` 的四档匹配模式、当前单价匹配所处层级、经验池预警与工作量抓取的复用边界单独沉淀，并加入 `AGENTS.md`、`README.md` 和总体规则文档入口。
- 新增回归测试，覆盖字段完全匹配文案、预警细节中的匹配模式、`单位一致 + 要素前缀匹配` 的放宽命中路径，以及“无预警”行写回 Excel / API 预览。
- 验证：`python -m pytest backend/tests/test_experience_warning.py backend/tests/test_api.py -v` 通过，结果为 42 passed、2 skipped；`npm run build` 通过。

## v3.26 - 2026-06-18

- 新生成 `05-经验池-预警数据/【经验池】-管勘智算-【codex】.xlsx` 时，不再手工创建空白工作簿，而是以 `05-经验池-预警数据/【经验池】【模板勿动】-管勘智算.xlsx` 为母版创建。
- 保留模板表头颜色、列宽、冻结窗格；如果模板第 2 行有数据行样式，导入写入的数据行会沿用该行样式，已存在的旧经验池导入时也会重新套用模板样式，避免颜色和边框被写乱。
- 经验池导入默认按 `工程量 / 数量` 过滤时，如果连续 200 行过滤字段为空，会提前停止扫描，避免 Excel 模板空白格式行把 `sheet.max_row` 撑得很大后导致导入卡死。
- 新增回归测试，覆盖按模板样式生成经验池，以及大量连续空模板行会提前停止扫描。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 81 passed、3 skipped；`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-18-v3.26.zip`；SHA256：`CDC108184EA7A7CB3FFC0CAE1EEEA352CA2379F23D74F05731F7311A454B9EF4`。

## v3.25 - 2026-06-18

- “导入已有控制价文件到经验池”模块新增“只导入某一列有值的行”选项，默认开启。
- 过滤字段默认选择 `工程量 / 数量`，即使用经验池字段 `工程量` 的映射列判断当前行是否有内容。
- 后端 `POST /api/experience-pool/import` 新增 `only_import_rows_with_value` 和 `value_filter_field` 参数；开启过滤时，映射字段为空的行会跳过，不写入经验池。
- 前端导入校验新增过滤字段检查：如果开启过滤但当前 sheet 未映射该字段，会提示先完成列选择。
- 新增回归测试，覆盖工程量为空的模板行不导入、工程量为 0 但有值的行仍导入。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 78 passed、3 skipped；`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-18-v3.25.zip`；SHA256：`9C60EBCAB0CF32A29D2C59E5908C7A603D518EE0E53E920BC8F4BA89079166E8`。

## v3.24 - 2026-06-18

- “导入已有控制价文件到经验池”模块右上角新增“字段设置”入口，可配置各经验池字段的优先识别关键词。
- 新增 `GET /api/experience-pool/field-preferences` 与 `POST /api/experience-pool/field-preferences`，字段偏好保存到 `05-经验池-预警数据/experience-field-preferences-【codex】.json`。
- 经验池 inspect 自动列映射改为先读取用户字段偏好；自定义关键词优先，内置默认规则兜底，避免破坏旧控制价文件导入流程。
- 字段设置保存后，如果当前已选择控制价文件，会重新读取表头并应用新识别规则。
- 新增回归测试，覆盖保存字段偏好后经验池 inspect 优先按偏好识别 `要素1`、`单位`、`基价` 和两个调整系数列。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 77 passed、3 skipped；`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-18-v3.24.zip`；SHA256：`78B32F0FCAAA005617A04E1CD689C5A223266B9B3A7A9EB6FF9CFCDD9E893295`。

## v3.23 - 2026-06-18

- 经验池预警分析改为手动按钮执行，初次 `/api/process` 不再自动分析经验池，也不再自动写入预警列，用于缩短首次识别等待时间。
- 新增 `POST /api/experience-warnings/run`：根据转换任务 `job_id` 查找输出 Excel，点击后执行经验池比选、写入 `预警参数` / `预警细节`，刷新表格预览，并重新生成带预警段落的报告。
- 前端预览区新增“运行经验池预警分析 / 重新运行经验池预警分析”按钮；未执行时显示提示，执行后展示原有预警摘要、分类和明细。
- 转换进度阶段移除“经验池预警分析中”，收尾提示改为“经验池预警可在转换完成后手动运行”。
- 新增回归测试，覆盖初次转换不写预警列、点击新接口后写入预警列并刷新预览。
- 验证：`python -m pytest backend/tests -v` 通过，结果为 76 passed、3 skipped；`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-18-v3.23.zip`；SHA256：`C2F4C0DD6903FA922651AA3FA30F3D2BBCC4EA5D030F441D4C6EAE4C4AE3E088`。

## v3.22 - 2026-06-18

- 右上角“页面设置”恢复独立的 Claude 字体开关，可叠加 Apple、Claude、Pinterest、Starbucks、VoltAgent 任意主题。
- 主题选择继续负责颜色、圆角、按钮和卡片气质；Claude 字体开关只覆盖整页 `--font-body` 和 `--font-display`，正文、表格、控件和标题都会跟随切换。
- 保留旧的 `guankanzhisuan-use-claude-font` 浏览器偏好键，刷新页面后仍记住是否启用 Claude 字体。
- 验证：`npm run build` 通过，`python -m pytest backend/tests -v -rs` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-18-v3.22.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.21 - 2026-06-18

- 右上角“页面设置”新增主题切换，支持 Apple、Claude、Pinterest、Starbucks、VoltAgent 五种主题。
- Apple 为当前默认风格，不增加覆盖样式，确保原页面视觉保持不变。
- Claude、Pinterest、Starbucks、VoltAgent 主题基于 `awesome-design-md` 的本地品牌参考抽取颜色、字体、圆角、按钮和卡片气质，并通过 `.shell.theme-*` CSS token 覆盖实现。
- 旧的 Claude 字体开关偏好会自动迁移为 Claude 主题，新的主题偏好写入浏览器 localStorage。
- 验证：`npm run build` 通过，`python -m pytest backend/tests -v -rs` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-18-v3.21.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.20 - 2026-06-17

- 转换进度改为阶段式提示：读取输入、结构化匹配、Excel 公式重算、经验池预警分析、生成报告与预览会按进度依次显示。
- 前端进度条不再停在 92% 等待后端返回；后处理阶段会继续缓慢推进到约 98%，减少“卡住失败”的误判。
- 保留 Excel 公式重算逻辑，明确显示“Excel 公式重算中”，用于确保费用汇总、公式缓存和 Word 报告金额与输出表一致。
- 阶段提示增加桌面端和移动端样式，窄屏下阶段标签自动改为两列。
- 验证：`npm run build` 通过，`python -m pytest backend/tests -v` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.20.zip`；SHA256：`155B5493A17ED955C69C65ED9E5EFC54C7F6993B2A08E405B416586D28D84255`。

## v3.19 - 2026-06-17

- 优化经验池预警输出链路：转换完成后先基于经验池分析输出结果，再把预警逐行回写到输出 Excel。
- 输出 Excel 在各 sheet 映射行追加或复用 `预警参数`、`预警细节` 两列；同一行多个预警参数合并写入，细节写明当前值、经验范围、历史项目来源文件/sheet/行号和值，以及复核建议。
- 前端表格预览同步显示 `预警参数` 和 `预警细节`，预警单元格使用醒目底色，长细节保留悬浮全文。
- 修复运行时追加列后预览刷新仍按旧列宽截断的问题：刷新预览时继续顺序读取，并只向右扩展少量列以覆盖新增预警列。
- 新增回归测试，覆盖预警列回写、API 下载 Excel 和前端预览数据同时包含预警两列。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.19.zip`；SHA256：`1D19EE40BF2C6EC9C718611403B38B1510DB7E416B6AE0172E1C6EED0DB4872A`。

## v3.18 - 2026-06-17

- 新增“原始工作量抓取模块”：页面底部可分别上传委托方工作量表和控制价计算表，自动读取 sheet、映射行和字段列，并允许人工调整列映射。
- 新增后端 `workload_capture` 独立模块和接口：`POST /api/workload-capture/inspect`、`POST /api/workload-capture/run`、`GET /api/workload-capture/download/{job_id}/{kind}`。
- 匹配规则复用现有 `要素1-5 + 单位` 归一化键，允许空要素、顺序差异、全角半角和比例/级别前缀差异；只有一对一确定匹配才写入。
- 控制价计算表写入 `数量(信息抓取)`、`实物工作费调整系数(信息抓取)`、`技术工作费调整系数(信息抓取)`、`委托方备注(信息抓取)` 和 `抓取日志`；匹配成功标绿，一对多、无匹配或风险行标红。
- 工作量表输出新增 `抓取日志`，有数值且匹配成功的源行标绿，有数值但未成功写入的源行标红。
- 新增回归测试，覆盖工作量抓取一对一填充、一对多预警、列映射建议、inspect API、run API 和下载结果。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.18.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## 未发布小改 - 2026-06-17

- 新增参赛汇报配图素材目录：`07-汇报PPT 和 演示素材/20260617-参赛汇报配图-【codex】`，包含封面、痛点、流程闭环、三层架构、规则引擎、经验池预警、AI 复核、技术架构升级、4A/推广路线等 9 组配图，每组 A/B 两版、AB 合图和 PPT 16:9 画布版。
- 本次为 PPT 视觉素材新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。
- 新增参赛汇报 PPT 文案：`07-汇报PPT 和 演示素材/20260617-管勘智算2.0参赛汇报PPT文案-【codex】.md`，用于基于当前 v3.17 技术架构制作人工智能创新大赛项目介绍 PPT。
- 本次为汇报资料新增，不涉及代码、规则引擎或运行逻辑，未提升版本号，未生成代码存档。
- Word 报告默认模板切换为 `03-知识库-二维数据库制作/01-报告模板-招标控制价报告模板/【模板勿动】控制价报告模板-yyyy-mm-dd.docx`。
- 新增回归测试锁定当前模板路径，避免后续误改回旧目录。
- 本次为模板路径小改，未提升版本号，未生成代码存档。

## v3.17 - 2026-06-17

- 经验池导入显式使用 `05-经验池-预警数据/【经验池】【模板勿动】-管勘智算.xlsx` 作为模板来源，读取首行字段作为落库结构。
- 导入函数只读模板、不写模板；新增回归测试确认模板文件不会被修改。
- 经验池行写入改为按模板列名组装，模板未来新增扩展列时，导入结果会保留列结构，未知字段为空。
- FastAPI 导入接口默认传入经验池模板路径；经验池仍只用于预警，不参与识别链和第二层经验提示。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.17.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.16 - 2026-06-17

- 经验池默认位置独立为 `05-经验池-预警数据/【经验池】-管勘智算-【codex】.xlsx`；历史旧池仍可作为预警读取兜底，新导入不再写入正式知识库目录。
- 新增 `POST /api/experience-pool/inspect`，导入前可读取所有 sheet、映射行、列选项和建议映射。
- 经验池导入支持按 sheet 选择、按映射行重读表头、按字段手工选择列，适配不规整控制价文件。
- 新经验池模板保留 `要素1-5` 与对应原表列名、单位、基价、工程量、实物/技术工作费调整系数、两个扩展参数、三列原表备注、来源文件/sheet/行、导入时间、批次和数据签名。
- 前端底部模块改为“独立经验池导入与预警数据”，新增经验池列选择窗口；经验池仍只用于预警比选，不参与识别链和第二层经验提示。
- 修复无 Word 模板时报告 fallback 未输出“五、其他需要注意的事项”标题的问题。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.16.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.15 - 2026-06-17

- 增强预警模块内容：后端预警结果新增 `summary_text`、按指标分类统计、风险等级中文名、行关键要素、经验值范围、可比选经验值文本和复核建议。
- Word/Markdown 处理报告新增“经验池预警”段落，写入预警摘要、重点预警明细、经验值比选和“仅用于预警、不参与识别”的边界说明。
- 前端预警面板新增摘要文字、基价/实物/技术分类统计、风险等级、行关键要素、复核建议、来源行展示，并支持查看全部/收起预警。
- 新增回归测试，覆盖预警摘要生成、重点预警报告行、报告 Markdown 中包含经验池预警段落。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.15.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.14 - 2026-06-17

- 新增独立经验池第一版：`POST /api/experience-pool/import` 可从已有控制价 Excel 的表2/表3/表4经验列中导入基价、实物工作费调整系数、技术工作费调整系数经验值。
- 经验池默认写入 `03-知识库-二维数据库制作/【数据库】【经验池】-管勘智算-【codex】.xlsx`，不改动正式知识库 `【数据库】【导入】.xlsx`，也不参与第二层经验提示层。
- 新增预警分析：转换完成后如存在经验池，后端会基于输出 Excel 与同类经验值比选，返回高风险/中风险预警、经验值列表、中位数和来源行。
- 前端在填价结果预览区显示经验池预警摘要和前 6 条预警明细；页面最底部新增“知识库扩充”模块，可选择控制价文件并勾选导入 `基价 / 实物工作费调整系数 / 技术工作费调整系数`。
- 新增回归测试，覆盖经验池独立落盘、同一行多个经验值比选、经验池不喂给第二层匹配、经验池导入接口临时路径。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.14.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.13 - 2026-06-17

- 大模型默认接入切换为官方 DeepSeek：`provider=deepseek`、`model=deepseek-v4-flash`、`base_url=https://api.deepseek.com`；后端兼容 OpenAI 风格 `/chat/completions` 地址拼接。
- 后端 API Key 读取新增 `DEEPSEEK_API_KEY`，并保留 `SILICONFLOW_API_KEY` 兼容旧硅基流动配置。
- 前端“大模型设置”改为模型预设选择，可在官方 DeepSeek 和硅基流动兼容模式之间切换；首页大模型卡片不再显示供应商和模型技术名。
- 填价结果预览表格的 AI 列改为右侧固定列，横向滚动时仍保持可见。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过；官方 DeepSeek 极短问答实测返回正常；本地后端健康检查返回 `v3.13`，前端 `5174` 返回 200。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.13.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.12 - 2026-06-17

- 修复上传项目例子后转换卡在 92% 的问题。根因为 v3.11 在公式重算后刷新预览时，对 `read_only=True` 工作表使用 `sheet.cell(row, column)` 逐格随机读取，openpyxl 会反复扫描工作表 XML，导致预览刷新耗时约 117 秒。
- 预览刷新改为按原预览列宽使用 `iter_rows(..., values_only=True)` 顺序读取，避免扫描无关列和随机访问。
- 新增回归测试，确保公式重算后的预览刷新不再使用逐格随机读取。
- 验证：项目例子 v2.3 通过 `/api/inspect` + `/api/process` 接口链路处理，`/api/process` 约 3.9 秒返回；`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-17-v3.12.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.11 - 2026-06-16

- 前端行级 AI 复核改为手动发送：点击行末 AI 图标只打开复核面板，用户点击“询问”后才调用大模型。
- 大模型调试信息改为保留最近 10 次请求，并在后端为每次风险报告、行级 AI 或普通问答生成可打开的提示词 Markdown 文件；返回的调试 JSON 包含 `prompt_markdown` 路径。
- 填价结果预览在输出 Excel 公式重算后重新读取 `data_only` 预览数据，公式列优先显示数值；如仍显示公式，前端最多显示 15 字并通过悬停查看全文。
- 风险报告 prompt 改为项目常规风险和造价风险摘要，不再把逐行匹配明细发送给大模型。
- Word 报告“五、其他需要注意的事项”中的“管勘智算匹配报告”不再输出逐行价格识别日志，只保留处理概况、三层命中统计和少量待复核提示。
- 大模型区域窗口标题、图标和字号统一为当前系统窗口样式。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.11.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.10 - 2026-06-16

- 后端大模型配置新增 `.env.local` 兜底读取 `SILICONFLOW_API_KEY`，解决后端进程未继承环境变量时行级 AI/风险报告调用失败的问题。
- 生成填价结果后在 Windows + Microsoft Excel 可用时自动重算并保存公式缓存，Word 报告金额改为读取 `费用汇总` 的 `合计（不含税）` 和 `增值税`，避免从旧输入缓存读到错误金额。
- Word 报告改为替换模板中的 `【匹配报告】` 段落，并按公文格式设置新增匹配报告段落：标题 14 号黑体加粗，正文 14 号宋体、28 磅行距。
- 前端预览规则调整：表2/表3/表4继续显示关键匹配列，其余 sheet 显示所有列；匹配说明最多显示 15 字，鼠标悬停查看全文。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过；用用户下载的输出 Excel 重算后验证报告金额为 `1720.5956` 万元、增值税为 `103.2357` 万元。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.10.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.9 - 2026-06-16

- Word 报告改为使用模板生成：自动替换 `yyyy-mm-dd`、`【yyyy】年【mm】月【dd】日`、`【项目名称】`、费用汇总合计和增值税占位符。
- “其他需要注意的事项”新增三层匹配摘要：输入行数、基价/单价匹配行数、实物/技术工作费调整系数第一层命中、第二层经验和待复核数量。
- 风险报告 prompt 调整为只做复核风险提示，重点关注待复核、候选冲突、第二层经验提示和费用汇总异常，不裁决最终数字。
- 前端新增转换 0-100% 动效、关键列宽表格预览、底部报告摘要和行级 AI 复核按钮。
- 填价结果预览只展示 `要素1/2/4/5`、`单位`、`基价`、两个调整系数和逐行匹配报告列；匹配到的数字统一用浅绿色底和绿色字展示。
- 修复价格列为公式时误判为“已有价格”的问题：公式占位列现在按待填项重新执行知识库匹配。
- 验证：`python -m pytest backend/tests -v` 通过，`npm run build` 通过。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.9.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.8 - 2026-06-16

- 默认知识库固定为 `03-知识库-二维数据库制作/【数据库】【导入】.xlsx`；A-H 用于基价第一层匹配，J-M 用于实物/技术工作费调整系数第二层经验提示。
- 三个数字统一命中颜色：第一层命中为绿色，第二层经验命中为黄色，未命中为红色。
- 实物工作费调整系数第一层规则暂时清零：`physical_factor_rules` 和 `physical_factor_overrides` 的 CSV/XLSX 均保留表头，当前未命中第二层经验时输出 `待复核` 并标红。
- 新增重要规则说明文件：`03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】项目以及总体匹配规则介绍.md`。
- 将重要规则说明文件加入代码存档白名单，继续排除知识库 Excel、项目例子 Excel、PPT 和临时输出资料。
- 更新回归测试，覆盖调整系数绿黄红着色、实物第一层暂未启用、技术第一层不被第二层经验覆盖、重要规则说明进入代码存档。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.8.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.7 - 2026-06-16

- 第二层经验提示层来源切换为项目例子 `03-知识库-二维数据库制作/【项目例子】【测试输入】铜梁江津-遵义-贵阳-可行性研究勘察测量控制价 -v2.3【批注】.xlsx`。
- 重新生成 `03-知识库-二维数据库制作/【数据库】【导入-编辑】-管勘智算数据库-v2.0-【母表】-已填经验数.xlsx`；生成结果为 `filled_rows=257`、`unmatched_rows=0`。
- 抽查 J-M 经验列，未发现 `na/NA` 残留；实物经验数非空 254 行，技术经验数非空 253 行。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.7.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.6 - 2026-06-16

- 新增第二层经验提示层：从项目例子表2、表3、表4抽取实物工作费调整系数、技术工作费调整系数经验数及 DWB 批注，写入单价知识库母表 J-M 列。
- 新增脚本 `tools/build_experience_database.py`，生成 `03-知识库-二维数据库制作/【数据库】【导入-编辑】-管勘智算数据库-v2.0-【母表】-已填经验数.xlsx`；本次生成结果为 `filled_rows=257`、`unmatched_rows=0`。
- 后端默认知识库切换为已填经验数母表；基价仍按 `要素1~要素5 + 单位` 匹配，经验字段只在第一层调整系数规则未命中时作为第二层兜底。
- 第二层经验数写入实物/技术工作费调整系数单元格时标为黄色，并在逐行匹配报告中写明“第二层经验提示层”和经验说明。
- 增加回归测试，覆盖知识库读取经验字段、第二层经验数回填、第一层标准规则不被经验数覆盖。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.6.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.5 - 2026-06-16

- 修复技术工作费调整系数类别误判：当行内说明列包含 `"简单","0.27";"中等","0.30";"复杂","0.33"` 等规则文本时，不再把说明中的 `简单` 误当作当前行技术类别。
- 后端自动识别 `要素-技术`、`要素技术`、`技术类别`、`技术工作类别` 列；技术费规则中的 `甲级/乙级/丙级/简单/中等/复杂/物探/试验` 优先在这些技术上下文字段中匹配。
- 项目例子表4第90行 `水文地质勘察 / 利用钻孔抽水` 已回归验证：O 列为 `复杂` 时，`输出-技术工作费调整系数` 写入 `0.33`。
- 增加回归测试 `test_technical_fee_uses_project_example_technical_column_for_hydro_row90`。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.5.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.4 - 2026-06-16

- 将 `第一层-标准规则命中-技术工作费调整系数.xlsx` 中的确定性规则固化到 `backend/app/rules/technical_fee_rules.xlsx` 和 `technical_fee_rules.csv`，旧技术费规则表内容被替换。
- 技术工作费调整系数新增第一层特殊规则：表2 `线路航测=0`、`走向图编制=0`，表4 `勘探点测放=0`，工程物探 `高密度电法经验=1.0`。
- 技术费规则表新增可选字段 `priority`；同属第一层规则时，`priority=高` 的规则优先于普通规则，同优先级仍按规则表顺序匹配。
- 更新回归测试，覆盖第一层技术费规则、勘探点测放自动填 0、高优先级规则胜出，以及项目例子表 AC 列空单价匹配。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-16-v3.4.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.3 - 2026-06-15

- 调整系数规则库新增同名 Excel 维护表：`physical_factor_rules.xlsx`、`physical_factor_overrides.xlsx`、`technical_fee_rules.xlsx`。
- 后端规则加载改为 Excel 优先；如果 Excel 文件不存在、字段缺失或读取失败，自动回退同名 CSV。
- 保留 CSV 兼容能力，字段结构一致时 Excel 与 CSV 匹配结果保持一致。
- 增加回归测试，覆盖 Excel 规则表与 CSV 字段/规则一致、Excel 优先读取、Excel 损坏时 CSV 回退。
- 同步更新 `三个数字匹配规则说明-【codex】.md`、`README.md`、`AGENTS.md` 和 `TASKS.md` 中的规则维护入口说明。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-15-v3.3.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.2 - 2026-06-14

- 根据重新转换的 `计价格[2002]10号-工程勘察设计收费标准使用手册` Markdown 校准实物工作费附加调整系数规则。
- 新增通用气温附加调整系数规则：气温大于等于 35 摄氏度或小于等于零下 10 摄氏度时，实物工作费调整系数为 `1.2`。
- 新增通用海拔高程附加调整系数规则：`2000~3000m=1.1`、`3001~3500m=1.2`、`3501~4000m=1.3`；4001m 以上仍需人工协商，不自动填。
- 新增表2工程测量 `数字化测绘 / 数字化地形图` 实物工作费调整系数 `1.5`。
- 根据重新转换的 `财建[2009]17号-测绘生产成本费用定额` Markdown，将表2航线网布点核减15%、室内外综合判调核减30%、DLG增加20%与带状15%叠加规则从“样表经验”升级为“标准明确”。
- 增加调整系数回归测试，覆盖表2数字化测绘、2009定额来源说明、气温与海拔组合公式。
- README 和项目 AGENTS 明确列出五个关键规则资产，固化“识别规则与程序执行器分离维护”的项目约定。
- 新增 `TASKS.md` 作为多线程协作总控文件，明确方案规划、后端、前端三个线程的职责、启动必读文件、当前任务和交接模板。
- 代码存档脚本新增 `TASKS.md` 白名单，确保恢复版本时保留多线程任务看板。
- 按当前实际协作方式优化 `TASKS.md`：长期线程收窄为方案规划线程、后端线程、前端线程；规则知识库职责并入方案规划/后端，验收发布并入各线程收尾动作。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-14-v3.2.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.1 - 2026-06-14

- 逐行匹配报告新增两列：`匹配说明-实物工作费调整系数`、`匹配说明-技术工作费调整系数`。
- 系数报告会写入命中的规则编号、规则说明、来源依据、输出系数；多个实物附加因素会写出 `1 + Σ(k-1)` 的组合计算过程。
- 技术工作费无法自动判断时，报告列写入 `待复核` 原因和命中的待复核规则，便于人工追溯。
- 兼容旧输出表：如果已经存在原三列匹配报告，新版会追加缺少的两列。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-14-v3.1.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v3.0 - 2026-06-14

- 新增调整系数规则引擎 `backend/app/adjustment_rules.py`，不改动原 `要素1~5 + 单位 -> 基价` 匹配逻辑。
- 新增规则库文件：`physical_factor_rules.csv`、`physical_factor_overrides.csv`、`technical_fee_rules.csv`、`manual_review_rules.csv`。
- `输出-实物工作费调整系数` 不再固定写 `1.5`，改为按 sheet、备注、业务关键词命中标准规则或样表规则；多个标准附加因素按 `1 + Σ(k-1)` 组合。
- `输出-技术工作费调整系数` 不再固定写 `0.22`，改为按表2工程测量、表3甲乙丙、表4岩土/水文/物探/试验等分支匹配；特殊不确定行输出 `待复核`。
- 增加调整系数规则回归测试，覆盖表2默认工程测量、表3带状地质测绘、表4泥浆护壁与夜间作业组合、勘探点测放待复核。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-14-v3.0.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.22 - 2026-06-14

- 大模型调试窗口提升为模块级折叠面板，不再只放在问答窗口内。
- 风险报告接口返回不含 API Key 的调试信息，包括 provider、model、base_url、temperature、max_tokens 和实际 messages。
- 风险报告和问答测试都会刷新同一个“本次发送给大模型的内容”调试面板。
- 增加风险报告调试信息回归测试，确认可查看 Word Markdown 和表格预览 prompt，且不暴露后端环境变量密钥。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-14-v2.22.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.21 - 2026-06-13

- 大模型问答接口返回不含 API Key 的调试信息，包括 provider、model、base_url、temperature、max_tokens 和 messages。
- 大模型问答测试窗口新增默认折叠的“调试信息：本次发送给大模型的内容”面板。
- 增加问答接口调试信息回归测试，确认不暴露后端环境变量密钥。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.21.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.20 - 2026-06-13

- 修复 Claude 风格字体只影响主标题的问题；开启后 `.shell` 整体重新声明字体族，正文、表格、控件都会继承切换后的字体。
- 各级局部标题、统计数字、弹窗标题和面板标题统一挂接到 `--font-display`，使 Claude 风格在页面下方区域也可见。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.20.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.19 - 2026-06-13

- 主页面右上角新增页面设置按钮，打开后可勾选“Claude 风格字体”。
- Claude 风格字体模式使用更温和的衬线标题和清爽正文，偏好写入浏览器 localStorage，刷新后保留。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.19.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.18 - 2026-06-13

- 匹配知识库前统一归一化要素和单位：忽略所有空白、单元格内换行、常见不可见分隔符，并继续不区分全角半角符号。
- 填价结果预览中，同行 `匹配状态` 为 `已匹配` 的价格单元格显示为绿色字体。
- 增加空白、换行、不可见字符和全角半角符号兼容匹配的回归测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.18.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.17 - 2026-06-13

- 填价结果预览的 sheet 区改为明确的“Sheet 切换”控件，当前 sheet 使用主题蓝高亮。
- Sheet 按钮改为 `Sheet：名称` 形式；无名称 sheet 显示为 `Sheet 1`，不再显示“未命名”。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.17.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.16 - 2026-06-13

- 主页标题调整为主题色大号“管勘智算”，副标题“长输管道勘察测量最高投标限价智能体”放大显示，删除“把空单价 Excel 放进来”。
- 列映射设置中未识别的关键字段标红，并显示“未识别”。
- 填价结果预览始终显示 sheet 标签；后端预览范围改为输出工作簿所有 sheet，便于直接切换查看。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.16.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.15 - 2026-06-13

- 填价结果预览增加 sheet 切换，后端返回所有启用处理 sheet 的预览数据。
- 每个预览 sheet 显示前 50 行数据。
- 前端预览表格中每个单元格最多显示 10 个字符，完整值保留在悬浮提示中。
- 增加多 sheet 预览和 50 行预览的回归测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.15.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.14 - 2026-06-13

- 输出 Excel 中，成功匹配并回填的价格单元格使用浅绿色底色和深绿色字体标识。
- 输出 Excel 中，未匹配而写入 `待复核` 的价格单元格使用浅红色底色和深红色字体标识。
- 增加价格输出单元格颜色的回归测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.14.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.13 - 2026-06-13

- 默认知识库改为 `03-知识库-二维数据库制作/【数据库】【导入】.xlsx`。
- 知识库读取限定为第一个 sheet 的 A 到 H 列，A-H 表头为 `序号`、`要素1`、`要素2`、`要素3`、`要素4`、`要素5`、`单位`、`基价`。
- 增加回归测试，防止 H 列之后的辅助列或重复表头覆盖 `基价`。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.13.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.12 - 2026-06-13

- 列映射设置中新增两个默认勾选项：纵向合并单元格按每一行继承同一值；横向合并单元格只让第一列继承值，其他列按空值。
- 后端 `/api/process` 增加 `merge_vertical_cells` 和 `merge_horizontal_cells` 表单参数。
- 合并单元格读取规则更新：跨行跨列合并区域仅第一列的每一行继承合并值，其余列按空值参与匹配。
- 增加横向、纵向关闭、跨行跨列合并单元格的回归测试，并增加 API 参数传递测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.12.zip`；SHA256 以 `python tools/archive_code.py` 输出为准。

## v2.11 - 2026-06-13

- 匹配读取改为使用 `data_only=True` 的 Excel 缓存值，避免公式单元格按公式字符串参与匹配。
- 修复项目例子表 `表4-通用工程勘察费用` 的 `F51`：公式 `=F50` 的缓存值 `Ⅵ` 可用于匹配。
- 输入表单元格内容为 `成图比例` 时视为空值参与匹配。
- 增加公式缓存值匹配和 `成图比例` 空值化的回归测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.11.zip`；存档不包含本地 `.env.local`。

## v2.10 - 2026-06-13

- `/api/inspect` 增加候选 sheet 识别，sheet 名包含 `表2`、`表3`、`表4` 时默认加入候选列表。
- 列映射支持按候选 sheet 独立配置，并可勾选是否逐行匹配该 sheet。
- `要素2` 至 `要素5` 未识别到列时默认填入 `空元素列`。
- `价格列` 前端改名为 `输出-价格列`，映射行中 `单价匹配-测试` 优先作为默认输出价格列。
- 映射行中存在 `匹配报告预留位置` 时，匹配报告优先写入该列之后。
- 增加 `输出-实物工作费调整系数` 与 `输出-技术工作费调整系数`，暂按固定值 `1.5` 和 `0.22` 写入。
- 大模型问答窗口默认折叠，发送按钮缩小。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.10.zip`；存档不包含本地 `.env.local`。

## v2.09 - 2026-06-13

- 修复忽略行统计口径：`要素1` 为空、`小计`、`合计` 的行不计入 `输入行数` 和 `report_text`。
- 项目例子表选择 P 列 `基准测试` 作为价格列时，统计为 `输入44行，匹配44行。`
- 增加忽略行不参与统计的回归测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.09.zip`；存档不包含本地 `.env.local`。

## v2.08 - 2026-06-13

- 大模型模块新增问答测试窗口，可输入问题并直接查看模型输出。
- 后端新增 `/api/llm-chat`，复用 `SILICONFLOW_API_KEY` 后端环境变量，不接收前端 API Key。
- 增加大模型问答接口成功返回与空问题拦截测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.08.zip`；存档不包含本地 `.env.local`。

## v2.07 - 2026-06-13

- 新增忽略行规则：`要素1` 为空、`小计` 或 `合计` 时直接跳过，不匹配、不填价、不写逐行匹配报告、不写价格识别日志。
- 新增大模型风险报告后端接口 `/api/risk-report`，默认按硅基流动 OpenAI 兼容 Chat Completions 接口调用。
- 大模型默认配置为硅基流动、`deepseek-ai/DeepSeek-V4-Flash`、`https://api.siliconflow.cn/v1/chat/completions`，API Key 改为由后端环境变量 `SILICONFLOW_API_KEY` 读取。
- 前端预览下方新增“大模型 / 风险报告”模块，支持设置接入方式、模型和接口地址；前端不保存、不上传 API Key。
- 一键启动器支持读取项目根目录 `.env.local` 并传入后端进程；代码存档测试明确排除 `.env.local`。
- 点击“输出风险报告”后，后端读取 Markdown 报告和部分输出表格内容，调用大模型生成风险提示，写入 Word 报告“五、其他需要注意的事项”，并返回页面预览。
- 增加忽略行和风险报告接口基础测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.07.zip`；存档不包含本地 `.env.local`。

## v2.06 - 2026-06-13

- 匹配规则升级为“字段完全匹配优先，非空要素顺序串匹配兜底”，`要素1` 至 `要素5` 的空值位置变化不再阻断匹配。
- 级别与比例尺支持前缀兼容：`Ⅰ类` 可匹配 `级别-Ⅰ类`，`1:500` 可匹配 `比例-1:500`。
- 匹配失败说明增强，可提示单位未匹配或从第几个要素开始没有匹配。
- 输出表逐行匹配报告改为 3 列：`匹配状态`、`候选数量`、`匹配说明`，默认从 R 列开始，若被占用则整体往后顺延。
- 前端“列映射设置”增加“输出逐行匹配报告”复选框，默认开启。
- Word 报告先生成同名 Markdown 源文件，再基于 `控制价报告模板-yyyy-mm-dd.docx` 生成 docx，并在“五、其他需要注意的事项”写入价格识别日志。
- 增加顺序串匹配、级别前缀、报告列起始位置、报告关闭和模板报告写入的测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.06.zip`。
- 代码存档规则调整为“每个版本完成时都备份一次”，不再隔版本备份。

## v2.05 - 2026-06-13

- 列映射中 `要素1` 至 `要素5` 支持选择 `空元素列`，后端将该要素按空字符串参与结构化匹配。
- 前端列映射下拉框为要素字段增加 `空元素列` 选项；`单位` 和 `价格列` 仍必须选择实际列。
- 后端读取输入数据时支持合并单元格取值，合并区域内非左上角单元格会沿用左上角值参与匹配。
- 增加 `空元素列` 和合并单元格继承取值的后端测试。

## v2.04 - 2026-06-13

- `/api/inspect` 默认扫描第一个 sheet 前 4 行，自动选择包含 `要素1` 的行作为映射行；未找到时回退第 1 行。
- `/api/inspect` 返回 `header_row` 和带列号的 `columns`，默认映射值改为 Excel 列号，兼容重复表头和乱序列。
- `/api/process` 支持 `header_row`，按指定映射行读取表头，并从映射行下一行开始处理数据。
- 前端列映射设置增加“映射行”数字输入和“读取该行”按钮；列下拉菜单显示 `A列 - 表头`。
- 增加映射行不在第一行时的后端引擎测试和 API 闭环测试。
- 生成代码版存档：`04-输出版本存档/管勘智算-2026-06-13-v2.04.zip`。

## v2.03 - 2026-06-13

- 后端新增 `/api/inspect`，上传 Excel 后读取第一行表头并返回默认列映射建议。
- 后端 `/api/process` 支持 `column_mapping`，可按用户指定列读取 `要素1` 至 `要素5`、`单位` 和价格列，输入列顺序不再要求固定。
- 输出 Excel 新增 `匹配状态`、`复核原因`、`候选数量`、`匹配说明`，用于说明已匹配、原有价格保留或待复核原因。
- 前端上传文件后自动读取表头，显示列映射下拉框，默认预选标准表头并允许用户手动修正。
- 增加乱序列输入表的后端引擎测试和 API 闭环测试，标准 100 行答案表验收继续通过。

## v2.02-archive-script - 2026-06-12

- 新增 `tools/archive_code.py`，用于生成代码版版本存档。
- 新增归档脚本测试 `backend/tests/test_archive_code.py`，防止资料目录、依赖目录、构建产物和运行临时输出进入代码包。
- 更新 `README.md` 和 `AGENTS.md`，明确版本存档优先运行脚本做代码存档。

## v2.02-code-archive - 2026-06-12

- 按当前 `v2.02` 项目状态重新生成代码版可恢复压缩包。
- 存档文件：`04-输出版本存档/管勘智算-2026-06-12-v2.02.zip`。
- 存档只包含代码、启动脚本、项目上下文和开发文档。
- 存档排除 `00-比赛要求与目标`、`01-weagent部分成果`、`03-知识库-二维数据库制作`、`07-汇报PPT 和 演示素材` 等资料目录，以及运行缓存、前端依赖目录、构建产物、运行临时输出和版本存档目录自身。

## v2.02-launcher - 2026-06-12

- 增加项目根目录一键启动脚本 `启动管勘智算-【codex】.bat`。
- 增加桌面快捷方式 `管勘智算-启动`，用于启动后端、前端并自动打开网页。
- 增加启动器脚本 `管勘智算启动器-【codex】.ps1`，统一处理服务检测、端口占用提醒和网页打开。
- 增加项目根目录状态检查脚本 `检查管勘智算状态-【codex】.bat` 与桌面快捷方式 `管勘智算-状态检查`。
- 固定使用后端 `8000`、前端 `5174` 端口；端口被非本程序占用时只提醒 PID、进程名和命令行，不自动换端口。
- 更新 `README.md` 运行方式，补充一键启动说明和手动排障命令。
- 应用接口版本暂保持 `v2.02`，本次不调整后端 API 与前端业务功能。

## v2.02 - 2026-06-12

- 前端重绘为 Apple 风格工作台界面。
- 上传区支持拖拽 `.xlsx` 文件并保留点击选择文件能力。
- 结果区强化转换简报，展示输入行数、转换成功行数、结构匹配行数和待复核行数。
- 增加窗口式可视化表格预览和报告摘要展示。
- 项目版本号同步更新为 `v2.02`。

## v2.01 - 2026-06-12

- 前端状态标识升级为 `v2.01`。
- 结果区增加“报告窗口”，显示 `输入xx行，匹配xx行。`。
- 结果区增加“表格预览”窗口，展示回填后前 10 行数据。
- 后端 `/api/health` 返回当前版本号 `v2.01`。
- 后端 `/api/process` 返回 `report_text` 和 `table_preview`，供界面直接展示。

## v0.1.0 - 2026-06-12

- 确定本地软件方案：React/Vite 前端 + FastAPI 后端。
- 建立结构化知识库匹配路线：`要素1` 至 `要素5`、`单位` 匹配 `基价`。
- 实现 Excel 回填、Word 报告和 100 行答案表自动验收。
- 增加 FastAPI 上传/下载接口和 React 本地网页界面。
- 增加项目 README、项目 AGENTS、实施计划和 `.gitignore`。
- 修复前端运行在 `http://127.0.0.1:5174` 时上传 Excel 被 CORS 拦截并显示 `Failed to fetch` 的问题。
