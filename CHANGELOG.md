# CHANGELOG

## v5.23.0 - 2026-08-13

- 修复普通飞书 / WeAct 共用 SQLite 任务队列时的锁竞争假健康：空队列轮询不再每秒申请写锁，`database is locked` 和其他工作周期异常改为记录后退避重试，不再让任务 Worker 静默退出而长连接仍误报运行。实际卡住任务 `FS-20260813-210649-723F1D` 在只重启普通飞书运行器后无需重新上传，已自动经 `matching → risk → report → uploading` 完成并回传 Excel / Word；双平台专项 `129 passed`，后端全量 `531 passed, 5 skipped`，另 5 项仅因工作区正在改名的结算审核演示样例缺失而失败。
- 在现有“专业能力中心”新增“数智员工上岗证据”：以只读接口聚合 Registry / Manifest、`SkillRuntimeContext`、真实 Task / TaskEvent、实际 Tool、Artifact、人工复核、Experience 血缘与 V3.1 正式成对验收，集中证明“能上岗、能干活、能成长，而且不越权”；未新增左侧菜单、第二套 Dashboard 或平行状态机。
- 已上线勘察测量 Skill 可查看岗位目标、范围、输入、正式成果、岗位与人工责任、完整能力包、允许能力、人工闸门、最近验证、真实 Task 下钻与安全成果下载；规划中 Skill 只显示不足态且继续不可创建任务。候选、确认、撤销和疑似失效 Experience 分别呈现，不把候选冒充已确认经验。
- 页面与导出 Markdown 使用同一个证据响应，包含生成时间、Skill ID / 版本、数据来源、已完成证据、事实边界、尚未完成事项和生产化免责声明；后端过滤绝对路径、平台 ID、秘密、完整哈希和个人敏感信息。任一聚合源异常只降级证据区，不阻断 Excel、匹配、风险、报告、知识问答或智能协同。
- UI 使用局部作用域、自托管苹方、现有 `--dws-*` 令牌和 28 / 20 / 16 / 14 / 13 / 12px 层级；深浅主题、键盘 Esc、减少动态、1366 / 1440 / 1920 及 768 / 430 窄屏均无页面级横向溢出，功能打开前后无新增控制台错误或警告。
- 验证通过：证据后端专项、专业 Skill / Task 专项、前端专项与全量、生产构建、UI 设计系统、PRD 一致性和后端全量。V3.1 回归 6 张共同 Sheet、1254 个目标单元格的三个数字、公式、颜色、复核状态、隐藏行和全值差异均为 0；Word 封面（日期除外）与业务核心差异为 0，业务内容通过。本版未部署、未 push、未发送外部消息，未修改正式规则、结构化计价库、经验池母版、报告模板或 V3.1 原始输入 / 答案。

## v5.22.1 - 2026-08-13

- `#知识库`、`查库：` 和知识问答 API 在本地正式资料、结构化资料及已确认知识记忆均无命中时，不再返回固定的“未找到明确依据”拒答；统一先显示“已自动转为大模型回答”，再调用普通大模型输出一般性兜底答案。
- 零证据响应继续保持 `evidence_found=false`、来源为空，并新增 `answer_mode=general_model_fallback` 与 `generated_by_model` 状态；兜底提示明确说明回答不属于本地知识库依据，禁止模型伪造项目价格、系数、条款编号、标准出处和内部事实。模型不可用时返回明确的兜底暂不可用提示，不恢复旧拒答。
- 飞书 / WeAct 的 `#知识库` 卡片同步识别模型兜底回答，底部改为“未检索到知识库明确依据”的专用边界说明；有证据知识库回答的现有卡片、来源和价格 / 系数人工复核提示保持不变。
- 本版不修改知识检索召回、经典 / 混合 RAG 模式、证据冲突处理、正式规则、结构化计价库、三个数字、Excel / Word 成果和原始资料。新行为 6 项红→绿、知识问答 / 记忆 / 机器人等相关 `270 passed, 2 skipped`，前端生产构建、Python 编译、混合 RAG 故障注入和 `git diff --check` 通过；全量后端仅 5 项结算审核测试因用户工作区已将既有演示样例改名而失败，PRD 严格巡检同样只报该既有缺失路径，知识库模块自身通过。

## v5.22.0 - 2026-08-13

- 完成跨模块专业数据可视化：新增共享 `ChartFrame / ChartState / chartTheme / chartFormatters / chartAccessibility`，并将 `VIS-01`—`VIS-15` 增量接入 Task、项目详情、经验池、AI填价、结果预览、结算审核、工作量抓取、智能协同、知识记忆、RAG 调试和专业能力中心；项目看板既有 KPI、八项图形、五阶段漏斗、历史表、筛选、联动和汇报模式一项未减少。
- 图形严格读取原责任模块的真实 Task 事件、候选、经验阈值、预览行、结算明细、工作量摘要、复核状态、知识审计、检索轨迹和 Registry / Manifest；后端只为结算结果增加同次审核明细聚合出的向后兼容 `sheet_summaries`。候选点选后仍需原确认动作，图形不写正式值、不改变三个数字、风险、审核、协同、知识或报告裁决。
- 完成完整状态、可访问文本摘要、键盘语义、减少动态效果和响应式；统一自托管苹方及 16 / 14 / 13 / 12px 层级，11px 仅用于有注释的纯数字坐标。修复隐藏无障碍表格导致的横向溢出；1366 / 1440 / 1920、深浅色、328px 窄工作区、computed 字体与控制台 0 告警通过。
- 验证通过：可视化专项 6 项、前端全量 78 项、后端全量 `527 passed, 5 skipped`、生产构建、UI 设计系统与 PRD 严格一致性。V3.1 Excel 6 张共同 Sheet、1254 个目标单元格的三个数字、公式、目标填值、复核状态、隐藏行和全值差异均为 0；Word 封面（日期除外）与业务核心差异为 0，风险事实无遗漏。
- 本版只做本地 Git 存档；未部署、未 push、未发送飞书 / WeAct 消息，未打包代码版 zip，未修改 V3.1 原始输入 / 答案和用户未跟踪资料。
- 后续增量新建“竖屏与触控适配”模块 PRD，并新增独立 `compact-touch.css`；全部业务样式只在 `max-width: 1024px` 内生效，现有 React 组件、桌面主样式、桌面三栏、折叠偏好、可见版本信息和业务状态均未修改。
- iPad / 手机使用横向触控模块带、单列主工作区和覆盖式智算 Sheet；主要按钮与表单控件达到 44px 触控高度，文本输入为 16px，并使用 `100dvh`、安全区和受控滚动处理移动浏览器环境。
- 适配层专项 3 项、生产构建、UI 设计系统和 PRD 一致性巡检通过；浏览器核对 390 / 768 / 820 / 1024 / 1366 / 1440 / 1920px，窄屏无页面级横向溢出，1440×900 六个核心节点及宽屏可见版本信息与实施前基线一致。本增量未部署、未重启现有服务、未 push。
