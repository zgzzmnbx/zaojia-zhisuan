# CHANGELOG

## v5.14.0 - 2026-07-22

- 完成专业 Skill P1 文件特征推荐：上传 Excel 后只读取文件名、有限工作表名和有限表头，按各 Manifest 的 `recommendationProfile` 返回候选分值、置信度和可解释命中理由；推荐不自动切换，必须由用户确认。
- 完成开放格式包装：三个能力目录均提供 `SKILL.md`，后端公开 `manifest.json + SKILL.md` 格式、必填字段、状态和安全策略；程序只信任结构化 Manifest 与代码白名单，不从 Markdown 解析价格、系数、风险规则或可执行入口。
- 完成专业能力管理中心 P2 安全接口与简约界面：可盘点本机能力、查看开放格式，并为安装、启停、升级、回滚、卸载和导出生成审核计划；当前统一返回 `changes_applied=false`，所有写操作关闭。
- 通用服务类和更多造价专业继续保持规划状态；本版本只搭建接口和治理边界，不复制勘察测量资产、不伪造第二专业数据、不导入未知包、不执行任意外部脚本。
- 验证完成：专业 Skill 专项 `22 passed, 1 skipped`，全量后端 `346 passed, 4 skipped`，前端生产构建、报告预览 `8 passed`、工作台 `3 passed`；内置浏览器已核对管理中心、审核计划、1280×720 无横向溢出和零控制台错误。

## v5.13.0 - 2026-07-22

- 完成智能协同 P0-A 外部任务主动派发模拟：新增标准 `TaskEnvelope`、来源任务幂等键、任务 / Skill / 模板快照和本地派发台账；先落库再投递，避免外部平台瞬时失败造成任务丢失。
- 复用现有第二层飞书 / WeAct 配置与受控人员映射，仅允许向唯一授权“智算测试”群发送经典卡片和任务专属 Excel 副本；精准单聊在主动触达未实测前保持硬关闭，不向其他群发送测试消息。
- 卡片与附件按独立步骤记录状态，支持只重试未完成步骤；前端智能协同页提供外部任务模拟表单、派发状态和脱敏台账，不回显平台用户 ID、群 ID、文件 Key 或运行秘密。
- 更新模块 13 PRD、产品总览、当前版本计划、README、飞书第二层说明和长期安全约束；本阶段不实现多人复核、意见闭环、成果版本或外部系统生产鉴权。
- 验证完成：外部派发专项及全量后端测试纳入 v5.13.0 回归，前端生产构建通过；真实投递仍严格受授权目标和现有平台配置约束。

## v5.12.0 - 2026-07-22

- 完成专业 Skill P0-B 运行时接口：新增任务级 `SkillRuntimeContext`，冻结输入规范、白名单处理器、结构化计价库、规则资产、经验池与阈值、知识源、报告模板、验证信息和能力开关；任务状态只保存项目内相对路径，公开接口不泄露真实资产路径。
- 现有勘察测量流程包装为代码白名单 `survey-measurement-v1`，继续复用同一 `FillEngine` 和三数字口径；批量匹配、辅助填价、经验池预警、任务内知识问答、风险报告、Word 重生成和下载均从任务快照恢复运行时上下文，不再重新读取当前默认 Skill。
- 报告生成支持显式 Skill 模板，知识检索支持按 Skill 知识源独立建索引，经验池预警按 Skill 风险配置读取；前端知识问答自动携带当前任务或所选 Skill 上下文。
- 新增测试专用第二 Skill 隔离夹具，验证不同计价库、技术规则、经验池、知识源和 Word 模板真实切换，并验证默认 Skill 改变后既有任务不漂移；该测试能力不进入生产清单，通用服务类仍待真实资料与数据。
- 验证完成：专业 Skill 专项 `19 passed, 1 skipped`；全量后端 `333 passed, 4 skipped`；前端 v5.12.0 生产构建通过。现有 100 行旧请求与显式 Skill 逐工作表、逐单元格一致。
- 内置浏览器运行态验收通过：v5.12.0 演示样例 101 / 101 匹配、0 行待复核、任务 Skill 已锁定；1433px 视口无横向溢出，控制台无错误。
