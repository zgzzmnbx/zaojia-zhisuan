# CHANGELOG

## v5.16.2 - 2026-07-29

- 新增 `docs/云端发布与验收操作指南.md`，固化版本确认、本地门禁、完整资产暂存、UTF-8 `tar.gz`、SHA256 比对、运行数据继承、软链接切换、主服务健康检查、第二层机器人条件恢复、失败回滚和上线验收流程。
- 新增 `00-PRD/05-线程交接提示词/云端发布专用线程-交接提示词.md`，后续可由独立 Codex 线程直接完成云端发布、验证、文档回写和 Git 推送，不再依赖单一聊天上下文。
- 本机 SSH 凭据统一保存在 Git 忽略的 `Codex-Temp/runtime/cloud-deployment-credentials.json`，仅供授权发布线程静默读取；密码不进入聊天、Git、发布包、项目文档或云端目录。
- 本版本只提升云端发布的可重复性、安全性和交接效率，不改变三个数字匹配、知识检索、经验池、报告模板、智能协同业务状态机或原始资料。`v5.16.2` 已发布到 `/opt/zaojiazhisuan/releases/20260729-v5.16.2-cloud-release-r3`；公网健康、首页、实际 JS / CSS、bundle 版本、知识检索、1285 端口保护和机器人关闭态均已通过验收。

## v5.16.1 - 2026-07-29

- `@知识库` 增加三层知识库选择：第一层项目正式知识库、第二层造价通用知识库、第三层已确认知识记忆；后端按选择建立独立检索索引并标注来源层级，选择只影响问答，不参与基价 / 单价、调整系数、经验池或报告裁决。
- 新增可人工维护的造价通用知识资产清单和常见问题配置；输入 `@` 后空格自动补全 `@知识库：`，输入完整前缀后可点选常见问题并继续修改，大小智算窗口行为一致。
- 大小窗口统一补齐 Markdown 加粗、列表、段落、行内代码、链接和表格解析；窄屏表格支持横向滚动，回答区增加“智算回答”，正式依据改为优先摘要与按需展开完整路径，知识记忆详情不再无条件堆叠。
- 右侧智算默认隐藏头像并提供显示开关，助手气泡与用户气泡右边界对齐；转换进度进入会话回复；左侧快捷入口与模块图标、输入区选中边框、操作按钮和建议面板继续按统一视觉规范收口。
- 辅助填价重构为“当前记录 / 建议值 / 行级 AI 复核”三栏工作区，结构化候选与大模型解释同屏核对；候选相似度、浅色滚动条、依据追溯和自适应确认备注完成视觉收口。大模型仍只解释、不写值，最终写入必须由人工选择。
- 原始工作量抓取上传卡片、智算回复宽度及多处窄屏布局完成小幅优化；新增知识库选择、知识记忆、Markdown / 表格和前端展示专项测试，不修改专业匹配规则、结构化计价库、报告模板或原始输入文件。

## v5.16.0 - 2026-07-28

- 将“大尾巴 Shadcn UI v1.0.0”从填价工作台和 Dashboard 扩展到高价值工作区：智能协同改为“协同任务 / 新建派发 / 连接设置 / 通知与记录”四标签单面板，工作量抓取增加四步工作流并重排输入表、目标表和字段设置，结果预览补齐生产级空状态与窗口工具条。
- 大模型设置重构为“模型接入 / 智算窗口 / 快捷指令”分区弹窗，顶部标题、内部滚动和固定底部操作遵循统一 Dialog 规范；智算助手专注页与右侧 Dock 统一空状态、主色和提示层级；页面设置移除本机绝对路径回显，界面微调改为右侧 Sheet。
- 项目看板恢复 KPI、分析图表、闭环漏斗、历史列表和当前任务的 `1px` 浅灰模块边界，保持纯白背景、无常驻阴影和蓝色图表；工作量抓取新增容器级响应式规则，右侧智算展开时不再产生横向溢出。
- 云端发布流程补齐机器人条件启动保护：先恢复并验收 1285 主服务，再按状态接口的 `enabled` 决定启动或停止第二层机器人，避免关闭状态下强制重启 systemd 单元。
- 修复云端精简包漏带 3 项专业 Skill 资产导致“专业能力所需资产缺失”的发布问题；新增 `tools/check_cloud_release.py`，上传前强制校验云端必需文件和所有已上线 Skill Manifest 资产。云端 WeAct 默认改为关闭且 systemd 不自启，只有用户明确启用后才建立单一长连接。
- 业务接口、三个数字匹配、经验池预警、报告模板和智能协同状态机均未改变；前端生产构建、后端全量测试、设计系统检查、PRD 巡检、浏览器跨页视觉验收和控制台检查作为发布门禁。
