# CHANGELOG

## v5.16.0 - 2026-07-28

- 将“大尾巴 Shadcn UI v1.0.0”从填价工作台和 Dashboard 扩展到高价值工作区：智能协同改为“协同任务 / 新建派发 / 连接设置 / 通知与记录”四标签单面板，工作量抓取增加四步工作流并重排输入表、目标表和字段设置，结果预览补齐生产级空状态与窗口工具条。
- 大模型设置重构为“模型接入 / 智算窗口 / 快捷指令”分区弹窗，顶部标题、内部滚动和固定底部操作遵循统一 Dialog 规范；智算助手专注页与右侧 Dock 统一空状态、主色和提示层级；页面设置移除本机绝对路径回显，界面微调改为右侧 Sheet。
- 项目看板恢复 KPI、分析图表、闭环漏斗、历史列表和当前任务的 `1px` 浅灰模块边界，保持纯白背景、无常驻阴影和蓝色图表；工作量抓取新增容器级响应式规则，右侧智算展开时不再产生横向溢出。
- 云端发布流程补齐机器人条件启动保护：先恢复并验收 1285 主服务，再按状态接口的 `enabled` 决定启动或停止第二层机器人，避免关闭状态下强制重启 systemd 单元。
- 业务接口、三个数字匹配、经验池预警、报告模板和智能协同状态机均未改变；前端生产构建、后端全量测试、设计系统检查、PRD 巡检、浏览器跨页视觉验收和控制台检查作为发布门禁。

## v5.15.4 - 2026-07-27

- 将当前已验收界面正式沉淀为“大尾巴 Shadcn UI v1.0.0”：以 shadcn/ui preset `b1au7YYAi` 的 Rhea / Neutral / Blue / Lucide 和 Inter 比例语言为设计输入，运行时继续使用项目自托管苹方，不全量执行 shadcn init / create / apply，也不引入 Tailwind 或第二套全局主题。
- 新增 `frontend/src/design-system/dabawei-shadcn-ui.tokens.json` 与 `dabawei-shadcn-ui.css`，建立 primitive → semantic → component 三层令牌；现有大尾巴主题和 Dashboard 通过 `--dws-*` 变量映射渐进接入，保留成功、警告、危险等业务语义色和组件局部作用域。
- UI 规范补齐按钮、输入框、卡片、分段切换、表格、弹窗、图表、状态与无障碍规则；新增可独立查看的组件样张和 `tools/check_ui_design_system.py` 自动检查器，并同步 README、产品总览、当前版本计划、Dashboard PRD 与开发提示词。
- 验证完成：设计令牌 JSON 与自动检查器通过，前端生产构建成功，后端 API 专项 `65 passed, 2 skipped`，PRD 一致性巡检和 `git diff --check` 通过；内置浏览器确认 v5.15.4、苹方字体、`#2563EB` 主色、`#E2E8F0` 边框、148×36 分段切换器均在运行态生效，项目看板、新建填价、结果预览和右侧智算无横向溢出、控制台 0 错误 / 0 警告。

## v5.15.3 - 2026-07-26

- Dashboard 在五项 KPI 与分析图表之间新增“项目进入 → 完成匹配 → 完成预警 → 生成报告 → 完成复核”横向处理漏斗，以当前筛选项目累计统计每阶段数量、相对上一阶段转化率和流失量，便于汇报对象一眼理解业务闭环。
- 后端项目台账新增 `lifecycle_funnel` 聚合和 `lifecycle_stage` 共用筛选参数；匹配、预警、报告和复核均由真实任务状态与成果存在性确定，后阶段严格属于前阶段，不使用补造数据。点击任一阶段会同步刷新 KPI、图表和历史项目，并生成可清除的阶段筛选 chip。
- 漏斗采用项目 UI 规范字号、浅蓝单色序列、键盘按钮与无障碍文本摘要；窄容器保留横向流程并受控滚动。全部视觉仍限定在 `.project-dashboard`，未修改新建填价、结果预览、Word 报告、智能协同、右侧智算或专业计算逻辑。
- 验证完成：后端全量 `392 passed, 4 skipped`，前端对话工作台 `3 passed`、Dashboard `10 passed`、报告预览 `8 passed`，生产构建、Python 编译和 PRD 巡检通过。1433×1021 内置浏览器确认真实漏斗为 `18 → 3 → 2 → 2 → 2`，阶段筛选与历史项目同步、可撤销，无横向溢出，控制台 0 错误 / 0 警告；切换新建填价后漏斗卸载且智算展开，返回看板后智算重新收缩。
