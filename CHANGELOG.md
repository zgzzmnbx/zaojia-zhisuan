# CHANGELOG

## v5.19.1 - 2026-08-04

- `#知识库` 在检索前确定性拆分“业务检索问题、回答与展示要求、补充说明要求”，只用业务内容扩展检索词；示例复杂题由49个词项降至18个，表格要求、字段清单和“讲述原理”等附加表达不再污染召回。
- 技术工作费兜底回答按题面表号收口；表2“不参与金额”问题完整区分线路航测、走向图编制的当前零系数规则，以及像控点联测、地物地貌调绘、DLG/DEM/DOM、地图编制“显示0.22但不参与小计”的兼容口径，不再额外输出表3、表4。
- 三层知识库演示问题文档改为“简单检索题优先、复杂约束题用于验收”，新增人工标准答案与机器可读回归题集；运行脚本按关键事实、禁止项、证据来源和无依据固定答案做语义验收，不要求模型逐字复现。
- 实际调用本机 `#知识库` 完成6道端到端回归，覆盖简单题、复杂题、表3系数、清单编码10101002、数字化项目5000万元管理费和无依据降级，结果全部通过。本版不修改三个数字匹配、Excel公式、经验池或报告模板。

## v5.19.0 - 2026-07-30

- 填价工作台在 Excel / Word 下载入口旁新增“发给同事复核”：用户完成批量匹配后，可直接选择普通飞书或企业 WeAct，复用智能协同已读取并临时保存的工作群 / 人员目录，逐一勾选 1—10 名复核人并确认本次受众。
- 新增网页成果复核后端接口：浏览器只提交当前网页任务编号和受控人员引用，后端从运行目录读取真实输出 Excel，校验任务、Skill 快照和文件格式，计算 SHA256 并保存只读成果快照；客户端不能指定任意本机路径或替换成果文件。
- 网页成果直接进入现有多人复核状态机，沿用复核评论、逐人通过 / 退回、全部通过后完成和页面状态同步；同一网页任务与同一成果哈希重复提交保持幂等，文件和复核卡按目标分别记录，重试只补发未成功步骤。
- 继续执行 WeAct / 飞书受众铁律：个人私聊只对已完成主动触达验证的平台开放且只触达本次明确勾选人员；工作群必须由用户明确选择、成员可核验、复核人均属于该群且实际成员不超过 10 人；切换平台、工作群或投递方式后前端自动清空复核名单和确认状态。
- 新增网页复核后端与 API 专项测试，覆盖真实后端成果读取、只读快照、明确名单、完成通知不臆造网页编制人、重复提交幂等和失败步骤重试；前端生产构建与既有外部派发专项回归通过。本版未部署云端，也未修改三个数字匹配、经验池、知识问答、报告模板或结算审核逻辑。

## v5.18.0 - 2026-07-29

- 新增长输管道勘察测量“结算审核助手”P0：基于前辈指导文档、统一结算模板和实际案例，将模板参数、算术关系、特殊规则、其他费用和证据完整性整理为 8 条当前启用的确定性规则卡；规则不足、合同 / 工程量 / 发票 / 签章或现场事实不确定时明确转人工核验，不让大模型裁决结算金额。
- 新增独立结算审核引擎、FastAPI 路由和运行目录；支持统一模板及同结构 `.xlsx` 上传，原始文件只读，审核任务隔离在 `Codex-Temp/runtime/settlement-audit/`，不接入也不改变既有填价、三个数字匹配、经验池、知识问答、项目台账和飞书 / WeAct 状态机。
- 一次审核生成保留全部原工作表并新增“审核结果”的 `【审核后】*.xlsx`、包含结论 / 金额 / 逐项发现 / 人工签署区的 `【审核报告】*.docx` 和结构化 `审核结果.json`；明细表同步写入审核状态、风险等级、规则编号、建议金额、差额和可追溯批注。
- 新增可重复生成的结算审核演示样例与精确预期结果：审核 8 行、识别 8 项风险，其中 7 项高风险、6 项待人工核验；上报明细试算 `¥461,877.02`、审核建议试算 `¥429,505.02`、建议差额 `¥32,372.00`。
- 左侧新增独立“结算审核”入口和比赛演示工作台，严格复用“大尾巴 Shadcn UI v1.0.0”的 `--dws-*` 令牌、Lucide 图标、纯白背景、细边框和统一圆角；模块内无渐变背景、无常驻阴影，未引入 Tailwind、`components.json` 或第二套主题。
- 结算审核是同一勘察测量专业内的真实独立场景 MVP，尚未登记为 Skill Registry 中的第二个真实专业 Skill；P0 仅支持统一结算模板及同结构 Excel，最终审定仍由有权限的造价人员完成。
- 飞书、WeAct 与网页智算的知识库和辅助审核指令统一为 `#知识库`、`#辅助审核`，避免平台输入 `@` 时触发人名联想；机器人帮助、自我介绍、快捷预填、推荐问题、绿色版默认配置、当前 PRD 和测试口径同步更新，同名 at 符号旧别名不再进入特殊业务路由。
