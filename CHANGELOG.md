# CHANGELOG

## v5.19.6 - 2026-08-09

- 智算助手含图表回答改为按完整段落呈现，避免 Markdown、列表和图表说明逐字跳动；费用洞察按标题、来源、三项指标、环形图、条形图和口径说明分八阶段自上而下展开，并在大小会话窗口中随阶段保持底部可见。
- 补齐动态效果无障碍边界：用户启用“减少动态效果”时，费用洞察立即完整展示，自动滚动不再使用平滑动画；普通纯文本回答继续保留原逐字呈现节奏。
- AI填价三栏弹窗统一自托管苹方字体、正文与标题层级、留白、候选卡和依据区边界，并把纵向布局断点调整到 `1100px`；只提升阅读和复核体验，不改变候选排序、模型边界、人工确认、写值或三个数字裁决。
- 梳理 README、当前版本计划和模块 PRD：完整混合 RAG 明确为已完成，开发提示词降为实现 / 回归参考，赛前下一步第一优先收敛为“可信经验进化闭环”；业务云端仍为 v5.19.4，本次 Git 推送不等于服务器发布。
- 验证通过：前端自动化 `56 passed`、前端生产构建、后端健康接口、UI 设计系统 11 项检查和 PRD 17 模块 / 241 项关联资产硬一致性检查均为绿色；本地运行态核对 v5.19.6 版本、AI填价三栏实际字体 / 字号 / 列宽、278px 窄 Dock 费用洞察、自动跟随和浏览器控制台，无错误或警告。

## v5.19.5 - 2026-08-06

- 完成完整混合 RAG 首版：保留 classic 默认路径，新增用户主动切换的 hybrid；混合模式真实包含 BM25、结构化字段、本地 384 维确定性向量、RRF 融合、权威 / 数字 / 编码 / 比例尺 / 单位硬门控和可选规则重排。
- 冻结知识问答黄金证据评测，classic 与 hybrid 均保持精确保护集 Recall@5 1.000；classic 热检索 P95 969.823 ms，hybrid 黄金 Recall@5 1.000、引用准确率 1.000、无依据误答率 0、nDCG@10 0.951188、热检索 P95 1,428.589 ms，均在 1.5 秒目标内；指标未改变默认模式。
- `/api/knowledge/search`、`/api/knowledge/ask` 和知识库列表返回请求 / 实际模式、通道、证据状态、能力和降级原因；前端新增 classic / hybrid 分段按钮、个人偏好、历史消息实际模式冻结和预置题 `curated_demo` 标记。
- 已完成索引损坏、向量不可用、重排超时和大模型不可用故障注入；索引显式恢复后复跑绿色。hybrid 只影响知识问答，不改变填价、三个数字、经验池、工作量抓取、报告计算、正式资料和云端版本。
- 费率类数字增加证据表格行硬门控：模型输出与命中行不一致或随机追加百分号时，自动回退到证据驱动表格，避免精确费率被误写或误判。
- 为保持两套模式热检索时延，经典索引增加按真源签名失效的进程内缓存，向量余弦计算改为稀疏双指针交集；不改变召回、融合或价格裁决边界。
- `/api/health.version` 保留旧运行器兼容值 `v5.19.4`，同时通过 `release_version` 返回当前发布版本 `v5.19.5`；不改变 FastAPI、前端和发布包的当前版本。

## v5.19.4 - 2026-08-05

- AI填价弹窗新增纯蓝分阶段进度条，从低进度起步，依次显示整理当前行要素和前三候选、检索正式依据并核对差异、组织 AI填价建议；等待真实返回时最高停在 94%，不再一开始接近完成。
- 大窗口与右侧小窗口复用任务执行和知识处理进度组件；小窗口在转换、批量匹配、经验池预警、风险处理等真实任务期间同步显示当前步骤与百分比。
- “费用构成洞察”统一进入大小窗口消息渲染；右侧 Dock 使用容器查询自动压缩指标卡，并将环形图、条形图切为单列竖排，窄至 278px 仍无横向溢出。
- 费用构成环形图、专业费用条形图和16种管径知识库柱状图增加 1.5 秒入场动画，大小智算窗口复用同一动效口径。
- 已发布到云端 `/opt/zaojiazhisuan/releases/20260805-v5.19.4-cloud-release`；独立迁移并合并本机与线上项目看板历史数据，云端现有 71 个项目、76 次运行、126 个可定位成果和 697 条模型调用记录，未把 SQLite、成果文件或秘密写入代码发布包与 Git。
- 新增前端进度复用与图表自适应测试；专项 11 项、费用图表 3 项、前端生产构建与浏览器运行态验证通过。本版不修改三个数字匹配、知识库、经验池算法、报告模板或原始 Excel。
