# CHANGELOG

## v5.18.0 - 2026-07-29

- 新增长输管道勘察测量“结算审核助手”P0：基于前辈指导文档、统一结算模板和实际案例，将模板参数、算术关系、特殊规则、其他费用和证据完整性整理为 8 条当前启用的确定性规则卡；规则不足、合同 / 工程量 / 发票 / 签章或现场事实不确定时明确转人工核验，不让大模型裁决结算金额。
- 新增独立结算审核引擎、FastAPI 路由和运行目录；支持统一模板及同结构 `.xlsx` 上传，原始文件只读，审核任务隔离在 `Codex-Temp/runtime/settlement-audit/`，不接入也不改变既有填价、三个数字匹配、经验池、知识问答、项目台账和飞书 / WeAct 状态机。
- 一次审核生成保留全部原工作表并新增“审核结果”的 `【审核后】*.xlsx`、包含结论 / 金额 / 逐项发现 / 人工签署区的 `【审核报告】*.docx` 和结构化 `审核结果.json`；明细表同步写入审核状态、风险等级、规则编号、建议金额、差额和可追溯批注。
- 新增可重复生成的结算审核演示样例与精确预期结果：审核 8 行、识别 8 项风险，其中 7 项高风险、6 项待人工核验；上报明细试算 `¥461,877.02`、审核建议试算 `¥429,505.02`、建议差额 `¥32,372.00`。
- 左侧新增独立“结算审核”入口和比赛演示工作台，严格复用“大尾巴 Shadcn UI v1.0.0”的 `--dws-*` 令牌、Lucide 图标、纯白背景、细边框和统一圆角；模块内无渐变背景、无常驻阴影，未引入 Tailwind、`components.json` 或第二套主题。
- 结算审核是同一勘察测量专业内的真实独立场景 MVP，尚未登记为 Skill Registry 中的第二个真实专业 Skill；P0 仅支持统一结算模板及同结构 Excel，最终审定仍由有权限的造价人员完成。

## v5.17.0 - 2026-07-29

- 第二层从“普通飞书 / 企业 WeAct 单配置下拉切换”升级为双平台并行架构：两平台各自保存启停控制、PID 和运行日志，由独立 `feishu_bot_runner.py` 进程分别接收消息、文件与 `card.action.trigger` 回调；任一平台关闭、启动或配置异常均不改变另一平台运行状态。
- 第二层 SQLite 任务、收件窗口和确定性会话查询新增 `platform_profile_id`：任务创建时冻结来源平台，知识库 / 普通问答、任务查询、成果重发、编制成果收件和卡片回调只能在原平台继续处理；旧任务与旧收件窗口启动时执行增量迁移，不改历史成果。
- 两个平台继续复用现有 FastAPI 专业能力和同一任务库，但两个 Worker 通过全局活动任务约束只允许一个专业任务运行，并按所有已启用平台的最早入队顺序领取；关闭中的平台停止领取新任务，当前任务完成后再安全退出。
- “智能协同”移除第二层机器人下拉替换，改为普通飞书、企业 WeAct 两张独立状态卡和两个开关；系统右上角增加两个蓝色状态标签，任务记录显示来源平台。Windows 主启动器和机器人独立启动入口会恢复全部已启用平台，重复启动不会生成同平台重复实例。
- 云端新增常驻双平台监督器和平台级启动互斥：systemd 服务按两个独立开关恢复运行器，关闭的平台不建连，页面与监督器同时操作也不会重复拉起；云端验收脚本逐平台核对启用、配置、运行和退出状态。
- 同一平台 App ID 的跨环境单实例边界保持不变：普通飞书和 WeAct 均不得在本地与云端同时在线，避免平台集群随机分发造成任务、文件和成果分裂。本版本完成本地代码、自动化测试、前端生产构建、飞书模块 PRD 硬检查和无消息发送的浏览器运行态验收；全局 PRD 严格巡检仅剩任务开始前已存在的模块 08 外部申报书绝对路径缺失，未伪造资料。云端仍为 v5.16.2，未部署本版本。

## v5.16.2 - 2026-07-29

- 新增 `docs/云端发布与验收操作指南.md`，固化版本确认、本地门禁、完整资产暂存、UTF-8 `tar.gz`、SHA256 比对、运行数据继承、软链接切换、主服务健康检查、第二层机器人条件恢复、失败回滚和上线验收流程。
- 新增 `00-PRD/05-线程交接提示词/云端发布专用线程-交接提示词.md`，后续可由独立 Codex 线程直接完成云端发布、验证、文档回写和 Git 推送，不再依赖单一聊天上下文。
- 新增 `tools/build_cloud_release.py`：默认从当前项目、上线 Skill Manifest 和知识库配置直接组装独立发布目录，保持 `frontend/dist/assets` 层级，阻断秘密、SQLite、日志和运行缓存进入发布包，并自动执行资产门禁、生成 UTF-8 `tar.gz` 与 SHA256；不再复制上一版暂存清单。
- 本机 SSH 凭据统一保存在 Git 忽略的 `Codex-Temp/runtime/cloud-deployment-credentials.json`，仅供授权发布线程静默读取；密码不进入聊天、Git、发布包、项目文档或云端目录。
- 本版本只提升云端发布的可重复性、安全性和交接效率，不改变三个数字匹配、知识检索、经验池、报告模板、智能协同业务状态机或原始资料。`v5.16.2` 已发布到 `/opt/zaojiazhisuan/releases/20260729-v5.16.2-cloud-release-r3`；公网健康、首页、实际 JS / CSS、bundle 版本、知识检索、1285 端口保护和机器人关闭态均已通过验收。
