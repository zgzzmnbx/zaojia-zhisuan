# CHANGELOG

## v5.15.2 - 2026-07-26

- 项目看板将原占位较高的筛选工具条收敛为页头“筛选”按钮和纵向居中的局部弹窗；弹窗使用草稿态，只有点击“应用筛选”才请求数据，支持取消、重置、遮罩 / Esc 关闭、初始聚焦、Tab 焦点约束和关闭后焦点回归，并用数字角标提示非默认条件数量。
- 新增“汇报模式”：在纯白全屏画布中隐藏全局导航、左侧模块栏和右侧智算窗口，只保留 Dashboard 标题、操作、关键指标、图表和历史项目；支持按钮或 Esc 退出。打开项目看板时智算默认收缩，切换到新建填价、结果预览、Word 报告和智能协同时自动展开，返回看板后重新收缩。
- Dashboard 接口新增与 KPI、图表、历史项目完全同口径的 `source_distribution`；前端增加“项目来源分布”多蓝色环形图，当前真实台账展示网页上传 3、飞书 3、企业 WeAct 12。来源不足 2 种或项目不足 3 个时不绘制误导性图表，图例和环形分区可反向筛选来源。
- 全部新增样式继续限制在填价工作台 `.project-dashboard` 局部作用域；未引入 Tailwind、第二套全局主题或额外在线资源。同步修正 Dashboard 既有 Mobbin 参考图的真实目录名，并修复大模型真实调用测试中写死历史日期导致的跨日失效。
- 验证完成：后端全量 `391 passed, 4 skipped`，前端对话工作台 `3 passed`、Dashboard `9 passed`、报告预览 `8 passed`，生产构建通过；1433×1021 内置浏览器确认筛选、来源图联动、汇报模式、跨页智算展开 / 收缩和欢迎页直达行为正常，控制台 0 错误、0 警告、无 Recharts 零尺寸告警。PRD 严格巡检确认模块 17 硬检查通过，全项目仍仅保留模块 08 的 1 项既有外部申报书路径缺失，未擅自修改。

## v5.15.1 - 2026-07-24

- Dashboard 新增本机大模型请求次数浅蓝平滑波形图和请求模型种类多蓝色环形图；两图随看板开始 / 结束日期同步，提供常驻数值、文本摘要、真实空状态和无障碍描述，右侧智算展开时按容器宽度自动收敛为单列。
- 新增 `llm-usage-ledger.sqlite3` 调用审计台账，只保存请求时间、Provider、模型、调用来源和成功 / 失败状态，不保存提示词、回答、接口地址或密钥；普通问答、知识库有证据问答和风险报告统一记录，纯检索与无证据直接返回不计数，台账异常不阻断模型回答。
- 既有提示词审计日志支持幂等历史回填，明确排除 `demo-model`、`test-model` 和 `mock-model`；同一提示词记录的后续实时成功 / 失败状态会更新而不重复计数。当前最近 30 天真实审计口径为 198 次、2 种模型、26 个有效日期。
- 同步收敛 Dashboard 筛选工具条、浅蓝搜索态、白字主按钮、页签间距、标题文案和左侧当前任务环形图蓝色系；全部样式继续限制在填价工作台 / `.project-dashboard` 作用域，不修改其他页面和专业计算链路。
- 验证完成：后端全量 `391 passed, 4 skipped`，大模型调用台账与项目看板专项 `18 passed`；前端对话工作台 `3 passed`、Dashboard `8 passed`、报告预览 `8 passed`，生产构建、Python 编译、PRD 严格巡检和 `git diff --check` 通过。1433×1021 内置浏览器确认双图对齐、纯白背景、规范字号、无横向溢出，截图保存为 `Codex-Temp/visual-qa/project-dashboard/15-llm-usage-charts-1433.png`。

## v5.15.0 - 2026-07-24

- 完成“项目看板与历史项目”P0：新增本机 SQLite 项目台账，分层保存项目、处理任务、文件版本、复核轮次和成果引用；网页填价自动登记，既有 `process-state.json` 与智能协同任务可幂等回填，无法可靠归属的旧任务单列为“待归类历史任务”。
- 新增 Dashboard、项目列表、详情、运行记录、成果、历史结果、下载和回填接口；KPI、趋势、状态、风险、匹配质量和历史列表共用日期、Skill、状态、来源、关键词、风险与质量筛选口径，成果下载只接受受控 ID 和相对引用。
- “填价工作台”新增“项目看板｜新建填价”双视图，默认展示最近 30 天真实数据；支持 5 项 KPI、4 类分析图表、筛选联动、分页排序、详情抽屉、预览 / 报告打开和成果下载，切换视图不清空上传文件、项目名、映射、任务或结果。
- Dashboard 以 shadcn/ui preset `b27I2r8C` 的 Rhea / Neutral / Blue / Inter / Lucide 为视觉基准，结合指定 Mobbin 图和 `$ui-ux-pro-max` 检索结果完成 `.project-dashboard` 局部作用域转译；经浏览器批注收敛为纯白底、项目 UI 规范固定字号、浅蓝同色系图表和自适应日 / 月真实趋势，Recharts 按页懒加载，未引入 Tailwind、`components.json`、在线资源或第二套全局主题。
- 真实 100 行样例完成上传、转换、100 行匹配、经验池预警、Word 报告、Excel / Word 下载和历史成果回看；浏览器覆盖 1366、1440、1920、右侧 Dock 展开、空筛选、详情抽屉和历史表，最终控制台 0 错误、0 警告，截图保存在 `Codex-Temp/visual-qa/project-dashboard/`。
- 验证完成：后端全量 `388 passed, 4 skipped`，项目看板专项 `15 passed`；前端对话工作台 `3 passed`、报告预览 `8 passed`、Dashboard `7 passed`，生产构建与 Python 编译通过。测试运行目录已自动隔离，完整回归后真实台账仍为 `17` 个项目、`17` 次任务；PRD 严格巡检中模块 17 硬检查通过，全项目仍保留模块 08 的 1 项既有外部申报书路径缺失，未擅自修改。
