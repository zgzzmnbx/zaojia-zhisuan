# CHANGELOG

## v5.23.2 - 2026-08-14

- 统一根项目、前端包、后端健康接口、README 和当前版本计划的版本标识为 `v5.23.2`；本版沿用 v5.23.1 已验收成果完成云端发布，不新增功能，不改变暗色 / 亮色主题、三个数字裁决、正式规则、结构化计价库、经验池母版、报告模板、业务流程或成果文件。
- 云端发布目录为 `/opt/zaojiazhisuan/releases/20260814-v5.23.2-cloud-release`，上一回滚目录为 `/opt/zaojiazhisuan/releases/20260811-v5.19.7-cloud-release`，UTF-8 `tar.gz` SHA256 为 `B75110858451FBD130D17F7D9E41E51077C9C313B11C7D0EE4CDE58228125480`。
- 发布验收通过：`/api/health` HTTP 200 且 `release_version=v5.23.2`；首页实际引用的 4 个 JS / CSS 资源均 HTTP 200，主 bundle 含“数智员工”“上岗证据”标识；`/api/knowledge/search` HTTP 200 并返回 5 条结果；主服务与双平台监督器 `active`，普通飞书和企业 WeAct 均保持 `enabled=false / running=false / profile_consistent=true`，真实 runner 为 0。前端构建、前端 62 项单测、后端 `536 passed, 5 skipped`、UI 设计系统、PRD 严格巡检和发布包资产门禁通过；未发送任何飞书 / WeAct 测试消息。

## v5.23.1 - 2026-08-14

- 左侧状态区升级为四种真实状态视图轮播：状态胶片带、纵向地铁线、四格状态舱和任务接力带共用同一业务事实，原环形图固定保留；支持圆点、键盘、悬停暂停、页面不可见暂停和减少动态效果，不以轮播伪造业务进度。项目看板在进入主程序 5 秒后静默预加载，首次主动打开仍立即加载且不重复请求。
- 经验池预警明细点击后可同步聚焦偏离率散点图与经验区间图，按选中记录重算显示域，避免低风险被极端高偏离值压缩；候选价格点明确标注首选。移除结果预览行风险迷你地图和结算审核两张不适配图形，保留真实明细、原定位按钮和原审核结论。
- 竖屏与触控适配继续限定在 `<=1024px`：修复专业能力菜单、智算消息表格、知识依据、输入工具条和安全区溢出；`>=1025px` 宽屏规则不改。预置知识答案在行级上下文中仍优先命中，处理中提示改为自然、通用且不暴露“预置答案”。
- 暗色主题集中补齐欢迎页说明标签和连接线、表格悬浮与滚动条、Sheet 标签滚动条、预警图边框、报告状态、下载 / 跳转 / 快捷指令等局部控件；真实 Word 报告预览新增“纸张 / 暗色”显式开关，暗色由现有 DOCX 渲染器处理，不修改下载文件。
- 项目 `AGENTS.md` 已固化长期原则：所有暗色模式改动必须严格限定在暗色主题作用域，且不得影响普通亮色模式。本版不修改三个数字裁决、正式规则、结构化计价库、经验池母版、报告模板或原始输入 / 答案。
- 验证通过：前端全量 `97 passed`、后端全量 `536 passed, 5 skipped`、前端生产构建、PRD 严格一致性巡检和 `git diff --check`。浏览器运行态确认亮色欢迎页仍保持原白底 / 浅边框，暗色欢迎页标签与连接线使用暗色表面 / 边框，表格与 Sheet 标签滚动条的 computed `color-scheme=dark` 且轨道、滑块均为暗色令牌。

## v5.23.0 - 2026-08-13

- 修复普通飞书 / WeAct 共用 SQLite 任务队列时的锁竞争假健康：空队列轮询不再每秒申请写锁，`database is locked` 和其他工作周期异常改为记录后退避重试，不再让任务 Worker 静默退出而长连接仍误报运行。实际卡住任务 `FS-20260813-210649-723F1D` 在只重启普通飞书运行器后无需重新上传，已自动经 `matching → risk → report → uploading` 完成并回传 Excel / Word；双平台专项 `129 passed`，后端全量 `531 passed, 5 skipped`，另 5 项仅因工作区正在改名的结算审核演示样例缺失而失败。
- 在现有“专业能力中心”新增“数智员工上岗证据”：以只读接口聚合 Registry / Manifest、`SkillRuntimeContext`、真实 Task / TaskEvent、实际 Tool、Artifact、人工复核、Experience 血缘与 V3.1 正式成对验收，集中证明“能上岗、能干活、能成长，而且不越权”；未新增左侧菜单、第二套 Dashboard 或平行状态机。
- 已上线勘察测量 Skill 可查看岗位目标、范围、输入、正式成果、岗位与人工责任、完整能力包、允许能力、人工闸门、最近验证、真实 Task 下钻与安全成果下载；规划中 Skill 只显示不足态且继续不可创建任务。候选、确认、撤销和疑似失效 Experience 分别呈现，不把候选冒充已确认经验。
- 页面与导出 Markdown 使用同一个证据响应，包含生成时间、Skill ID / 版本、数据来源、已完成证据、事实边界、尚未完成事项和生产化免责声明；后端过滤绝对路径、平台 ID、秘密、完整哈希和个人敏感信息。任一聚合源异常只降级证据区，不阻断 Excel、匹配、风险、报告、知识问答或智能协同。
- UI 使用局部作用域、自托管苹方、现有 `--dws-*` 令牌和 28 / 20 / 16 / 14 / 13 / 12px 层级；深浅主题、键盘 Esc、减少动态、1366 / 1440 / 1920 及 768 / 430 窄屏均无页面级横向溢出，功能打开前后无新增控制台错误或警告。
- 验证通过：证据后端专项、专业 Skill / Task 专项、前端专项与全量、生产构建、UI 设计系统、PRD 一致性和后端全量。V3.1 回归 6 张共同 Sheet、1254 个目标单元格的三个数字、公式、颜色、复核状态、隐藏行和全值差异均为 0；Word 封面（日期除外）与业务核心差异为 0，业务内容通过。本版未部署、未 push、未发送外部消息，未修改正式规则、结构化计价库、经验池母版、报告模板或 V3.1 原始输入 / 答案。
