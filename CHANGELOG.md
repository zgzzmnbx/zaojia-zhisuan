# CHANGELOG

## v5.22.0 - 2026-08-13

- 完成跨模块专业数据可视化：新增共享 `ChartFrame / ChartState / chartTheme / chartFormatters / chartAccessibility`，并将 `VIS-01`—`VIS-15` 增量接入 Task、项目详情、经验池、AI填价、结果预览、结算审核、工作量抓取、智能协同、知识记忆、RAG 调试和专业能力中心；项目看板既有 KPI、八项图形、五阶段漏斗、历史表、筛选、联动和汇报模式一项未减少。
- 图形严格读取原责任模块的真实 Task 事件、候选、经验阈值、预览行、结算明细、工作量摘要、复核状态、知识审计、检索轨迹和 Registry / Manifest；后端只为结算结果增加同次审核明细聚合出的向后兼容 `sheet_summaries`。候选点选后仍需原确认动作，图形不写正式值、不改变三个数字、风险、审核、协同、知识或报告裁决。
- 完成完整状态、可访问文本摘要、键盘语义、减少动态效果和响应式；统一自托管苹方及 16 / 14 / 13 / 12px 层级，11px 仅用于有注释的纯数字坐标。修复隐藏无障碍表格导致的横向溢出；1366 / 1440 / 1920、深浅色、328px 窄工作区、computed 字体与控制台 0 告警通过。
- 验证通过：可视化专项 6 项、前端全量 78 项、后端全量 `527 passed, 5 skipped`、生产构建、UI 设计系统与 PRD 严格一致性。V3.1 Excel 6 张共同 Sheet、1254 个目标单元格的三个数字、公式、目标填值、复核状态、隐藏行和全值差异均为 0；Word 封面（日期除外）与业务核心差异为 0，风险事实无遗漏。
- 本版只做本地 Git 存档；未部署、未 push、未发送飞书 / WeAct 消息，未打包代码版 zip，未修改 V3.1 原始输入 / 答案和用户未跟踪资料。

## v5.21.0 - 2026-08-13

- 完成 Task 任务对象与执行轨迹 P0：新增独立 `BusinessTask / TaskLink / TaskEvent` 聚合库，稳定业务 `task_id` 显式关联 `job_id / run_id / collaboration_task_id / source_task_id`；任务定义、输入与 Skill ID / 版本 / Manifest 哈希创建后冻结，更换 Skill 或可靠来源才新建 Task。
- 专业处理、风险、报告、网页成果复核与外部协同只在真实动作发生时写事件；成果内容哈希变化才增加 Task 成果版本。聚合异常降级为“轨迹暂不可用”，不阻断 Excel、风险、报告、问答或协同；项目恢复保持状态单调，不再把已完成 Task 回退为处理中。
- 智算助手、专业工作区、结果预览、Word 报告、项目详情和右侧 Dock 复用当前任务条、详情抽屉和真实时间线；不新增一级菜单。修复 Task 条与模块内容共占 Grid 行导致按钮被遮挡的问题，保持 12 / 13 / 14px 自托管苹方、1px 边界、低圆角、无渐变和无常驻阴影。
- 验证通过：后端 Task 专项 10 项、风险报告结构化事实兜底 1 项、Task / 项目组合 26 项、全量 `527 passed, 5 skipped`；前端 Task 6 项、智算助手 13 项、项目看板 15 项、生产构建、UI 设计系统和 PRD 严格巡检。V3.1 Excel 6 张表 1254 个目标单元格的三个数字、公式、颜色、复核状态、隐藏行和全值差异均为 0，Word 业务核心一致；外部模型遗漏风险事实时只补充真实结构化证据，不参与价格或系数裁决。1366 / 1440 / 1920、深浅主题、Dock、computed 字体与控制台 0 告警通过。
- 本版只做本地 Git 存档；未部署、未 push、未发送飞书 / WeAct 消息，未修改三个数字规则、正式知识库、经验池母版、报告模板和 V3.1 原始输入 / 答案。

## v5.20.0 - 2026-08-11

- 完成可信经验进化闭环首版：人工改单 / AI填价确认和多人复核最终意见成功落盘后，以幂等事件进入既有 `knowledge-memory.sqlite3` 候选池；保存项目 / 隔离任务、Skill、Sheet / 行 / 字段、原值 / 新值 / 原因、成果版本 / 哈希和复核血缘，不保存绝对路径、平台 ID 或秘密。
- 候选继续服从既有人工治理状态机：价格、系数、正式标准、项目口径、冲突项及全部项目 / 任务范围始终人工确认；跨项目只允许提升为通用候选并再次人工审核。捕获失败只返回可重试警告，不回滚改单或复核结论；重复请求、无变化改单和无正文通过意见不造知识。
- 新增事件、重试、项目记忆胶囊和真实指标 API；已完成项目胶囊只汇总项目台账、Skill、成果、知识状态和复核意见的受控引用。知识问答命中记录知识 ID、来源项目、目标项目和时间，正式依据继续优先，撤销后立即停止命中。
- 在现有知识记忆弹窗内增加“知识记录｜项目胶囊｜进化成效”，详情展示事件来源和审计入口；沿用自托管苹方、字号 token、白底 / 深色表面、1px 边界和 6—8px 圆角。已完成 1366 / 1440 / 1920 / 720、深浅色、焦点、滚动、空态、computed 字体和零控制台告警验收。
- 隔离演示以真实 API / SQLite 状态跑通“项目 A → 候选 → 两次人工审核 → 项目 B 带来源命中 → 审计 → 撤销停止命中”，重复候选、撤销后跨项目命中和正式规则资产改动均为 0；本次只做本地 Git 存档，不部署、不 push、不发送飞书 / WeAct 消息。
