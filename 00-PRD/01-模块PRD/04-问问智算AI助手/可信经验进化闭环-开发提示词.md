你是本任务执行者，本文件是唯一任务来源；中途无人可问，拿不准的事项写入本任务目录下 `可信经验进化闭环-实施/BLOCKED.md`，继续完成不受影响的工作。
任务中断或换会话后，先读同目录 `PROGRESS.md` 续做；每完成一项立即更新，禁止从头重跑。
这项工作的目标，是把人工改值和多人复核意见沉淀为有来源、可审核、可撤销、可复用的项目经验，完成比赛可证明的“可信经验进化闭环”。
要求冲突时按“业务正确与不越权 > 数据可追溯 > 不破坏现有功能 > UI 规范一致 > 功能数量 > 开发速度”让步。
文中“只允许／不得／必须”是硬规则；“建议”可调整，但须在 `PROGRESS.md` 说明理由。

## 我替领导拍的板

- 目标版本采用 `v5.20.0`（猜的）。
- 首版只接两类真实触发：人工改单元格／AI填价确认、多人复核意见闭环；问答纠错、风险关闭、项目结项自动提取留到下一增量。
- 复用现有 `knowledge-memory.sqlite3`、知识记忆治理状态机和弹窗，不新建左侧入口、独立应用或第二套经验库。
- 首版跨项目只走现有“提升为通用候选”；事件来源的提升项无论类型都必须再次人工审核，不新增尚未设计完的专业范围。
- 没有可靠 `project_id` 的事件只进入隔离的任务范围并标记“待归类”，不得偷偷按项目名合并。
- UI 只能半托管：自动门禁全绿后，仍须用截图和 computed style 人工复核。
- 最终只做本地 Git 提交；不 push、不部署、不发飞书／WeAct 消息。

## 界限

只允许修改或新建：`backend/app/knowledge_memory.py`、`backend/app/trusted_experience.py`、`backend/app/main.py`、`backend/app/external_task_dispatch.py`；对应 `backend/tests/test_knowledge_memory.py`、`test_governance_fill_assist.py`、`test_external_task_dispatch.py`、新 `test_trusted_experience.py`；`frontend/src/App.tsx`、`frontend/src/styles.css`、可新建 `frontend/src/utils/trustedExperience.ts` 与对应测试；本模块 PRD、当前版本计划、README、CHANGELOG 和版本声明文件；本任务自己的 `PROGRESS.md/BLOCKED.md`、演示检查脚本和脱敏测试夹具。

其余路径只读。严禁改 `fill_engine.py`、三个数字规则、结构化计价库、经验池、正式资料、报告模板、V3.1 输入／答案、飞书受众与投递规则、既有验收脚本和阈值。现有测试只能追加，不得删除、skip、放宽断言或换成 mock 假闭环。不要碰当前工作区既有的 PRD 巡检日期差异、模块18手册、测试归档、VI、`output/outputs` 等非本任务改动；提交时逐文件暂存。

## 现状与任务 0

2026-08-11 实测：`v5.19.7` 的 `main` 与 `origin/main` 同在 `616ccdf`；知识记忆已有 `knowledge_items / knowledge_versions / knowledge_audit`，范围为 `general/task/project`。专项基线为知识记忆 26、辅助填价 6、外部协同 41，共 `73 passed`；前端 `60 passed`；后端 `507 collected`；前端构建及 UI 设计系统 15 项检查通过。组合专项退出 0 后会出现既有 Python 3.14／Windows `0x80010108` 收尾栈，须如实记录；若退出码非 0 或出现新失败则不算通过。

先运行 `git status --short`、`python -m pytest backend/tests/test_knowledge_memory.py backend/tests/test_governance_fill_assist.py backend/tests/test_external_task_dispatch.py --collect-only -q`、`node --test frontend/tests/*.test.ts`、`npm run frontend:build`、`python tools/check_ui_design_system.py`，核对以上数字。对不上时把证据置于 `BLOCKED.md` 首行，只做不受影响部分；对上后在 `PROGRESS.md` 用不超过10行写目标、顺序、最大风险。

## 任务 1：两类真实事件进入候选池

建立迁移安全的事件／来源模型，保存幂等事件键、项目／任务、Skill ID与版本、Sheet／行／字段、原值／新值／原因、成果版本与哈希、复核人／轮次／意见、候选ID和时间；元数据不得含绝对路径、平台ID或秘密。人工改值成功落盘后、复核任务最终完成后再捕获：重复请求只产生一条事件和一条候选；无变化的改单、无正文的通过意见不造知识。价格、系数、正式标准、项目口径、冲突项及全部项目／任务候选始终人工确认。

捕获失败不得回滚改单或复核结论，但 API／任务审计必须明确返回可重试警告，禁止静默丢失。先补测试并保存红灯，再实现到绿；反向验证重复事件、跨项目检索、数据库不可用三种失败。

## 任务 2：胶囊、晋级、命中与真实指标

为已完成项目提供显式“生成／刷新项目记忆胶囊”：只汇总项目台账、Skill快照、成果版本、已确认知识、候选／失效项和复核意见的受控引用，不复制文件与聊天，不直接灌入模型。新增事件、胶囊、真实指标 API；指标只数候选来源、确认／驳回／撤销、检索命中、版本更正和疑似失效，不估算节省工时。知识问答命中需记录知识ID、来源项目、目标项目和时间；正式依据仍先于记忆展示。

用隔离夹具和脚本跑通并机器断言：项目A改单／复核意见 → 候选 → 人工确认 → 提升通用候选且再次人工确认 → 项目B带来源命中 → 查看审计 → 撤销后再次查询不命中。不得直接插入“已确认”终态冒充流程。

## 任务 3：在现有知识记忆工作区交付统一 UI

不新增一级页面。在现有知识记忆弹窗内用项目既有分段控件组织“知识记录｜项目胶囊｜进化成效”；知识详情增加事件来源、项目／任务、Skill、Sheet行字段、版本哈希和审计入口，胶囊与指标均显示真实空态。使用现有 Lucide、`--dws-*`、白底、1px边框、6—8px圆角，无常驻阴影、无渐变、无卡片套娃，所有样式收口在本模块根类。

字体是硬门禁：统一自托管苹方；页面／模块／面板／正文／控件／辅助说明只用 `28/20/16/14/13/12px` 对应 token，可见中文不得小于12px，禁用9/10px，11px仅限带注释的图表坐标或纯数字标记。同层按钮、标签、人员和说明必须同字号、字重、行高；表单继承相同字体。交付前静态扫描本次 CSS，并在实际运行的 1366、1440、1920 及窄弹窗宽度核对 computed `font-family/font-size/font-weight/line-height`、焦点、滚动、空态、深浅色和控制台；截图存 `Codex-Temp/visual-qa/trusted-experience/`。

## 规矩

不新增依赖、权限、网络流程或外部消息；确需扩大白名单先写 `BLOCKED.md`。禁止伪造项目、进度、复核结论和指标，禁止 `|| true`、删测试、改阈值或用 mock 替代端到端状态。每项连续失败3次就换下一项并记录；结果比基线差则回滚本项。完成后更新 P1 状态、README、当前版本计划、CHANGELOG和全部版本号，运行 `python tools/trim_changelog.py`；PRD检查使用 `python tools/check_prd_consistency.py --no-write`，避免覆盖既有巡检文件；本地提交只包含白名单文件。

## 完成条件

硬指标一：上述项目A→项目B→撤销链路由真实 API／SQLite 状态跑通，重复候选为0、撤销后跨项目命中为0、正式资产改动为0。
硬指标二：专项测试不少于73项、前端不少于60项、后端收集不少于507项且全量回归无新失败；前端构建、UI 15项检查、PRD普通巡检、浏览器字体与多宽度验收全部通过。

最终必须实际运行 `python -m pytest backend/tests -v`、`node --test frontend/tests/*.test.ts`、`npm run frontend:build`、`python tools/check_ui_design_system.py`、`python tools/check_prd_consistency.py --no-write` 和 `git diff --check`。每条都要在最终对话贴命令输出，包含新增测试红→绿、跨项目阻断与撤销停止命中的反向证据；只说“完成”不算。提交本任务 `BLOCKED.md`，无阻塞也写“无”。最多执行6轮；满轮即停，如实说明卡点和剩余工作。
