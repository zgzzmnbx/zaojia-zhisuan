# 跨模块专业数据可视化实施进度

> 更新时间：2026-08-13
> 当前状态：`VIS-01`—`VIS-15` 已完成；自动化、V3.1 正式成果对比与浏览器验收通过，本文件随最终本地 Git 存档保存

## 完成结果

- 已建立共享 `ChartFrame / ChartState / chartTheme / chartFormatters / chartAccessibility`，统一完整状态、格式化、键盘语义和隐藏表格摘要。
- 已在原责任页面增量实现 15 张专业图形，没有建立第二套 Dashboard，没有删除或替换项目看板既有 KPI、八项图形、五阶段漏斗、历史表、筛选或汇报模式。
- 正式金额、风险、候选、阈值、Task、复核、知识治理与 Skill 状态继续来自原业务模块；前端只做确定性显示变换。结算后端只补充同次审核明细聚合出的向后兼容 `sheet_summaries`。
- 当前真实数据不足以形成多版本趋势、旧轮次或工作量结果时，页面使用不足态 / 当前轮 / 未运行态，不向生产台账注入夹具或演示事实。
- 2026-08-13 已先完成本地 Git 检查点 `c48d421`，只保存既有 Task 导航与流程引导调整；用户测试资料、界面截图、企业 VI 与输出目录保持未跟踪且未触碰。

## 图形验收台账

| 编号 | 图形 | 状态 | 自动化 / 数据对账 | 浏览器与交互证据 |
| --- | --- | --- | --- | --- |
| VIS-01 | Task 真实执行轨迹 | 已完成 | 复用真实 `TaskEvent`，完成事件和实际 Tool 分开显示 | Task 详情 1440px 验收，无横向溢出 |
| VIS-02 | 成果—经验血缘图 | 已完成 | Task→Run / ReviewRound→Artifact→Experience 状态来源于模块 19 | Task 详情可见血缘关系和安全入口 |
| VIS-03 | 偏离率—样本数散点图 | 已完成 | 使用真实样本数、绝对偏离率与本次阈值 | 真实 2 条预警，深浅色与窄容器通过 |
| VIS-04 | 经验区间子弹图 | 已完成 | 当前值超范围时自动扩展坐标域，不裁点 | 真实当前值、均值、范围与样本数对账通过 |
| VIS-05 | AI填价候选价格点图 | 已完成 | 保持程序稳定排序，不生成候选外数值 | 点选 `3203` 后仅联动第 3 个单选项，仍需原“采用并写入” |
| VIS-06 | 结果预览行风险迷你地图 | 已完成 | 主状态优先级固定，原始多状态不丢失 | 真实 11 / 18 行地图、Sheet 与 Excel 行定位通过 |
| VIS-07 | 结算金额瀑布图 | 已完成 | `461,877.02 − 32,372.00 = 429,505.02`，支持增加 / 审减方向 | 1440px 真实演示审核结果，无溢出 |
| VIS-08 | 分 Sheet 风险小多图 | 已完成 | `sheet_summaries` 由同次 8 条真实风险聚合，统一最大值 | 4 个 Sheet 同尺度显示通过 |
| VIS-09 | 工作量抓取分组条形图 | 已完成 | 填写、覆盖、保守跳过、未匹配、一对多、预警保持独立计数 | 当前未上传工作量表时诚实不显示结果图；隔离夹具覆盖完成态 |
| VIS-10 | 复核人状态矩阵 | 已完成 | 平台、轮次、投递、复核、意见和更新时间来自真实任务状态 | 专业能力中心同屏真实外部任务状态通过 |
| VIS-11 | 复核轮次阶段带 | 已完成 | 只显示真实轮次；旧轮次不存在时不伪造历史 | 当前轮状态与成果摘要诚实显示 |
| VIS-12 | 可信经验来源 / 治理双条图 | 已完成 | 来源、确认 / 驳回 / 撤销与命中 / 纠偏 / 失效分开计数 | 仅在真实指标存在的知识记忆上下文显示 |
| VIS-13 | 项目版本阶段趋势 | 已完成 | 数量与金额上下分图；少于 2 个真实版本显示不足态 | 项目详情 / Task 详情 1440px 通过，真实 1 版本显示不足 |
| VIS-14 | RAG 检索通道并列图 | 已完成 | 使用真实 debug trace；经典 / 混合分开，并行召回不画漏斗 | 仅调试信息中渲染；普通问答不出现 |
| VIS-15 | 专业 Skill 能力矩阵 | 已完成 | Registry / Manifest 真实状态，测试 Skill 隐藏 | 能力中心 1440px 通过；规划项不可创建任务 |

## 自动化与正式业务验收

- 前端可视化专项：`6 passed`。
- 前端全量：`78 passed`；项目看板防减少断言覆盖既有八项图形、五阶段漏斗、KPI、历史表与筛选入口。
- 后端全量：`527 passed, 5 skipped`。
- 前端生产构建：通过；仅保留既有 chunk-size 提示。
- UI 设计系统检查：通过；无 9px / 10px 可见文字、渐变或常驻阴影。
- PRD 严格一致性：`0` 个问题模块、`0` 个缺失关联资产。
- V3.1 正式验收：Excel 6 张共同 Sheet、1254 个目标单元格；三个数字、公式、目标填值、复核状态、隐藏行与全值差异均为 `0`。Word 封面（日期除外）和业务核心差异为 `0`，风险事实无遗漏。
- V3.1 证据：`Codex-Temp/acceptance/task-p0-v31/`。

## 浏览器与 UI 验收

- 项目看板在 1366 / 1440 / 1920px 下保持现有图形和历史表，正文无横向溢出。
- 新图完成 1440px 真实数据检查；经验预警完成浅色、暗色和 328px 窄工作区检查；候选点图、行风险地图、Task 血缘、项目版本、Skill 矩阵、结算瀑布和分 Sheet 风险完成交互检查。
- computed 字体为自托管 `Zaojia PingFang SC`；图形标题为 16px / 600，坐标 11px 仅用于有注释的纯数字技术标记。
- 修复隐藏可访问表格造成的横向溢出后，1440px 与 328px 图形容器均无溢出。
- 浏览器控制台：`0 error / 0 warning`，无 Recharts 零尺寸警告。
- 截图证据：`Codex-Temp/acceptance/data-visualization-browser/`。

## 保护边界

- 不改变基价 / 单价、两个调整系数、风险、结算、协同、知识和报告裁决。
- 不自动确认候选、不自动发送消息、不部署、不 push、不打包代码版 zip。
- 最终暂存使用任务文件白名单，不使用 `git add .`；未跟踪用户资料、截图、VI、状态面板实验与输出目录继续排除。
