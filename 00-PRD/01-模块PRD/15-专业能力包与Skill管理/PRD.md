# 专业能力包与 Skill 管理 PRD

## 模块目标

把“造价智算”从单一勘察测量场景的软件原型，升级为可装载不同造价专业能力的智能体底座。用户开始业务前选择“专业能力（Skill）”，系统据此确定本次任务使用的输入规范、结构化计价库、规则、风险检查、知识资料、报告模板和验收口径。

当前长输管道勘察测量最高投标限价编制能力作为首个已登记专业 Skill。v5.10.0 已完成 P0-A 能力选择入口、Skill 清单、安全校验和任务元数据快照，未移动现有正式资产、未改变现有计算结果；但实际处理仍固定使用全局计价库、FillEngine、规则和报告模板，尚未由 Skill 动态装载完整专业能力包。下一步先完成 P0-B 运行时路由，再以“通用服务类造价测算”验证第二个真实专业 Skill。

对外产品口径：

> 造价智算不是一个勘察测量填价工具，而是一套可装载专业 Skill 的工程造价智能体底座；勘察测量最高投标限价编制，是首个经过完整验证的专业 Skill。

## 需求清单

| 状态 | 需求 | 说明 | 验收口径 |
| --- | --- | --- | --- |
| [已完成] | P0：选择专业能力入口 | 在全局导航栏提供默认折叠的“专业能力”入口，点击后展开专业能力选择菜单 | 默认选中勘察测量 Skill；用户能查看、切换和确认，不新增第二套主流程 |
| [已完成] | P0：勘察测量 Skill 清单化 | 用结构化 Manifest 登记当前场景的名称、版本、能力、资产引用、适用输入和验证状态 | Manifest 只引用现有资产，不搬动知识库、规则表、模板和样例，不改变当前路径 |
| [已完成] | P0：当前 Skill 状态可见 | 全局导航栏持续显示当前专业能力、版本和任务快照锁定状态 | 用户在上传、预览、报告等关键阶段都能确认本次任务使用的专业 Skill |
| [已完成] | P0：默认兼容与安全降级 | 未显式传入 Skill 的旧请求继续使用当前勘察测量能力 | 现有开发版、绿色版、Tauri、云端和 WeAct 专业任务保持兼容；Manifest 异常时拒绝错误切换并回退当前默认能力 |
| [进行中] | P0：任务隔离与可追溯 | v5.10.0 已记录 Skill ID、版本、Manifest 哈希和资产标识；实际运行资产仍是全局固定实现 | 元数据追溯已完成；必须通过两个可执行 Skill 验证计价库、规则、知识、风险和模板不串用后才能标记完成 |
| [已完成] | P0：评委展示与真实状态 | 能力菜单展示已上线、规划中 / 内测中、即将开放三层状态 | 勘察测量标记“已上线”；通用服务类标记“规划中”；更多造价专业标记“即将开放”，后两者均不能创建任务 |
| [待开发] | P0-B：完整专业能力包运行时路由 | 建立 `SkillRuntimeContext`，让输入规范、处理器、计价库、规则、风险、知识、报告模板和验证资产真实参与运行 | `/api/process`、批量匹配、预警、问答、报告和下载不再只依赖全局默认路径；处理器只能使用代码白名单；测试专用第二 Skill 证明真实切换和隔离 |
| [待开发] | P1：通用服务类真实 Skill | 建立独立输入、计价依据、规则、风险检查、报告模板和样例，跑通最小业务闭环 | 与勘察测量 Skill 使用同一底座但专业资产隔离；至少完成一组确定性样例回归 |
| [待开发] | P1：Skill 推荐与开放格式包装 | 根据上传文件特征推荐专业能力，并为专业能力包提供 `SKILL.md` 描述 | 推荐只做提示，必须由用户确认；程序不得从自然语言 `SKILL.md` 解析价格规则 |
| [暂缓] | P2：Skill 管理中心与能力市场 | 支持安装、启停、升级、回滚、权限、签名、导入导出和内部能力市场 | 生产化前必须补可信发布、兼容性校验、权限审计和供应链安全，不执行任意外部脚本 |

## v5.10.0 P0-A 实现状态与实际边界

- P0-A 已于 2026-07-20 完成。前端将宽度受控的“专业能力”入口放入全局导航栏并默认折叠；点击后以三行能力菜单展示已上线、规划中和即将开放三层卡片及详情，导航入口持续显示当前选择或已锁定任务快照。
- 能力详情通过 Manifest 的 `subSkills` 展示能力包内部组成，逐项区分“专业专用 / 通用能力复用”和“已启用 / 规划中”；子 Skill 只描述可审计能力模块，不代表新增可独立执行的插件。
- 后端新增独立 Skill Registry，校验 Manifest 完整性、状态、项目内相对路径、资产存在性、路径越界、可执行入口和运行秘密；未知、规划中、停用、版本不匹配或不完整能力均不能创建真实任务。
- `process-state.json` 固化 Skill ID、版本、Manifest SHA-256 摘要、创建时间和资产标识；处理、预览、预警、风险、报告和下载按该任务快照追溯。
- 现有勘察测量结构化计价库、三数字匹配引擎、规则表、经验池和报告模板未改动。真实 100 行样例的旧请求与显式 Skill 请求已逐工作表、逐单元格对比一致。
- 当前快照只承担登记、校验和追溯：`/api/process` 仍固定读取 `DEFAULT_KB_PATH` 并调用同一 `FillEngine`，报告仍使用默认模板，Manifest 中的专业资产尚未形成真实运行时装载。对外不得将 P0-A 表述为已经实现多专业执行。
- 绿色版构建显式携带 `business-skills/`；Tauri 发布复用绿色版阶段目录，因此使用同一份非秘密能力清单。

## 关联资产

| 类型 | 文件 | 用途 |
| --- | --- | --- |
| 产品总览 | `00-PRD/00-产品总览.md` | 统一“一套底座、多个专业 Skill”的产品定位和推广路线 |
| 当前计划 | `00-PRD/02-当前版本计划.md` | 记录 P0 优先级、边界和下一步实施顺序 |
| UI 总纲 | `00-PRD/03-整体UI设计PRD.md` | 约束选择入口、能力卡片、状态标签和主工作区布局 |
| UI 规范 | `00-PRD/03-UI设计规范.md` | 约束颜色、字号、边框、按钮、弹窗和响应式行为 |
| 项目默认配置 | `config/project-default-settings.json` | 登记默认 Skill ID 和非秘密的能力选择默认值 |
| 专业能力包 | `business-skills/` | 保存已上线勘察测量 Manifest、说明、引用说明和验收说明，以及通用服务类规划卡片和更多造价专业即将开放卡片 |
| 宿主前端 | `frontend/src/App.tsx` | P0 选择入口、当前 Skill 状态和任务请求上下文的现有承载位置 |
| 宿主后端 | `backend/app/main.py` | Skill 清单接口、任务上下文校验和现有业务路由的薄接入位置 |
| Skill Registry | `backend/app/professional_skills.py` | Manifest 读取、安全校验、公开清单和任务快照生成 |
| 路径定义 | `backend/app/paths.py` | 集中定义专业能力包目录和 Manifest 读取位置 |
| 结构化计价库 | `03-知识库-二维数据库制作/【数据库】【导入】.xlsx` | 勘察测量 Skill 当前基价和第二层经验提示资产，只引用不移动 |
| 核心规则说明 | `03-【匹配规则】-勘察测绘知识库-匹配规则提炼/【重要匹配规则】项目以及总体匹配规则介绍.md` | 勘察测量 Skill 的三个数字来源和规则边界 |
| 规则资产 | `backend/app/rules/` | 勘察测量 Skill 当前结构化规则目录，只引用不批量重组 |
| 报告模板 | `03-知识库-二维数据库制作/01-报告模板-招标控制价报告模板/【模板勿动】控制价报告模板-yyyy-mm-dd.docx` | 勘察测量 Skill 当前 Word 成果模板 |
| 知识记忆 PRD | `00-PRD/01-模块PRD/04-问问智算AI助手/PRD.md` | 约束通用知识与同项目知识的检索范围，不因切换 Skill 越界 |
| 智能协同 PRD | `00-PRD/01-模块PRD/13-飞书连接的场景智能体/PRD.md` | 约束 WeAct 任务必须携带并保持专业 Skill 上下文 |
| 对话式工作台 PRD | `00-PRD/01-模块PRD/16-对话式智能体工作台/PRD.md` | 约束会话底部 Skill 选择必须绑定真实运行时上下文，不得只展示标签 |

## 产品概念与命名

### 用户看到的名称

- 一级名称：`选择专业能力`。
- 辅助标签：`Skill`。
- 当前状态：`当前专业能力：勘察测量最高投标限价编制`。
- 不建议把入口直接命名为“选库”，因为用户选择的不只是数据表。
- 不建议只展示英文“Skill”，避免普通业务人员不理解。

### 产品内部定义

一个专业 Skill 是完整的业务能力包：

```text
专业 Skill
＝业务场景
＋输入规范
＋结构化计价库
＋规则包
＋字段映射
＋风险检查
＋知识资料
＋报告模板
＋标准流程
＋验收用例
```

### Skill、工具与资产的边界

| 对象 | 产品定位 | 是否直接称为 Skill |
| --- | --- | --- |
| 勘察测量最高投标限价完整业务闭环 | 用户可选择的专业能力包 | 是，首个专业 Skill |
| 通用服务类完整测算闭环 | 第二个横向扩展能力包 | 是，P1 专业 Skill |
| 结构化计价库 | 专业数据资产 | 否，由专业 Skill 引用 |
| 确定性匹配引擎 | 通用业务工具或服务 | 否，由专业 Skill 编排调用 |
| 调整系数规则表 | 可审计规则资产 | 否，由专业 Skill 引用 |
| Word 模板 | 成果资源 | 否，作为 Skill 资产 |
| 问问智算与知识记忆 | 跨专业共享智能能力 | 可作为通用能力 Skill，但不取代正式依据 |
| 智能协同与 WeAct | 协同流程加外部连接能力 | 更接近 Skill 与连接器组合的 Plugin |

## 功能边界

| 范围 | 本模块负责 | 本模块不负责 |
| --- | --- | --- |
| 用户入口 | 专业能力选择、详情查看、状态展示和当前能力标识 | 重做现有填价、预警、报告和问答页面 |
| 能力登记 | 通过 Manifest 登记 Skill 元数据、资产引用、版本和验证状态 | 在自然语言描述中维护可执行价格规则 |
| 任务上下文 | 把 Skill ID、版本和关键资产标识固化到新任务 | 切换或改写已经创建任务的专业上下文 |
| 资产治理 | 引用现有结构化计价库、规则表、知识资料和模板 | P0 搬迁、复制或重组现有正式资产 |
| 横向扩展 | 为第二个真实专业 Skill 提供统一装载协议 | 在通用服务类业务闭环未验证前伪装成已上线能力 |
| 平台生态 | P0 预留可扩展结构，P2 再建设管理中心 | P0 安装第三方包、运行任意脚本或开放能力市场 |

## 总体架构

```text
造价智算通用底座
├─ Excel 解析、字段映射和任务状态
├─ 确定性规则执行与人工复核
├─ 经验池、风险清单和依据追溯
├─ 问问智算、知识记忆和报告生成
├─ 智能协同与成果分发
└─ 专业 Skill 注册、选择和隔离
   ├─ 勘察测量最高投标限价 Skill（P0-A 已登记，P0-B 待运行时装载）
   ├─ 通用服务类造价测算 Skill（P1）
   └─ 其他造价专业 Skill（P2 储备）
```

通用底座保持稳定；不同专业 Skill 只装载自己的专业输入、数据、规则、模板和验收资产。共享能力可以复用，但不得用共享经验替代专业正式规则。

## 专业能力包结构

P0 推荐新增业务能力包目录，不立即迁移现有真实资产：

```text
business-skills/
└─ survey-measurement-limit-price/
   ├─ manifest.json
   ├─ SKILL.md
   ├─ references/
   ├─ assets/
   └─ evals/
```

- `manifest.json`：造价智算程序确定性读取的运行清单，保存 ID、版本、状态、能力开关和现有资产相对路径。
- `SKILL.md`：面向智能体和开发人员的工作说明，描述适用场景、所需输入、执行步骤、输出要求、边界和检查项。
- `references/`：按需放置专业说明或对现有正式资料的引用说明。
- `assets/`：只放该 Skill 新增且不存在稳定外部路径的模板或静态资产；P0 不复制现有结构化计价库和正式规则。
- `evals/`：保存专业 Skill 的样例清单、预期行为和回归说明。

P0 的 `SKILL.md` 参考 Agent Skills 开放格式，但程序不得把 Markdown 自然语言当成价格和系数裁决依据。所有机器运行参数必须来自结构化 Manifest、正式规则和确定性代码。

外部规范参考：

- [Agent Skills Specification](https://agentskills.io/specification)
- [OpenAI Skills](https://help.openai.com/en/articles/20001066)
- [OpenAI Plugins in Codex](https://help.openai.com/en/articles/20001256-plugins-in-codex/)

## P0-A 详细方案：选择专业能力

### P0-A 目标

P0-A 只解决“产品架势、能力清单、元数据上下文和默认兼容”，不代表已经根据 Manifest 动态装载专业计价库、规则、知识、风险和报告模板。

P0-A 完成后，评委和用户应明确感知：

1. 造价智算是一套可扩展底座，不是只服务一张勘察测量表。
2. 当前真正上线的是勘察测量最高投标限价 Skill。
3. 未来新增专业场景时，通过新增能力包扩展，而不是复制整套前后端。
4. 每次任务明确记录使用了哪个 Skill、哪个版本和哪些专业资产。

### 入口位置

主入口放在全局导航栏，所有模块共用，不占用填价工作台上传和结果区域：

```text
专业能力 · 勘察测量最高投标限价编制 · v1.0       [展开]

展开后：选择能力包                                 [当前能力详情]

[勘察测量最高投标限价编制]     已上线       当前比赛验证场景
[通用服务类造价测算]           规划中       下一项横向扩展能力
[更多造价专业]                 即将开放     通过专业能力持续扩充

新任务将使用：勘察测量最高投标限价编制 · v1.0
```

交互要求：

- 只有一个可用 Skill 时默认选中，但选择入口仍可见。
- 用户点击已上线能力卡后查看详情并确认。
- 规划中能力卡不可进入业务主流程，只展示规划说明。
- 用户选定后，在全局导航栏显示紧凑的“专业能力 + 当前能力名称 + 版本”状态；已有任务同时显示低权重的锁定状态。
- 已选择能力使用浅色低权重状态标识并取消重复点击；未选择且可用的能力才显示蓝底白字“选择此能力”主操作。
- 不要求用户在每个页面重复选择；同一任务生命周期保持不变。
- 切换专业能力前，如已有未完成任务，必须提示“仅影响后续新任务”，不得改变已创建任务。

### 能力详情

P0 能力详情至少展示：

- Skill 名称、ID、版本、状态。
- 适用业务和不适用范围。
- 支持的输入文件类型和关键模板特征。
- 已启用能力：字段映射、三数字匹配、经验池预警、报告、问答、协同。
- 包含的子 Skill：名称、用途、专业专用 / 通用复用属性和真实启用状态。
- 已加载专业资产数量与名称摘要。
- 最后验证时间、验证样例和已知限制。
- “规则裁决、模型解释、人工兜底”边界说明。

不得展示 App Secret、API Key、服务器内部路径或其他运行秘密。

### 专业能力卡状态

| 状态 | 行为 | 展示要求 |
| --- | --- | --- |
| 已上线 | 可选择并创建任务 | 显示版本、验证状态和主要能力 |
| 内测中 | 仅授权试用或样例验证 | 明确不是正式生产能力 |
| 规划中 | 不可创建真实任务 | 只展示方向和预计包含的资产，不伪装为已实现 |
| 已停用 | 禁止创建新任务 | 历史任务仍可按原 Skill 版本追溯 |

## P0 Manifest 设计

P0 Manifest 至少包含：

```json
{
  "id": "survey-measurement-limit-price",
  "displayName": "勘察测量最高投标限价编制",
  "version": "1.0.0",
  "status": "active",
  "domain": "工程造价/勘察测量",
  "description": "长输管道勘察测量最高投标限价编制专业能力",
  "inputProfile": {
    "extensions": [".xlsx"],
    "templateHints": []
  },
  "capabilities": {
    "pricing": true,
    "workloadCapture": true,
    "experienceWarning": true,
    "knowledgeQa": true,
    "knowledgeMemory": true,
    "wordReport": true,
    "collaboration": true
  },
  "assets": {
    "knowledgeBase": "现有结构化计价库相对路径",
    "rules": [],
    "reportTemplate": "现有报告模板相对路径"
  },
  "validation": {
    "status": "verified",
    "sample": "当前100行空单价输入表",
    "updatedAt": "待实现时填写"
  }
}
```

实际字段可在开发前结合现有后端收敛，但必须满足：

- 全部资产路径使用项目相对路径或跨平台配置，不写死本机盘符。
- Manifest 不保存密钥和用户隐私。
- Manifest 读取失败时给出明确错误，不静默加载半套规则。
- `status` 不是装饰字段，后端必须禁止使用规划中或停用 Skill 创建任务。
- P0 不允许 Manifest 覆盖任意 Python 模块或执行外部命令。

## P0 运行流程

```text
进入填价工作台
→ 读取专业 Skill 清单
→ 默认选择勘察测量 Skill
→ 用户查看或确认
→ 上传 Excel
→ 前端把 skill_id 和 skill_version 传入任务请求
→ 后端校验 Skill 状态和 Manifest
→ 生成任务级 Skill 快照
→ 现有勘察测量处理链按原逻辑运行
→ 预览、报告、下载和协同沿用同一任务 Skill 上下文
```

任务创建后必须以任务快照为准。即使默认 Skill 后续升级，历史任务仍显示创建时的 Skill ID 和版本，不被新版本静默改写。

## P0-A 兼容与隔离

### 向后兼容

- 旧前端或 WeAct 未传 `skill_id` 时，后端使用项目默认的勘察测量 Skill。
- 默认 Skill 的现有输入、输出、匹配、预警和报告结果必须保持一致。
- P0-A 不修改三个数字的匹配顺序、结构化计价库内容或规则表内容。
- P0-A 不移动正式资产，不批量重命名历史“管勘智算”路径。

### 隔离要求

- 专业 Skill 只决定后续新任务的资产装载和能力开关。
- 已创建任务不能因用户切换 Skill 被改变。
- 知识记忆中的项目范围与专业 Skill 范围是两个维度；P0-A 不自动把某个项目知识提升为跨 Skill 正式规则。
- 通用知识可跨专业辅助解释，但不得覆盖当前 Skill 的正式规则和结构化计价库。
- 经验池、任务缓存、报告模板和未来专业知识库必须具备 Skill 维度，第二个 Skill 上线前完成串用防护。

## P0-B：完整专业能力包运行时路由

P0-B 负责把“选择了哪个 Skill”从任务标签升级为真实运行上下文。建议建立只接受代码白名单处理器的 `SkillRuntimeContext`：

```text
SkillRuntimeContext
├─ input_profile
├─ processor_id
├─ knowledge_base_path
├─ rule_assets
├─ risk_profile
├─ knowledge_sources
├─ report_template_path
└─ validation_profile
```

实施要求：

1. `processor_id` 只能映射到代码内登记的可信处理策略，Manifest 不得提供 Python 模块、命令或脚本路径。
2. `/api/process`、批量匹配、经验池预警、风险、知识问答、报告和成果下载使用任务创建时冻结的运行时上下文。
3. 现有勘察测量逻辑先包装为白名单策略，不能复制第二份 `FillEngine` 或修改三个数字口径。
4. 报告生成接收当前 Skill 的模板路径，不再只读取全局 `REPORT_TEMPLATE_PATH`。
5. 知识检索和风险检查具有 Skill 作用域，通用知识可以复用，但专业正式资产不得跨 Skill 串用。
6. 使用测试专用第二 Skill 和独立夹具验证不同计价库、规则、模板、知识和输出真正切换；该测试能力不作为用户可见的已上线业务。
7. P0-B 完成后，再使用通用服务类真实 Skill 做第二轮业务验收，才能声称底座具备完整专业能力包装载能力。

## P0-A / P0-B 非功能要求

- 首屏能力清单读取应为轻量本地操作，不因读取大量知识库拖慢页面。
- 能力选择失败不影响用户查看现有页面，但必须阻止使用未知或不完整 Skill 开始专业计算。
- 所有新增接口返回结构化错误，不向用户暴露绝对路径和堆栈。
- 绿色版、Tauri 和云端均从同一份非秘密 Skill 清单读取能力状态。
- UI 延续大尾巴主题和 Navattic 极简 SaaS 风格，不增加高饱和“应用商店”视觉。
- 选择入口支持键盘操作、焦点状态和无障碍标签。

## P1：第二个真实专业 Skill

“通用服务类造价测算”作为第二个横向验证能力，不能只复制一张卡片。P1 至少需要：

1. 独立业务定义和适用范围。
2. 独立输入模板与字段模型。
3. 独立计价依据、结构化数据和确定性规则。
4. 独立风险检查与人工复核点。
5. 独立 Excel / Word 成果模板。
6. 独立样例、预期答案和回归测试。
7. 与勘察测量 Skill 的资产隔离验证。
8. 使用同一选择入口、任务框架、问答、知识记忆、报告和协同底座。

只有最小闭环真实跑通后，卡片状态才能从“规划中”改为“内测中”或“已上线”。

## P2：Skill 管理与生态

P2 再考虑：

- Skill 安装、启用、停用和卸载。
- 版本升级、兼容性检查和回滚。
- 组织、角色和专业授权。
- Skill 签名、发布审批和供应链安全。
- Agent Skills 格式导入导出。
- 单位内部专业能力市场。
- 根据文件特征、用户角色和项目类型推荐 Skill。
- Skill 使用统计、命中率、人工复核率和质量评估。

P2 不允许用户上传任意 Skill 后直接执行其中脚本。外部包必须经过白名单、签名、权限和人工发布审核。

## 验收口径

以下前 11 项为 P0-A 选择与登记骨架验收口径；最后 4 项为 P0-B 真实运行时路由验收口径：

- 全局导航栏始终可见默认折叠的“专业能力”入口；点击后显示三行能力菜单，不占用上传和结果区域。
- 勘察测量最高投标限价编制显示为“已上线”且默认选中；通用服务类显示为“规划中”；更多造价专业显示为“即将开放”。
- 用户能打开能力详情，看到适用范围、版本、能力、资产摘要、验证状态和边界。
- 当前专业能力、版本和任务锁定状态在全局导航栏持续可见，切换只影响后续新任务。
- 已选择能力使用清晰可读、不可重复点击的浅色状态标识；可切换能力才使用主操作按钮。
- 前端任务请求、后端任务状态和下载 / 报告元数据可追溯 Skill ID 与版本。
- 未传 Skill 的旧请求继续运行当前勘察测量流程，100 行关键样例输出与改造前逐行一致。
- 规划中、停用、未知、Manifest 不完整或路径越界的 Skill 不能创建任务。
- P0-A / P0-B 不移动、不覆盖、不批量重命名现有知识库、规则表、经验池、模板和样例。
- 问问智算、知识记忆、智能协同、绿色版、Tauri 和云端行为不因 P0 退化。
- `SKILL.md` 只承载工作说明；任何价格、系数和风险等级仍由结构化规则和正式资产确定。
- P0-B 的 `SkillRuntimeContext` 从已校验 Manifest 生成，并冻结到任务；所有后续动作使用同一上下文。
- 计价库、规则、风险、知识源和报告模板能随测试专用 Skill 真实切换，不再只读取全局默认路径。
- 处理器只能使用代码白名单，不允许 Manifest 声明任意模块、命令或脚本。
- 测试专用第二 Skill、当前勘察测量 Skill 和旧请求均通过隔离与回归；完成前“任务隔离与可追溯”保持 `[进行中]`。

## 开发前置与验证要求

进入 P0-B 代码开发前：

1. 先记录当前 100 行样例、知识检索和项目样例的真实基线。
2. 先修复或记录当前唯一已知回归：正式标准资料目录改名后没有进入知识索引；新增 Skill 运行时不得扩大失败范围。
3. 先检查 `frontend/src/App.tsx`、`backend/app/main.py` 和当前工作区 diff，采用增量式接入。
4. 复用现有 Skill Registry，优先新建独立运行时上下文和白名单处理器注册模块，避免把装载逻辑继续堆入 `main.py`。

至少验证：

```powershell
python -m pytest backend/tests -v
npm run frontend:build
python tools/check_prd_consistency.py --strict
git diff --check
```

新增专项测试至少覆盖：Manifest 合法性、默认兼容、状态禁用、路径越界、任务快照、白名单处理器、不同计价库 / 规则 / 知识 / 报告模板真实切换、切换隔离、未知 Skill、绿色版携带和当前 100 行样例不回归。

## 本阶段不做

- P0-B 不开发通用服务类真实计价规则，只使用测试夹具证明运行时路由，通用服务类继续保留诚实的规划状态。
- P0-B 不建设 Skill 商店、在线下载、任意脚本执行或外部包自动安装。
- P0-B 不搬迁现有勘察测量结构化计价库、规则表、经验池和报告模板。
- P0-B 不允许大模型根据 `SKILL.md` 自由生成价格、系数、规则或风险等级。
- P0-B 不把功能点助手业务代码纳入本专业 Skill 目录；数字化项目助手继续保持独立子项目边界。
- P0-B 不把“选择专业能力”新增为左侧一级功能页；它是开始任务前的上下文选择能力。
