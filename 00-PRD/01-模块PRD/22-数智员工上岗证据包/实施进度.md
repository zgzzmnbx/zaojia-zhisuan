# 数智员工上岗证据包实施进度

## 当前状态

- 版本：v5.23.0。
- 状态：P0 已完成，本地完成，未部署。
- 正式入口：全局“专业能力” → “专业能力中心” → “数智员工上岗证据”。
- 实施原则：只读聚合、事实同源、失败降级、人工负责，不新增状态机或左侧菜单。

## 已完成

1. 新增安全只读证据聚合，复用 Registry、Manifest、Task、TaskEvent、成果、复核、可信经验和 V3.1 验证事实。
2. 已上线 Skill 展示“能上岗、能干活、能成长、不越权”的真实证据；规划中 Skill 显示不足态且不可创建任务。
3. Task 详情可展开冻结 Skill、实际 Tool、成果下载、复核轮次和 Experience 血缘；状态不混淆。
4. 页面与 Markdown 导出使用同一个证据响应，包含数据来源、完成证据、事实边界、不足项和生产化免责声明。
5. 输出过滤绝对路径、Manifest 哈希、平台 ID、秘密和个人敏感信息；聚合源异常只降级本模块。
6. UI 使用局部样式，已覆盖深浅主题、键盘关闭、减少动态效果、宽屏和窄屏。

## 验证记录

- 后端专项：数智员工证据、专业 Skill、业务 Task；后端全量 `536 passed, 5 skipped`。因其他任务已将结算演示样例改名，全量测试通过仅在测试进程内把旧测试常量映射到现有 `-v0.1.xlsx`，未复制、覆盖或提交该资产。
- 前端专项与全量：证据契约、同源 Markdown、状态标签、静态 UI 门禁及既有全部前端测试，`96 passed`；v5.23.0 生产构建通过。
- V3.1 正式回归：6 张共同工作表、1254 个目标单元格；三个数字、公式、颜色、复核状态、隐藏行和全值差异均为 0；Word 封面（日期除外）和业务核心差异为 0，业务内容通过。
- 浏览器：已上线 / 规划中、有 Task / 无 Task、导出、深浅色、键盘、1366 / 1440 / 1920 及窄屏无页面级横向溢出；功能打开前后无新增控制台错误或警告。

## 尚未完成

- 生产级统一身份、组织岗位授权、企业审批和跨实例可信证明。
- 第二个真实 Registry 专业 Skill 的独立业务资料与完整验收。
- PDF 不作为 P0 必选；当前优先交付零大型依赖的 Markdown 证据包。
