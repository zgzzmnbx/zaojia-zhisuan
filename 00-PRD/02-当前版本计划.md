# 当前版本计划

## 1. 文档用途

本文件只维护当前基线、最近完成事项、下一步优先级和已知阻塞。历史版本流水以 `CHANGELOG.md` 和 `CHANGELOG - 【归档】.md` 为准；模块长期需求以 `00-PRD/01-模块PRD/*/PRD.md` 为准。

## 2. 当前基线

- 产品名称：造价智算。
- 产品定位：工程造价辅助智能体。
- 当前版本：`v5.8.9`。
- 核心场景：长输管道勘察测量最高投标限价编制。
- 核心方法：规则裁决、模型解释、人工兜底。
- 当前主线：稳定专业计价闭环，补齐可信复核，再推进协同生产化。

## 3. 当前能力状态

| 能力域 | 当前状态 | 仍需补齐 |
| --- | --- | --- |
| Excel 填价与三数字匹配 | 主流程已可用，结构化计价库、第一层规则和第二层经验提示分层运行 | 结构化计价库只读质量检测、两个既有回归基线问题 |
| 结果预览与成果输出 | Excel 预览、人工修改、下载、Word 生成和真实 DOCX 预览已完成 | 持续做真实样例稳定性回归，不扩张新流程 |
| 经验池与可信复核 | 经验池预警、治理报告、风险清单、轻量追溯和基价辅助填价 MVP 已完成 | 两个调整系数候选、分字段确认、完整出处映射 |
| 问问智算 | 普通问答、强制知识库、行级解释、风险报告和飞书知识库问答已完成 | 回答等待体验、标准资料检索排序、完整依据出处 |
| 智能协同 | Webhook、长连接收件、自动处理、成果回传、双机器人与企业 WeAct 接入已完成 | 常驻监控、权限审计、风险派单、台账和审批 |
| 数字化项目助手 | 独立 v0.1.0 MVP 与造价智算菜单 / iframe 薄接入均已完成 | 双项目联合运行验收、绿色版 / 发布启动编排 |
| 桌面与交付 | 传统开发版、Windows 绿色版和 Tauri 桌面版可用 | 按重要版本执行真实绿色版 / Tauri 回归，统信方向暂不主推 |

## 4. 最近完成事项（2026-07-11—2026-07-13）

| 时间 / 版本 | 完成事项 | 结果与边界 |
| --- | --- | --- |
| v5.8.0—v5.8.2 | 智能协同第二层长连接、Excel 自动处理、成果回传和知识库问答 | 复用现有专业接口；飞书不复制匹配逻辑，不修改价格和正式知识库 |
| v5.8.1 | 数字化项目助手宿主入口 | 左侧新增入口和可降级 iframe；业务代码仍全部在独立项目 |
| v5.8.5 | 第二层多企业应用机器人选择 | 切换时安全退出旧实例，继续共用单任务顺序队列 |
| v5.8.6 | 第一层多 Webhook 与统一秘密配置 | Webhook / App Bot 分区保存，兼容旧配置，前端不回显秘密 |
| v5.8.7 | 企业 WeAct 第二层真实接入 | REST 与 WebSocket 同时使用企业域名，已验证访问令牌和企业长连接 |
| v5.8.8 | 第二层机器人运行控制台 | 智能协同页实时显示脱敏连接、消息与任务日志，支持 3 秒自动刷新和手动刷新 |
| v5.8.9 | 第二层机器人业务追踪日志 | 控制台由按钮打开弹窗；显示发送人、会话、完整消息与附件名，名称无权限时回退 ID；明确长连接无来源 IP，同时继续隔离凭证与连接票据 |
| 当前文档整理 | “二维价格知识库”统一为“结构化计价库” | 只改产品术语，不改真实目录、Excel 文件名、API 和匹配逻辑 |
| 独立项目 v0.1.0 | 功能点法计算助手完成独立 MVP | 已具备文档解析、候选确认、确定性测算、Excel 导出和嵌入主题 |

## 5. 下一步建议

### P0：先恢复全量回归基线

这是下一项最优先工作，先于新增业务功能。

1. 修复知识库标准资料检索前 5 条未出现 `standard` 来源的问题。
2. 核对项目样例工作簿第 90 行缺少“利用钻孔抽水”的原因：确认是样例替换、数据漂移还是测试选错文件。
3. 修复后运行 `python -m pytest backend/tests -v`，目标是消除当前 `2 failed`，形成 v5.8.9 可复现全绿基线。

边界：先查资料与测试基线，不夹带修改核心匹配规则；若真实资料口径已经变化，应修正测试和说明，不为“过测试”伪造业务数据。

### P1：完成数字化项目助手联合验收

1. 启动造价智算 `5174/8000` 与独立助手 `5175/8010`。
2. 从左侧入口验证 iframe 加载、嵌入主题、上传、计算、导出、独立打开和服务不可用降级。
3. 更新跨项目交接信箱，将状态从“待接入”收口为“联合验收通过”或列出真实缺陷。
4. 明确绿色版 / Tauri 是否需要自动启动独立服务；未明确前不把外部项目业务代码复制进宿主。

### P1：推进辅助填价与人工复核闭环

1. 在现有基价 / 单价候选基础上增加实物工作费调整系数和技术工作费调整系数候选。
2. 增加分字段人工确认、自定义值备注、候选来源和处理留痕。
3. 没有完整标准出处时显示“暂无标准出处映射”，不让大模型补条款。

承载 PRD：`00-PRD/01-模块PRD/09-辅助填价与人工复核闭环/PRD.md`。

### P1：建设结构化计价库只读质量检测

先做只读脚本或只读接口，检查重复组合、同条件价格冲突、单位缺失、经验数格式异常和关键字段空值，输出 Markdown 报告。

边界：不自动修库、不改变匹配引擎、不调用大模型裁决。

### P2：智能协同生产化

在真实群稳定运行一段时间后，再评估风险派单、多维表格台账、审批归档、角色权限、监控和审计。当前不建议继续优先堆飞书功能，先把专业主线、回归基线和联合验收收口。

## 6. 当前已知问题

| 问题 | 当前证据 | 建议责任模块 |
| --- | --- | --- |
| 标准资料检索排序回归失败 | 全量后端测试中 `test_knowledge_search_includes_original_standard_materials` 失败 | 问问智算 / 知识库检索 |
| 项目样例第 90 行数据漂移 | 全量后端测试中缺少测试预期“利用钻孔抽水” | 样例资产 / 匹配测试基线 |
| 数字化项目助手尚未联合运行验收 | 独立 MVP、嵌入主题和宿主入口已完成，但本轮未同时启动四个服务验证完整闭环 | 模块 14 / 双项目联合验收 |
| 智能协同已完成项与远期项混杂 | 第二层基础能力已完成，风险派单、台账、审批和权限仍待开发 | 模块 13 |

## 7. 本阶段不做

- 不重构核心匹配引擎。
- 不让大模型直接裁决最终价格或系数。
- 不批量重命名历史目录、知识库文件、样例和兼容脚本。
- 不把数字化项目助手业务代码复制进造价智算。
- 不在专业主线和回归基线未收口前优先建设复杂审批、台账和组织权限。
- 不在当前 Windows 主线中强行推进 Tauri Linux、deb 或 AppImage。

## 8. 当前验收口径

- 新线程能在本文件中快速找到当前版本、最近完成、下一步第一优先级和真实阻塞。
- 模块 PRD 状态与代码、README、CHANGELOG 和跨项目交接状态一致。
- 完成项与未来增强分开表达，不把 MVP 完成等同于全部产品化完成。
- 新功能开始前有对应 PRD；完成后回写需求状态、验证结果和版本记录。
