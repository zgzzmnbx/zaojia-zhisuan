# 当前版本计划

## 1. 文档用途

本文件只维护当前基线、最近完成事项、下一步优先级和已知阻塞。历史版本流水以 `CHANGELOG.md` 和 `CHANGELOG - 【归档】.md` 为准；模块长期需求以 `00-PRD/01-模块PRD/*/PRD.md` 为准。

## 2. 当前基线

- 产品名称：造价智算。
- 产品定位：工程造价辅助智能体。
- 当前版本：`v5.22.0`（本地完成，未部署）。
- 云端基线：`v5.19.7`，运行目录为 `/opt/zaojiazhisuan/releases/20260811-v5.19.7-cloud-release`，可回滚到 v5.19.4。
- 核心场景：长输管道勘察测量最高投标限价编制、勘察测量结算辅助审核。
- 核心方法：规则裁决、模型解释、人工兜底。
- 当前主线：专业 Skill 底座、对话式工作台、项目看板、飞书 / WeAct 协同、结算审核、完整混合 RAG、可信经验进化闭环首版、Task P0、跨模块 `VIS-01`—`VIS-15` 专业数据可视化和竖屏触控适配均已完成；第二个 Registry 专业 Skill 仍等待独立真实资料。当前以独立窄屏样式层补齐 iPad 竖屏和手机基本操作，并冻结 `>=1025px` 桌面宽屏界面及可见版本信息。下一步转入 8 分钟作品介绍与现场展示主线复验；Task P1、可信经验其余事件触发、批量失效扫描与可信身份继续后置。

### 2.1 现场展示赛制与复赛内部汇报硬节点（2026-08-12 确认）

赛事通知原文：**“本次预赛统一采用现场展示+答辩的方式：8分钟作品介绍（含展示）、2分钟答辩，请各位选手提前做好作品准备和材料编制。”** 本通知中的“预赛”与项目现有文档所称“复赛”指同一场比赛；项目材料统一按“8 分钟作品介绍包含 PPT 讲述与现场展示、2 分钟答辩独立准备”执行。

| 时间 | 节点 | 本项目材料要求 |
| --- | --- | --- |
| 2026-08-10—08-16 | 本周打磨材料并向本中心领导汇报 | 形成可完整讲述的 PPT 主线、纳入现场展示的 8 分钟逐页讲稿、最短演示金线和 2 分钟备答初稿；所有数据、功能状态和现场路径能够回到项目证据 |
| 2026-08-17—08-23 | 根据本中心意见收口并向公司领导汇报 | 完成接近定稿的 PPT、逐页控时稿、演示降级预案和高频问答；按“8 分钟作品介绍（含展示）+ 2 分钟答辩”完整排练 |
| 2026-08-24（星期一） | 赛道一“桌面办公助手创新赛道”复赛 | 使用最终核验版本参赛；作品介绍 8 分钟（含展示），答辩 2 分钟 |

赛前工作以这三个节点倒排。现有 10—12 分钟 PPT 内容只作母稿；正式上台稿必须压缩为包含现场展示的 8 分钟单一故事线，答辩与备份页单独准备。具体表达约束以 `00-PRD/00-产品总览.md` 的“已确认的现场展示赛制与材料节奏”小节为准。

## 3. 当前能力状态

| 能力域 | 当前状态 | 仍需补齐 |
| --- | --- | --- |
| 专业 Skill 扩展框架 | v5.14.0 已完成 `SkillRuntimeContext`、任务冻结、文件特征推荐、开放格式和只读治理中心；测试专用第二 Skill 已证明资产隔离 | 第二个真实专业能力待用户补充独立业务资料、结构化数据、规则、风险口径、模板和样例后建设；可信签名、审批授权、真实包导入导出和内部市场仍待生产化 |
| Task 任务对象与执行轨迹 | v5.21.0 已完成 P0：稳定 Task 身份与旧编号映射、不可变定义 / Skill 快照、真实事件时间线、当前任务条、详情抽屉、项目 Task 列表和成果 / 协同 / Experience 只读血缘均已贯通；聚合异常降级不阻断专业主流程，项目恢复不回退完成状态 | P1 待补刷新后自动恢复、任务搜索 / 继续处理和脱敏任务摘要导出；P2 组织级编排继续暂缓 |
| 跨模块专业数据可视化 | v5.22.0 已完成共享图形基础与 `VIS-01`—`VIS-15`：真实责任页面、数据契约、完整状态、下钻、无障碍摘要、响应式和字体门禁均已落地；项目看板既有 KPI、八项图形、漏斗、历史表与交互一项未减少 | 后续只按真实业务数据补充多版本 / 多轮次样本和演示讲述，不向生产台账注入夹具，不改变正式裁决 |
| 竖屏与触控适配 | 已完成 `<=1024px` 独立适配层：横向模块触控带、单列主工作区、覆盖式智算 Sheet、44px 触控目标、16px 输入和动态视口 / 安全区处理均已落地；`>=1025px` 桌面三栏、可见版本信息与现有交互冻结不变 | 后续只在真实 iPad / 手机设备补充 Safari / Chrome 手感复核，不以设备差异反向修改桌面宽屏界面 |
| 对话式智能体工作台 | v5.11.0 已完成左侧 / Dock 双入口、同一会话状态、宽屏专注布局、附件按钮与拖拽上传、真实处理动作和成果入口；v5.12.0 已接入任务级 Skill 运行时上下文；v5.19.4 统一大小窗口的任务 / 知识处理进度和费用图表；v5.19.6 将含图表回答按段呈现并分阶段展开；v5.19.7 固化唯一字体 / 字号硬门禁，完成会话内双平台复核三步设置卡、逐人结论进度卡、部分失败原卡反馈和重复指令原卡前移 | P1 待补刷新后会话恢复、命名 / 搜索 / 保留策略、通用动作卡与高风险确认协议；第二真实 Skill 待业务资料 |
| Excel 填价与三数字匹配 | 主流程已可用，结构化计价库、第一层规则和第二层经验提示分层运行；v5.11.1 已恢复正式标准资料检索并形成全绿回归基线 | 结构化计价库只读质量检测 |
| 结算审核助手 | v5.18.0 已完成统一结算模板及同结构 `.xlsx` 的 P0 辅助审核：独立引擎 / API / 运行目录，8 条规则卡，风险分级与人工核验，输出审核后 Excel、Word 审核报告和 JSON；演示样例审核 8 行并形成 `¥32,372.00` 建议差额 | P1 待补任意格式映射、合同 / 工程量 / 发票 / 签章附件结构化识别、规则配置治理和人工确认案例库；最终审定始终由有权限人员完成 |
| 项目看板与历史项目 | v5.15.3 已完成 P0：沿用“填价工作台”入口，内部提供“项目看板｜新建填价”双视图；真实 SQLite 台账区分项目、任务、版本和成果，提供统一 KPI / 图表 / 历史列表 / 详情 / 幂等回填接口；新增“项目进入 → 完成匹配 → 完成预警 → 生成报告 → 完成复核”累计漏斗，逐阶段展示转化与流失并可联动历史项目；本机模型请求波形与模型种类环图继续服从时间筛选；v5.16.0 已统一恢复 KPI、分析图表、漏斗和历史项目的浅灰模块边界 | P1 待补继续处理历史任务、关联已有项目、版本时间线、收藏 / 归档和问问智算确定性项目查询；组织级、跨人员和跨实例驾驶舱仍待可信身份与授权 |
| 结果预览与成果输出 | Excel 预览、人工修改、下载、Word 生成和真实 DOCX 预览已完成 | 持续做真实样例稳定性回归，不扩张新流程 |
| 经验池与可信复核 | 经验池预警、治理报告、风险清单、轻量追溯和基价辅助填价 MVP 已完成；v5.19.3 将机器人入口升级为“AI填价”，结构化候选稳定排序后把前三名交给模型解释，并由程序确定性展示排序首选，最终仍由人工确认写入；v5.19.6 完成三栏弹窗字体、层级、留白、候选卡和响应式阅读体验收口 | 两个调整系数候选、分字段确认、完整出处映射 |
| 问问智算 | 普通问答、强制知识库、行级解释、风险报告、飞书知识库问答、可治理知识记忆、v5.19.5 完整混合 RAG 和 v5.20.0 可信经验进化闭环首版均已完成；两类真实事件进入候选、项目胶囊、二次审核晋级、跨项目命中审计、撤销停止命中和真实指标已贯通 | 问答纠错、风险关闭、项目结项自动提取、正式依据 / Skill 版本变化后的批量失效扫描、企业身份与授权和 WeAct 提醒仍待下一增量；hybrid 继续手动可选 |
| 智能协同 | 已形成受控 P0 简易闭环：Webhook、普通飞书 / 企业 WeAct 独立长连接、标准 Excel 收件与自动专业处理、知识库 / 普通问答、确定性任务查询、主动派发、指定编制人领取、编制成果 Excel 回传、多人复核、复核评论、退回再提交和全员通过完成；v5.19.0 新增网页填价成果“一键发给同事复核”；v5.19.7 完成会话内飞书 / WeAct 双平台独立配置、真实逐人进度与意见展示，并修复单个目标不可达阻断其余复核卡的问题，可达人员继续收到成果与按钮，失败人员只补发未完成目标 | 完整不可变成果版本模型、版本差异、sheet / 行级结构化意见、风险派单 / 催办 / 转交、企业原生审批、唯一正式发布版、归档、多维表格对账、生产级身份权限和常驻监控尚未完成；同一平台 App ID 在本地与云端仍只能启用一个实例；个人主动触达仍受企业应用可用范围约束，不可达人员必须先与应用建立可触达关系后再重试 |
| 数字化项目助手 | 独立 v0.1.0 MVP 与造价智算菜单 / iframe 薄接入均已完成 | 双项目联合运行验收、绿色版 / 发布启动编排 |
| 桌面与交付 | 当前对外交付维护传统开发版和 Windows 绿色版；Tauri 2 仅作为开发调试与未来桌面化资产保留 | 按重要版本执行真实绿色版回归；Tauri 仅在相关改动时专项验证，统信方向暂不主推 |

## 4. 最近完成事项（2026-07-11—2026-08-11）

| 时间 / 版本 | 完成事项 | 结果与边界 |
| --- | --- | --- |
| 2026-08-13 | 竖屏与触控适配 | 新建独立模块 PRD 和仅在 `<=1024px` 生效的适配层；iPad / 手机改用横向触控模块带、单列工作区与覆盖式智算 Sheet，主要触控目标 44px、输入 16px，并处理 `100dvh` 与安全区。为满足宽屏零改动门禁，本次不改变页面与接口显示版本号。专项 3 项、生产构建、UI 设计系统和 PRD 巡检通过；浏览器核对 390 / 768 / 820 / 1024 / 1366 / 1440 / 1920px，窄屏无页面级横向溢出，1440×900 六个核心节点实施前后计算样式完全一致。未部署、未重启服务、未 push |
| v5.22.0 | 跨模块专业数据可视化 | 新增共享图形基础和 `VIS-01`—`VIS-15`，就近覆盖 Task 血缘、经验偏离、候选确认、逐行风险、结算金额 / Sheet 风险、工作量、复核、知识治理、版本趋势、RAG 调试与 Skill 能力矩阵；结算仅增加同次真实审核明细的只读聚合。前端全量 78 通过，后端 527 通过 / 5 跳过，V3.1 Excel 6 张表 1254 个目标单元格全值差异为 0，Word 业务核心一致；1366 / 1440 / 1920、深浅色、328px 窄工作区、computed 字体和零控制台告警通过。未部署、未 push、未发送外部消息 |
| v5.21.0 | Task 任务对象与执行轨迹 P0 | 新增独立 Task 登记、旧编号映射和真实事件聚合，冻结任务定义、输入与 Skill；智算助手、专业工作区、结果预览、Word 报告、项目详情和 Dock 显示同一 Task，详情可追溯 Tool、成果、协同和 Experience。后端全量 527 通过 / 5 跳过，V3.1 Excel 6 张表 1254 个目标单元格及全值差异为 0，Word 业务核心一致，风险附录在模型遗漏时由真实结构化证据补全；1366 / 1440 / 1920、深浅色、Dock、computed 字体和零控制台告警通过。未部署、未 push、未发送外部消息 |
| v5.20.0 | 可信经验进化闭环首版 | 复用 `knowledge-memory.sqlite3` 和既有治理状态机，接入人工改单 / AI填价确认及多人复核意见两个真实触发；事件保存幂等键、可靠项目 / 隔离任务范围、Skill 与成果血缘，失败只返回可重试警告，不回滚业务结论。已完成项目可生成受控引用胶囊，通用提升项必须再次人工审核，问答记录跨项目命中，撤销后停止命中；现有知识记忆弹窗增加“知识记录｜项目胶囊｜进化成效”，指标只取真实审计。项目 A→B→撤销由真实 API / SQLite 隔离流程通过，正式规则资产零改动；未部署、未发送飞书 / WeAct 消息 |
| v5.19.7 | 会话复核卡、部分失败隔离与字体硬门禁 | “发送同事复核”以紧凑三步卡嵌入对话，飞书 / WeAct 各自折叠、各自配置方式与人员并支持同时发送；重复指令把原卡移动到最新会话位置，不再出现无反应或滚到底部空白。审核进度卡减少装饰色并直接显示逐人复核结论、意见和未提交状态。WeAct 个人私聊中单个复核人不可达时继续为其余可达人员发送成果和复核卡；失败人员保留脱敏原因，恢复后只补发个人缺失步骤。UI 规范、项目 AGENTS 和自动检查统一字体家族、唯一字号层级与 12px 最小可见文字；不改变审核状态机、匹配、知识库或成果内容。2026-08-11 已完成云端发布、公网验收、双平台运行态核对和可回滚保护 |
| v5.19.6 | 智算结果呈现与 AI填价可读性收口 | 含图表回答改为按完整段落呈现；费用洞察按标题、来源、三项指标、两张图表和口径说明分八阶段展开，并让大小会话窗口自动跟随。减少动态效果时立即完整展示；普通文本保持原节奏。AI填价三栏弹窗统一字体、层级、留白、候选卡和 `1100px` 响应式断点。前端 56 项、生产构建、健康接口、UI 规范、PRD 巡检和本地浏览器运行态均通过；仅调整结果表达和复核体验，不改变候选排序、人工确认、三个数字、知识检索、报告或原始 Excel |
| v5.19.5 | 完整混合 RAG 检索底座 | 冻结经典题集与黄金证据报告；新增 BM25、结构化、384 维本地离线向量、RRF 融合、权威 / 数字 / 编码 / 比例尺 / 单位硬门控、规则重排、能力探测、API 轨迹和知识库旁路切换。classic 默认不变；hybrid 黄金 Recall@5 1.000、精确保护集 1.000、引用准确率 1.000、无依据误答率 0、nDCG@10 0.951188；已完成索引损坏、向量 / 重排 / 模型故障降级注入与恢复。未改变填价、三个数字、经验池、工作量抓取、报告计算和正式资料 |
| v5.19.4 | 大小智算进度、图表自适应与云端历史看板 | AI填价新增纯蓝分阶段进度条，从低进度起步并在等待真实返回时最高停在 94%；大小智算窗口复用任务执行和知识处理进度，右侧 Dock 同步显示当前步骤；费用构成洞察在小窗口自动切为单列竖排，费用图和预置知识库柱状图统一使用 1.5 秒入场动画。当前版本已发布云端并合并本机与线上历史看板数据，保留 71 个项目、76 次运行、126 个成果和 697 条模型调用记录。仅增强过程反馈、响应式展示和演示数据完整性，不修改匹配、知识库、经验池算法、报告模板或原始 Excel |
| v5.19.3 | 行级 AI 升级为 AI填价 | 表格机器人入口直接打开三栏 AI填价弹窗并自动分析；知识库与经验池候选由程序按相似度等规则排序，前三名和当前行完整要素进入模型上下文，回答顶部确定性展示排序首选、来源、理由与风险。模型只解释候选，不生成新价格、不自动写值；本版仅覆盖基价 / 单价候选，不修改三个数字正式裁决规则 |
| v5.19.2 | 四道预置演示标准答案 | 常见问题前四项改为技术工作费确定、走向图编制价格、三个过路过桥费编码和16种管径柱状图；题末使用低对比蓝点。原题在检索和模型调用前直接返回人工审核答案，实质改写后恢复正常问答；不修改三个数字的裁决规则 |
| v5.19.1 | 知识库问题拆分与标准答案回归 | `#知识库` 输入先拆为业务检索问题、回答与展示要求、补充说明要求，只有业务问题进入扩词与召回；示例复杂题由49个检索词降到18个。演示文档增加简单题优先入口，新增人工标准答案、机器可读关键事实和运行态检查脚本；6道真实问答覆盖项目规则、造价通用资料和无依据降级，全部通过。模型继续只解释，不裁决三个数字 |
| v5.19.0 | 网页填价成果一键多人复核 | 填价工作台在 Excel / Word 下载旁新增“发给同事复核”；复用智能协同的平台、工作群和人员目录，逐一明确选择复核人后，由后端读取当前任务真实输出、冻结 SHA256 快照并直接进入既有待复核、评论、通过 / 退回和完成状态机。个人私聊仅限主动触达已验证平台，工作群必须明确选择、成员可核验且不超过 10 人；同一成果重复提交不重复发送，失败重试只补发未成功步骤 |
| v5.18.0 | 结算审核助手 P0 | 基于前辈指导、统一结算模板和实际案例建立独立结算审核闭环；支持同结构 `.xlsx` 上传、规则卡核验、风险分级、人工核验和审核后 Excel / Word 报告 / JSON 输出；演示样例审核 8 行、识别 8 项风险，其中 7 项高风险、6 项待人工核验，上报 `¥461,877.02`、审核建议 `¥429,505.02`、建议差额 `¥32,372.00`。页面严格复用纯白、无渐变、无常驻阴影的大尾巴 Shadcn UI；未改动既有填价、三数字匹配、经验池、知识问答和飞书 / WeAct 逻辑，也不宣称第二个 Registry Skill 已上线 |
| v5.17.0 | 普通飞书 / WeAct 双平台独立长连接 | 普通飞书与企业 WeAct 各自由独立运行器接收消息、文件和卡片回调，可在智能协同页分别启停，不再通过下拉框互相替换；任务、收件窗口、会话查询、成果重发和卡片回调均绑定创建平台，两侧只共享任务库、专业处理能力和一个全局顺序队列；页面右上角持续显示两平台状态，任务台账同时标识来源平台；云端以常驻监督器按两个开关分别恢复运行器，并用平台级启动互斥防止重复实例。每个同名 App ID 在本地与云端仍只允许一个实例在线。本版完成本地代码、自动化与浏览器无发送验收，尚未部署云端 |
| v5.16.2 | 云端发布流程固化 | 新增云端发布包自动构建器、云端发布与验收操作指南、专用线程交接提示词和 Git 忽略的本机 SSH 凭据入口；自动保持前端资源层级、排除秘密与运行数据、复制 Manifest / 知识库资产，并生成 UTF-8 `tar.gz` 和 SHA256；明确机器人条件恢复、失败回滚、文档回写和 Git 推送闭环。已完成真实云端发布与公网验收；本版本不改变专业匹配、知识检索、经验池、报告模板或智能协同业务逻辑 |
| v5.16.1 | 三层知识库与可信复核体验升级 | `#知识库` 可选择项目正式知识库、造价通用知识库和已确认知识记忆；常见问题可从配置维护并点选；大小智算窗口统一 Markdown / 表格渲染、精简依据和默认隐藏头像；辅助填价形成当前记录、结构化候选和行级 AI 复核三栏同屏界面。所有知识选择和模型建议只用于检索解释，不改变三个数字裁决、规则表或原始 Excel |
| v5.16.0 | 高价值工作区 Shadcn 化 | 智能协同、大模型设置、工作量抓取、智算助手、结果预览和页面设置统一接入“大尾巴 Shadcn UI”；看板恢复浅灰模块边界，工作量抓取补容器响应式；不改变专业规则、报告模板或协同状态机 |
| v5.15.4 | 大尾巴 Shadcn UI v1.0.0 | 以 preset `b1au7YYAi` 的 Rhea / Neutral / Blue / Lucide 和 Inter 比例语言为设计输入，建立 primitive → semantic → component 三层令牌、组件状态规范、组件样张与自动检查器；运行时继续使用自托管苹方，现有 React / CSS 通过 `--dws-*` 渐进接入，不全量安装或应用 shadcn / Tailwind |
| v5.15.3 | 项目闭环漏斗 | KPI 下方新增五阶段横向漏斗；后端按同一筛选项目累计计算完成匹配、完成预警、生成报告和完成复核，前端显示数量、阶段转化率与流失量，点击阶段同步刷新 KPI、图表和历史项目 |
| v5.15.2 | Dashboard 观图与汇报体验 | 页头改为紧凑筛选按钮，日期、Skill、状态、来源、风险、质量和搜索在居中弹窗中分组编辑，应用后统一刷新；新增纯白全屏汇报模式和同口径项目来源多蓝色环图，来源不足时省略；未修改其他页面和专业业务链 |
| v5.15.1 | Dashboard 大模型调用统计 | 新增只保存调用元数据的独立 SQLite 台账；普通问答、知识库有证据问答和风险报告的真实模型调用统一记录，纯检索与无证据直接返回不计数；Dashboard 增加请求次数平滑波形图和模型种类环形图，随时间范围同步，历史审计日志回填排除明确测试模型 |
| v5.15.0 | 项目看板与历史项目 P0 | 建立真实项目 / 任务 / 版本 / 成果 SQLite 台账、统一 Dashboard 与历史项目 API、受控幂等回填、Rhea 局部作用域响应式界面和成果入口；真实 100 行样例完成上传、匹配、预警、报告和下载浏览器验收，不修改三个数字规则、报告模板和智能协同状态机 |
| v5.14.6 | WeAct 任务领取稳定性 | 待填 Excel 先投递、任务卡后开放，消除卡片可点击与文件状态落库之间的竞态；机器人启动时预热协同运行库，快速领取不再被投递收尾覆盖；创建任务按钮调整为蓝色主按钮 |
| v5.14.5 | 多人复核评论闭环 | 经典复核卡提供最多 500 字评论输入；通过评论可选、退回评论必填；评论按复核人和轮次写入运行库、审计、卡片和智能协同台账，不改变专业审核结论与受众安全闸门 |
| v5.14.4 | WeAct 辅助审核口令与受众硬安全闸门 | `#辅助审核` 复用现有 1 分钟收件和专业审核链路；单任务、单次发送最多触达 10 名可核验姓名人员，个人投递只限明确指定并映射的编制人 / 复核人；工作群每次发送前实时核验成员，无法核验、未命名或超限一律拒绝 |
| v5.14.3 | 智能协同编制成果回传与卡片回调稳定性 | 编制人点击提交后开启 1 分钟任务专属 `.xlsx` 收件窗口；成果绑定任务后与复核卡一并投递；卡片回调即时应答，后续通知异步发送，消除已成功但客户端首次点击报错 |
| v5.14.1 | 智能协同 P0-A 领取闭环 | 外部任务编号与缺省项目名自动生成；经典卡片新增“领取任务”，仅指定编制人可用真实平台身份领取且重复点击幂等；页面显示流程参与人，不回显平台标识 |
| v5.14.2 | 智能协同 P0 简易多人复核 | 支持选择多名复核人；编制人提交后逐人通过或退回，全部通过才完成；退回后可开启新一轮，页面展示人员、轮次和复核卡状态 |
| v5.14.0 | 专业 Skill P1 推荐 / 开放格式与 P2 管理安全接口 | Excel 文件名、有限 sheet / 表头特征生成可解释推荐，必须人工确认；公开 Manifest + `SKILL.md` 开放格式；管理中心仅盘点本机能力并生成生命周期审核计划，全部写操作关闭，不执行任意外部脚本；第二真实专业仍待资料 |
| v5.13.0 | 智能协同 P0-A 外部任务主动派发模拟 | 新增受控模拟器、标准 `TaskEnvelope`、业务幂等、任务 / Skill / 模板快照、人员映射、经典卡片 + Excel 分步投递与单步重试；真实群投递只允许唯一“智算测试”，精准单聊未完成主动触达实测前不启用 |
| v5.12.0 | 专业 Skill P0-B 运行时接口 | 建立任务级 `SkillRuntimeContext`，冻结输入规范、可信处理器、计价库、规则、风险、知识、报告模板和验证信息；测试双 Skill 证明资产隔离与任务不漂移；专项 `19 passed, 1 skipped`、全量后端 `333 passed, 4 skipped`、前端构建通过；内置浏览器 101 / 101、0 待复核、Skill 已锁定、无控制台错误；不开发通用服务类真实业务内容 |
| v5.11.1 | 正式标准资料索引兼容与全绿基线恢复 | 修复知识源发现只查旧 `01-原始资料` 的问题，同时兼容当前业务资料目录并保持 `standard` 分类；专项 `1 passed`，全量后端 `331 passed, 4 skipped`；不解析 PDF，不修改正式资料、三数字匹配和规则资产 |
| v5.11.0 | 对话式智能体工作台 P0 | 完成左侧“智算助手”与 Dock 双入口、同一会话 / 草稿 / 任务状态、宽屏工作台、Registry Skill 锁定、Excel 按钮 / 拖拽附件和真实处理 / 匹配 / 预警 / 报告 / 下载链路；后续完成白底简约会话视觉、用户指令分轮、自绘 Skill 菜单、折叠任务操作和快捷操作对话回显收口；不修改三数字匹配与正式资产，多 Skill 运行时路由仍待 P0-B |
| v5.10.0 | 选择专业能力（Skill）P0 | 完成全局导航栏“专业能力”入口、默认折叠菜单、能力详情、真实状态、Manifest 安全校验、任务快照、旧请求默认兼容和发布携带；勘察测量为已上线 v1.0.0，通用服务类为规划中，更多造价专业为即将开放，后两者均不可创建任务；未修改三数字匹配和正式资产 |
| 当前测试口径校正 | 项目样例“利用钻孔抽水”定位 | 测试明确选择带数量的项目例子，并按“水文地质勘察＋利用钻孔抽水”定位真实业务行，不再写死第 90 行；全量后端现为 `328 passed, 4 skipped, 1 failed` |
| v5.9.2 | 知识记忆治理增强 | 通用知识按类型配置自动或人工审核；增加同范围去重、相似/冲突提示、更正版本、项目知识提升通用，以及对话式保存、修改、撤销、查看；不让大模型裁决冲突，不改变正式规则和价格匹配 |
| v5.9.1 | 通用知识默认与自然语言记忆 | 知识候选默认为通用范围且当前自动通过，保留审核接口和完整审计；“记住这一条”可保存最近的结构化候选；项目/任务范围仍人工审核 |
| v5.9.0 | 问问智算项目知识记忆 P0 | 完成候选创建、编辑、提交、确认、驳回、撤销、疑似失效、版本和审计；只有同项目已确认知识参与问答，正式依据优先，数据库故障不阻断专业主流程；当前身份仍为本地试点 |
| v5.8.21 | 文件收件即时表情确认 | 已命中收件窗口的 `.xlsx` 文件添加“了解”；群内普通文件、未开启窗口文件和其他格式保持静默 |
| v5.8.20 | WeAct 延迟文件事件兼容 | 收件窗口仍为 1 分钟；仅以平台发送时间命中原窗口且 15 分钟内送达的单个 `.xlsx` 可继续处理，普通历史消息不放宽 |
| v5.8.19 | 云端机器人专业服务端口加固与标题区分 | 本地默认 8000、云端环境覆盖 1285；仓库化 systemd 安装与发布检查；运行器启动前健康预检；浏览器标题自动标识本地 / 云端，不改专业处理逻辑 |
| v5.8.18 | 第二层机器人身份配置加固 | 普通飞书 / 企业 WeAct 预期 App ID 与域名集中登记；页面、后端启动器和独立运行器三处拒绝不一致配置；本地与云端 WeAct 统一使用应用 A，秘密仍不入库 |
| v5.8.17 | 第二层消息防重放保护 | 事件 ID + 消息 ID 持久去重；超过 5 分钟的过期消息不发表情、不回复、不进入业务；控制台补齐平台与本机时间链路 |
| v5.8.0—v5.8.2 | 智能协同第二层长连接、Excel 自动处理、成果回传和知识库问答 | 复用现有专业接口；飞书不复制匹配逻辑，不修改价格和正式知识库 |
| v5.8.1 | 数字化项目助手宿主入口 | 左侧新增入口和可降级 iframe；业务代码仍全部在独立项目 |
| v5.8.5 | 第二层多企业应用机器人选择 | 切换时安全退出旧实例，继续共用单任务顺序队列 |
| v5.8.6 | 第一层多 Webhook 与统一秘密配置 | Webhook / App Bot 分区保存，兼容旧配置，前端不回显秘密 |
| v5.8.7 | 企业 WeAct 第二层真实接入 | REST 与 WebSocket 同时使用企业域名，已验证访问令牌和企业长连接 |
| v5.8.8 | 第二层机器人运行控制台 | 智能协同页实时显示脱敏连接、消息与任务日志，支持 3 秒自动刷新和手动刷新 |
| v5.8.9 | 第二层机器人业务追踪日志 | 控制台由按钮打开弹窗；显示发送人、会话、完整消息与附件名，名称无权限时回退 ID；明确长连接无来源 IP，同时继续隔离凭证与连接票据 |
| v5.8.10 | 第二层收件表情确认 | 收到可见消息后异步添加“了解”（`Get`）表情；普通飞书 / WeAct 共用，失败不阻断业务处理 |
| v5.8.11 | 第二层消息精确分流 | `@上传` / `@上传文件` 开启 1 分钟收件；`#知识库`、`你好`、普通大模型托底各自分流；群内普通消息不再添加表情 |
| v5.8.12 | WeAct 问候与大模型连接修复 | 兼容编号化 @ 占位符；“你好”返回完整介绍；大模型 TLS / 超时 / 连接瞬断有限重试，真实 DeepSeek 问答通过 |
| v5.8.13 | 群聊 @对象精确过滤 | 读取本机器人 Open ID，仅明确 @本机器人时回应；@其他人、其他机器人或身份未确认时保持静默，单聊不受影响 |
| v5.8.14 | 群成员确定性查询 | 群聊明确 @本机器人后，三条完整指令通过群成员接口分页返回真实人数和名单；不经过大模型，单聊只提示使用边界 |
| v5.8.15 | 第二层只读完成卡片 | 成果文件上传后用绿色卡片汇总任务和风险；可选跳转造价智算，卡片失败降级文字，不接收业务回调 |
| v5.8.16 | 第二层第一梯队任务指令 | 当前会话内确定性查询最近任务、进度、风险和高风险；可重发历史成果，跨会话不可读且不经过大模型 |
| 当前文档整理 | “二维价格知识库”统一为“结构化计价库” | 只改产品术语，不改真实目录、Excel 文件名、API 和匹配逻辑 |
| 独立项目 v0.1.0 | 功能点法计算助手完成独立 MVP | 已具备文档解析、候选确认、确定性测算、Excel 导出和嵌入主题 |

## 5. 下一步建议

当前执行顺序：**① 用 v5.22.0 完成 V3.1 主业务与 8 分钟演示主线复验 → ② 收口 PPT、逐页讲稿、现场切换和降级预案 → ③ 可信经验下一增量与身份 / 失效治理 → ④ 两个调整系数的辅助填价闭环 → ⑤ 结构化计价库只读质量检测。** Task P0、跨模块专业数据可视化和竖屏触控适配已经完成；后续演示必须使用真实 Task、真实图形、真实成果和可追溯证据，不新增平行页面、不复制状态、不修改三个数字。第二个真实专业 Skill 等独立资料齐备后再进入。

### 已完成：Task 任务对象与执行轨迹 P0

承载 PRD：`00-PRD/01-模块PRD/19-Task任务对象与执行轨迹/PRD.md`。

1. 统一定义 `Project 项目 → Task 业务任务 → Run 技术运行 → ReviewRound 复核轮次 → ArtifactVersion 成果版本 → ExperienceLineage 经验血缘`，建立稳定 `task_id` 与既有 `job_id / run_id / 协同 task_id / source_task_id` 的兼容映射。
2. 任务创建时冻结项目、来源、目标、输入、Skill、预期成果、成功标准和人工闸门；模型只能根据用户明确指令辅助预填，不得补造业务事实。
3. 智算助手和专业工作区复用紧凑“当前任务”组件；任务详情以真实时间线显示 Skill 冻结、输入接收、结构识别、规则处理、风险检查、人工复核、成果、协同和 Experience 治理。
4. 项目看板展示项目下 Task 列表和轨迹；智能协同、结果预览、Word 报告和知识记忆继续使用各自状态机，只通过统一 Task 身份关联，不建立第二份可编辑状态。
5. 以网页 / 智算助手任务和外部协同任务各跑通一例；再用 V3.1 正式样例完成 Task 创建、Skill 冻结、匹配、复核、Excel / Word、协同 / 经验追溯，并确认三个数字和正式成果未改变。

边界：P0 不新增左侧“Task 中心”，不把普通聊天自动变成业务 Task，不把重试 / 重算 / 报告刷新计成新任务，不建设通用 BPMN、多智能体自治或在线自训练平台。任务聚合层异常时必须降级，不阻断现有专业主流程。

### 已完成：跨模块专业数据可视化 P0/P1

承载 PRD：`00-PRD/01-模块PRD/20-跨模块专业数据可视化/PRD.md`。

完成证据：`00-PRD/01-模块PRD/20-跨模块专业数据可视化/实施进度.md`；目标模式提示词仅保留为实现边界参考。

1. 项目看板现有 KPI、项目处理趋势、项目状态、大模型调用波形、模型种类、风险排行、匹配质量、条件式来源分布和五阶段漏斗一项未减少；15 张新图已在这些既有能力之上按真实责任页面增量实现。
2. `VIS-01`—`VIS-15` 已全部进入代码与真实入口：执行轨迹、成果—经验血缘、偏离散点、经验区间、候选点图、行风险地图、结算瀑布、分 Sheet 风险、工作量分组条、人员矩阵、轮次阶段带、经验治理双条、版本趋势、检索通道和 Skill 能力矩阵。
3. 图形只读取现有业务真相并下钻回同口径 Task、Sheet、Excel 行、复核人、成果版本或知识记录；当前数据不足时实现准确不足态和隔离测试，不给生产台账造数据。
4. 共享图形基础必须统一自托管苹方、`--dws-*` 令牌、完整状态、键盘 / 文本摘要和响应式；严格执行 28 / 20 / 16 / 14 / 13 / 12px，中文业务文字不得使用 9px / 10px，同层图例、坐标和 Tooltip 不得忽大忽小。
5. 新增专项、全部现有前端专项、全量后端、前端构建、UI / PRD 检查、浏览器逐图验收和 V3.1 Excel / Word 对应答案核对均已通过。

边界：不建设第二套 BI，不新增“数据分析”一级页面，不引入大型图形库，不改变三个数字、预警、审核、协同、知识和报告裁决，不以静态 demo、图片、模型生成数字或假数据替代真实实现。

### 已完成：恢复全量回归基线

v5.11.1 已完成以下收口：

1. 知识源发现同时兼容当前 `01-【业务资料】原始资料-勘察测量相关基础资料` 与旧 `01-原始资料`。
2. 当前目录内 Markdown / Excel / CSV 继续标记为 `standard`；已更新测试路径断言，未降低“前 5 条至少出现正式标准来源”的验收要求。
3. 正式标准资料检索专项 `1 passed`；`python -m pytest backend/tests -v` 全量结果为 `331 passed, 4 skipped`，已恢复可复现全绿基线。

边界：索引当前只处理 Markdown、Excel 和 CSV，不以本次修复为由扩张 PDF OCR；不调整三个数字匹配和正式资料内容。

### 已完成：P0-B 专业能力包运行时接口

v5.12.0 已完成：

1. `SkillRuntimeContext` 从已校验 Manifest 生成，统一冻结输入规范、白名单处理器、计价库、规则、风险配置、知识源、报告模板和验证资产。
2. 处理、批量匹配、预警、问答、报告和下载均读取任务创建时的运行时上下文；默认 Skill 后续变化不改写历史任务。
3. 现有勘察测量逻辑包装为 `survey-measurement-v1` 白名单策略，继续复用同一 `FillEngine` 和原有三数字口径。
4. 测试专用第二 Skill 已验证不同计价库、技术规则、经验池、知识源和 Word 模板真实切换且不串用；该夹具不进入用户能力清单。

边界：当前完成的是可扩展底座接口，不是第二个专业业务交付。用户尚未提供其他项目资料和数据，因此“通用服务类造价测算”继续保持“规划中”。

承载 PRD：`00-PRD/01-模块PRD/15-专业能力包与Skill管理/PRD.md`。

### 已完成：对话式智能体工作台 P0

v5.11.0 已完成左侧 / Dock 双入口、同一会话状态、宽屏专注布局、底部附件 / Skill / 任务上下文、真实处理动作、进度、风险和 Excel / Word 成果入口。复杂字段映射、宽表逐行复核和报告精查继续进入原专业页面，切换不丢任务。

2026-07-21 已完成第二轮 UI / 交互收口：会话采用助手左侧无框正文、用户右侧浅色气泡和轮次分割；输入区支持 Excel 拖拽；任务操作恢复默认折叠；Skill 包改为浅蓝标签与自绘向上菜单；快捷操作点击后先在右侧形成用户指令，再由左侧反馈执行过程。前端构建、工作台专项 `3 passed` 和内置浏览器运行态验收通过。

承载 PRD：`00-PRD/01-模块PRD/16-对话式智能体工作台/PRD.md`。

后续：P1 补刷新后会话恢复、命名 / 搜索 / 保留策略、通用动作卡协议、高风险写操作确认和专业页面处理后的结构化回执。P0-B 接口已完成，但在第二专业真实资料和样例验收前仍只能宣传当前单一勘察测量业务已上线。

### 已完成：P1 推荐与开放格式、P2 管理安全接口

v5.14.0 已完成：

1. 用户选择 Excel 后，后端只读取文件名、最多 12 个工作表、每表最多 12 行和 80 列的表头特征，按 Manifest 的 `recommendationProfile` 返回分值、置信度和命中理由。
2. 推荐不会自动切换专业能力；只有唯一高置信候选才显示推荐，仍必须由用户确认。演示样例没有真实浏览器 `File` 对象时不伪造分析结果。
3. 公开格式固定为 `manifest.json` + `SKILL.md`：结构化 Manifest 承担机器校验和运行路由，Markdown 只承担人类可读说明，不裁决价格、系数和风险。
4. 专业能力中心可盘点本机三个能力目录并查看开放格式；安装、启停、升级、回滚、卸载、导出只生成审核计划，返回 `changes_applied=false`，不修改文件或配置。

边界：可信签名、发布审批、组织授权、真实包导入导出和内部能力市场仍是后续生产化事项；当前绝不执行任意外部 Skill 脚本。

### 已完成：P0 项目看板与历史项目

承载 PRD：

```text
00-PRD/01-模块PRD/17-项目看板与历史项目/PRD.md
```

1. v5.16.1 已在“大尾巴 Shadcn UI v1.0.0”基线上完成三层知识问答、Markdown / 表格回答、常见问题和辅助填价三栏复核体验升级，并继续沿用左侧“填价工作台”与“项目看板｜新建填价”双视图；知识库和大模型只做证据检索与解释，不改变三个数字裁决。
2. 已建立真实 SQLite 项目台账，严格区分项目、处理任务、业务版本和复核轮次；重复转换、重算和报告刷新不虚增项目数。
3. 已提供真实 KPI、项目趋势、状态环形图、风险排行、匹配质量、历史项目表、详情、成果入口和受控回填；普通网页任务自动登记，智能协同历史任务按可靠关系回填；短日期范围按真实日粒度、长范围按月粒度展示，不补假项目，自动化测试使用独立运行目录且不污染本机台账。
4. 已参考指定 Mobbin Dashboard，并以 shadcn/ui preset `b1au7YYAi` 的 Rhea / Neutral / Blue / Lucide 与 Inter 比例语言作为组件质感基准；视觉复核后统一为纯白主背景、自托管苹方、规范字号和浅蓝同色系图表。公共设计语言由 `--dws-*` 三层令牌承载，Dashboard 具体样式继续使用 `.project-dashboard` 局部实现，未全量引入 Tailwind / shadcn。
5. P0 只展示当前本机实例授权范围；没有数据、预警未运行、成果失效和旧任务待归类均诚实表达，不用假数据填充页面。

下一步：P1 处理历史任务恢复、已有项目关联、版本时间线、收藏 / 归档和问问智算确定性查询。继续不做组织级人员绩效、远程 BI、相似文件自动归并、跨实例统计和大模型自由统计；不修改三个数字匹配、经验池预警、报告模板和智能协同状态机。

### 已完成：结算审核助手 P0

v5.18.0 已完成：

1. 基于 `【结算模板】260723-勘察测量结算统一报价模板-v1.0.xlsx`、前辈指导文档和实际结算案例，建立 JS-001—JS-009 中 8 条当前启用规则卡；规则结论可定位到工作表、行、字段、依据和人工动作。
2. 新增独立 `backend/app/settlement_audit.py`、`backend/app/settlement_audit_api.py` 和 `/api/settlement-audit/*` 路由；运行文件隔离在 `Codex-Temp/runtime/settlement-audit/`，不复用或改写既有填价任务状态。
3. 审核后 Excel 保留原工作表并新增首张“审核结果”总览，同时在明细表写入审核状态、风险、建议和批注；Word 报告包含结论、金额汇总、逐项发现和人工签署区，另输出结构化 JSON。
4. 内置可重复生成的演示样例与精确预期结果；样例审核 8 行、识别 8 项风险，其中 7 项高风险、6 项待人工核验，建议差额 `¥32,372.00`。
5. 左侧新增“结算审核”工作台，严格复用 `大尾巴 Shadcn UI v1.0.0` 的 `--dws-*` 令牌、Lucide 图标、纯白背景、细边框和统一圆角；模块内无渐变背景、无常驻阴影，未引入第二套 Tailwind / shadcn 工具链。

边界：P0 只支持统一结算模板及同结构 `.xlsx`；合同解释、工程量真实性、发票、现场事实、签章和最终金额仍须人工核验。该模块是同一勘察测量专业内的独立真实场景 MVP，不是 Skill Registry 中已上线的第二个专业 Skill。

承载 PRD：`00-PRD/01-模块PRD/10-结算审核助手模块/PRD.md`。

### 待资料：P1 建设第二个真实专业 Skill

第二个专业能力继续由同一模块 PRD 承载；待用户补充资料后再启动：

```text
00-PRD/01-模块PRD/15-专业能力包与Skill管理/PRD.md
```

1. 收集通用服务类项目的独立业务定义、输入模板、计价依据、结构化数据和确定性规则。
2. 建立独立风险检查、人工复核点、Excel / Word 成果模板和最小验证样例。
3. 使用当前 Registry、选择入口和任务快照完成与勘察测量资产的串用防护回归。
4. 只有最小业务闭环和确定性样例全部通过后，才把卡片从“规划中”调整为“内测中”或“已上线”。

统一边界：不复制勘察测量资产冒充第二个 Skill，不让大模型解析 `SKILL.md` 裁决价格，不开放任意外部 Skill 安装和脚本执行。

### P1：完成数字化项目助手联合验收

1. 启动造价智算 `5174/8000` 与独立助手 `5175/8010`。
2. 从左侧入口验证 iframe 加载、嵌入主题、上传、计算、导出、独立打开和服务不可用降级。
3. 更新跨项目交接信箱，将状态从“待接入”收口为“联合验收通过”或列出真实缺陷。
4. 明确绿色版 / Tauri 是否需要自动启动独立服务；未明确前不把外部项目业务代码复制进宿主。

### P1：推进辅助填价与人工复核闭环

1. 在现有基价 / 单价候选基础上增加实物工作费调整系数和技术工作费调整系数候选。
2. 增加分字段人工确认、自定义值备注、候选来源和处理留痕。
3. 没有完整标准出处时显示“暂无标准出处映射”，不让大模型补条款。

承载 PRD：`00-PRD/01-模块PRD/09-辅助填价与人工复核闭环/PRD.md`。

### P1：建设结构化计价库只读质量检测

先做只读脚本或只读接口，检查重复组合、同条件价格冲突、单位缺失、经验数格式异常和关键字段空值，输出 Markdown 报告。

边界：不自动修库、不改变匹配引擎、不调用大模型裁决。

### 已完成：完整混合 RAG 检索底座 P1

承载 PRD：`00-PRD/01-模块PRD/18-完整混合RAG检索底座/PRD.md`。

v5.19.5 首版已完成并保持灰度：默认 classic，hybrid 由用户主动切换；本地索引 / 向量 / 重排不接外部服务，指标与故障证据见模块 PRD 和 `backend/tests/fixtures/knowledge_qa_hybrid_fault_injection_report.json`。

实现与回归参考：`00-PRD/01-模块PRD/18-完整混合RAG检索底座/开发提示词.md`。

1. 已保留“经典检索”为默认稳定模式，并在知识库选择器旁提供“经典检索｜混合 RAG”切换，大小窗口共用选择状态。
2. 已实现 BM25 / 词法、结构化字段、本地向量、多路融合、权威与字段冲突门控、规则重排和证据充分性判断。
3. 已冻结标准题和经典指标，并覆盖编码 / 数字保护、语义改写、跨文档、冲突和无依据题。
4. 已验证索引损坏、向量不可用、重排超时和模型不可用时的显式降级与恢复；回答保留请求模式、实际模式和降级原因。
5. 后续只扩充真实题集、观察资源与误召回；指标未证明显著收益前不改默认，不让 hybrid 外溢到 AI填价、三个数字、经验池、工作量抓取和成果计算。

边界：新模式必须可切换、可解释、可评测、可降级；指标未达标前不得改为默认，预置演示题继续在检索前返回人工审核答案。

### 已完成首版：可信经验进化闭环 P1（比赛主创新）

承载 PRD：`00-PRD/01-模块PRD/04-问问智算AI助手/PRD.md`。

1. 已跑通人工改值 / 辅助填价确认、多人复核意见闭环两个真实触发，候选保留项目 / 隔离任务、Skill、Sheet / 行 / 字段、成果版本 / 哈希和原因；无变化改单及无正文通过不造知识。
2. 已复用现有知识记忆工作区完成人工确认、驳回、撤销和审计；价格、系数、正式标准、项目口径、冲突项及全部项目 / 任务候选始终人工确认。
3. 已为完成项目提供显式生成 / 刷新“项目记忆胶囊”，只汇总项目台账、Skill、成果、知识状态和复核意见的受控引用，不复制文件或聊天。
4. 已实现“提升为通用候选并再次人工审核”，问答命中记录知识 ID、来源项目、目标项目和时间；正式依据继续先于记忆展示。
5. 已增加候选来源、确认 / 驳回 / 撤销、检索命中、版本更正和疑似失效真实指标，不估算节省工时。
6. 已由隔离脚本和真实 API / SQLite 状态跑通“项目 A → 两次人工审核 → 项目 B 命中 → 查看审计 → 撤销后停止命中”，重复候选、撤销后跨项目命中和正式资产改动均为 0。

下一增量：补问答纠错、风险关闭和项目结项自动提取；接入可信企业身份和项目成员授权；增加 WeAct 待确认提醒和安全网页链接；对正式依据或 Skill 版本更新执行冲突 / 失效扫描。部署迁移时仍须将 `Codex-Temp/runtime/knowledge-memory.sqlite3` 纳入运行数据备份。

边界：P1 的“自动”只负责发现和预填候选，不自动发布组织知识，不让大模型批准知识、裁决冲突或修改规则资产；跨项目复用必须经过适用范围提升、再次审核、明确授权和条件核对。

### P2：智能协同场景化编审

项目整体排序仍放在专业主线、回归基线和数字化项目助手联合验收之后；进入本模块开发时，按模块内部 P0 / P1 / P2 推进：

1. **模块 P0-A｜外部任务主动派发模拟**：在“智能协同”页模拟外部造价系统产生标准 `TaskEnvelope`，生成任务专用待填 Excel，先通过第二层应用机器人向“智算测试”授权群发送经典任务卡和 Excel；保存目标编制人平台 ID，群聊 / 单聊共用同一投递服务，精准单聊经主动触达权限实测后启用。
2. **模块 P0-B｜简易多人复核**：完成 `编制 → 并行多人复核 → 复核完成`，冻结成果版本，通过 WeAct 通知和安全链接进入网页处理；独立收集每名复核人的结论和可定位意见，形成意见汇总并由编制人逐条回复；所有复核人通过后完成，任一人退回则生成修改轮次，不强制增加审批人。
3. **模块 P1｜标准编审与风险协同**：增加风险派单、截止时间、催办、可选审批、正式发布和归档；验证企业 WeAct 原生审批或经典卡片回调后，再开放可靠的通过 / 退回轻量动作。
4. **模块 P2｜场景智能与管理治理**：增加材料检查、流程和复核人推荐、审核摘要、版本差异、退回意见转待办、多维表格元数据镜像、统计分析、权限审计和生产监控。

统一边界：网页和后端任务服务是业务状态、成果版本和审核结论的唯一真相；WeAct 只负责人员触达、提醒、安全跳转和经验证的轻量操作，不直接修改价格、系数、规则或风险等级。

## 6. 当前已知问题

| 问题 | 当前证据 | 建议责任模块 |
| --- | --- | --- |
| 第二真实专业能力资料缺失 | P0-B 运行时接口和测试隔离已完成，但当前没有第二专业的输入、计价依据、结构化数据、风险口径、模板和样例 | 待用户补充资料后进入模块 15 / P1，不复制勘察测量资产冒充 |
| Task 身份与执行证据仍分散 | 外部 `TaskEnvelope`、网页 Job、ProjectRun、Skill 快照、协同审核、成果和 Experience 血缘均真实存在，但用户无法沿同一 Task 一次看完 | 模块 19 / P0 统一身份、定义快照与只读执行轨迹；不重做业务状态机 |
| 专业页面的图形判断能力不足 | 现有图形主要集中在项目看板，偏离、候选、逐行风险、结算差额、多人复核、经验治理和版本证据仍主要依赖长列表或文字 | 模块 20 按 `VIS-01`—`VIS-15` 在责任页面增量实施；项目看板既有图表一项不减少，当前数据不足时诚实显示不足态 |
| 对话式工作台尚未持久化 | P0 已保留当前页面会话与任务，但刷新后的历史会话恢复、命名、搜索和保留策略尚未建设 | 模块 16 / P1 可恢复会话 |
| 数字化项目助手尚未联合运行验收 | 独立 MVP、嵌入主题和宿主入口已完成，但本轮未同时启动四个服务验证完整闭环 | 模块 14 / 双项目联合验收 |
| 智能协同生产化能力尚未完成 | P0-A 主动派发及 P0-B 简易多人复核、评论闭环已完成；精准单聊主动触达仍未实测，不可变成果冻结、P1 风险派单与可选审批、P2 场景智能仍待开发 | 模块 13 |
| 可信经验触发与失效治理尚未覆盖全部场景 | v5.20.0 已完成人工改单 / AI填价确认和多人复核意见两个触发、项目胶囊及真实指标；问答纠错、风险关闭、项目结项自动提取和正式依据 / Skill 更新后的批量失效扫描尚未接入 | 问问智算 / 可信经验进化闭环下一增量 |
| 知识记忆尚未接入企业身份 | 操作人 / 确认角色仍由页面填写，低风险通用知识自动通过仍是本实例试点策略 | 问问智算 / 身份与权限 |

## 7. 本阶段不做

- 不重构核心匹配引擎。
- 不让大模型直接裁决最终价格或系数。
- 不批量重命名历史目录、知识库文件、样例和兼容脚本。
- P0 不移动现有结构化计价库、规则表、经验池和报告模板，只通过 Manifest 引用。
- P0 不把“规划中”的通用服务类包装成已上线能力。
- P0 不建设在线 Skill 商店，不安装或执行任意外部 Skill 脚本。
- 对话式工作台不删除专业页面，不用模型文本伪造任务执行，不把全部聊天自动写入知识记忆。
- 不把数字化项目助手业务代码复制进造价智算。
- 不在专业主线和回归基线未收口前优先建设复杂审批、台账和组织权限。
- 不在当前 Windows 主线中强行推进 Tauri Linux、deb 或 AppImage。

## 8. 当前验收口径

- 新线程能在本文件中快速找到当前版本、最近完成、下一步第一优先级和真实阻塞。
- 模块 PRD 状态与代码、README、CHANGELOG 和跨项目交接状态一致。
- 完成项与未来增强分开表达，不把 MVP 完成等同于全部产品化完成。
- 新功能开始前有对应 PRD；完成后回写需求状态、验证结果和版本记录。
- 跨模块专业数据可视化已经满足完成门禁：`VIS-01`—`VIS-15` 全部进入真实页面，逐项数据口径、可用下钻、字体 / 响应式 / 完整状态、全量回归和 V3.1 Excel / Word 核对均通过；项目看板现有 KPI、八项图形（含五阶段漏斗）及其数据系列、筛选、联动、空态、历史表和汇报模式一项未减少。
- 当前核心业务验收的唯一现行基线是 `08-测试与演示/测试-V3.1-260802/`：使用其中 `【项目例子】【测试输入】可行性研究勘察测量控制价计算 -v3.1【批注-完备】.xlsx` 跑正式流程，生成的 Excel、Word 分别与两份 `【答案-对应版本v3.1】` 文件核对。旧 100 行、500 行测试表只作历史回归，自动化测试和构建通过不能替代本项验收。
